[
  {
    "id": "payments_by_customer",
    "question": "Show payments for each customer",
    "expected_tables": ["payments", "orders", "customers"],
    "forbidden_tables": ["audit_logs", "users"],
    "required_sql_fragments": [
      "payments.order_id = orders.id",
      "orders.customer_id = customers.id"
    ],
    "forbidden_sql_fragments": [
      "payments.customer_id",
      "SELECT *"
    ],
    "expected_columns": [
      "customers.name",
      "payments.amount"
    ],
    "max_attempts": 2,
    "sql_responses": [
      "SELECT customers.name, payments.amount FROM customers JOIN payments ON payments.customer_id = customers.id",
      "SELECT customers.name, payments.amount FROM customers JOIN orders ON orders.customer_id = customers.id JOIN payments ON payments.order_id = orders.id"
    ],
    "required_prompt_tables": ["payments", "orders", "customers"],
    "forbidden_prompt_tables": ["audit_logs", "users"],
    "required_prompt_edges": [
      "payments.order_id -> orders.id",
      "orders.customer_id -> customers.id"
    ]
  },
  {
    "id": "orders_by_customer",
    "question": "List orders by customer",
    "expected_tables": ["orders", "customers"],
    "forbidden_tables": ["payments", "audit_logs", "users"],
    "required_sql_fragments": [
      "orders.customer_id = customers.id"
    ],
    "forbidden_sql_fragments": [
      "payments",
      "audit_logs",
      "users",
      "SELECT *"
    ],
    "expected_columns": [
      "orders.id",
      "customers.name"
    ],
    "max_attempts": 1,
    "sql_responses": [
      "SELECT orders.id, customers.name FROM orders JOIN customers ON orders.customer_id = customers.id"
    ],
    "required_prompt_tables": ["orders", "customers"],
    "forbidden_prompt_tables": ["audit_logs", "users"],
    "required_prompt_edges": [
      "orders.customer_id -> customers.id"
    ]
  },
  {
    "id": "product_names_and_quantities",
    "question": "Show product names and quantities for order items in order number 123",
    "expected_tables": ["order_items", "products", "orders"],
    "forbidden_tables": ["customers", "audit_logs", "users"],
    "required_sql_fragments": [
      "order_items.product_id = products.id",
      "order_items.order_id = orders.id",
      "orders.id = 123"
    ],
    "forbidden_sql_fragments": [
      "customers",
      "audit_logs",
      "users",
      "SELECT *"
    ],
    "expected_columns": [
      "products.name",
      "order_items.quantity"
    ],
    "max_attempts": 1,
    "sql_responses": [
      "SELECT products.name, order_items.quantity FROM order_items JOIN products ON order_items.product_id = products.id JOIN orders ON order_items.order_id = orders.id WHERE orders.id = 123"
    ],
    "required_prompt_tables": ["order_items", "products", "orders"],
    "forbidden_prompt_tables": ["audit_logs", "users"],
    "required_prompt_edges": [
      "order_items.product_id -> products.id",
      "order_items.order_id -> orders.id"
    ]
  },
  {
    "id": "support_tickets_by_customer",
    "question": "List support tickets by customer",
    "expected_tables": ["support_tickets", "customers"],
    "forbidden_tables": ["orders", "payments", "audit_logs", "users"],
    "required_sql_fragments": [
      "support_tickets.customer_id = customers.id"
    ],
    "forbidden_sql_fragments": [
      "orders",
      "payments",
      "audit_logs",
      "users",
      "SELECT *"
    ],
    "expected_columns": [
      "support_tickets.id",
      "customers.name"
    ],
    "max_attempts": 1,
    "sql_responses": [
      "SELECT support_tickets.id, customers.name FROM support_tickets JOIN customers ON support_tickets.customer_id = customers.id"
    ],
    "required_prompt_tables": ["support_tickets", "customers"],
    "forbidden_prompt_tables": ["orders", "payments", "audit_logs", "users"],
    "required_prompt_edges": [
      "support_tickets.customer_id -> customers.id"
    ]
  },
  {
    "id": "audit_logs_for_user",
    "question": "Show latest audit log action for user account admin",
    "expected_tables": ["audit_logs", "users"],
    "forbidden_tables": ["customers", "orders", "payments"],
    "required_sql_fragments": [
      "audit_logs.user_id = users.id",
      "users.username = 'admin'",
      "ORDER BY audit_logs.timestamp DESC"
    ],
    "forbidden_sql_fragments": [
      "customers",
      "orders",
      "payments",
      "SELECT *"
    ],
    "expected_columns": [
      "audit_logs.action",
      "users.username"
    ],
    "max_attempts": 1,
    "sql_responses": [
      "SELECT audit_logs.action, users.username FROM audit_logs JOIN users ON audit_logs.user_id = users.id WHERE users.username = 'admin' ORDER BY audit_logs.timestamp DESC"
    ],
    "required_prompt_tables": ["audit_logs", "users"],
    "forbidden_prompt_tables": ["customers", "orders", "payments"],
    "required_prompt_edges": [
      "audit_logs.user_id -> users.id"
    ]
  }
]
