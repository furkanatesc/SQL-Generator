# Sprint 27.3 — User Feedback Capture Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Bir SQL üretimine (`job`) dair kullanıcı feedback'ini topla + sakla + 27.9 tüketicisine okunabilir hale getir (backend-only).

**Architecture:** Saf taksonomi paketi (`backend/app/feedback/`, stdlib-only) → append-only `feedback` tablosu + repo fonksiyonları (`backend/app/database.py`) → dedicated router (`backend/app/api/feedback.py`) + Pydantic schema'ları (`backend/app/api/schemas.py`). Bağımlılık tek yönlü; `feedback/` hiçbir şeyi import etmez.

**Tech Stack:** Python 3.12, FastAPI, Pydantic 2.13.4, SQLite (sqlite3), pytest. Kaynak spec: `docs/superpowers/specs/2026-07-22-sprint-27.3-user-feedback-capture-design.md`.

## Global Constraints

- `backend/app/feedback/` **SAF** kalır: stdlib dışına import YOK (purity testiyle kanıtlanır). `app.errors` deseninin birebir eşi.
- Sınırda değerler **string** taşınır (`enum.value`), enum değil — DB'de plain string; API response'unda `str`. (27.2.1 `error_code` deseni.)
- `feedback` tablosu **append-only**: her submit yeni satır; aynı `job_id`'ye N satır olabilir. Yabancı anahtar kısıtı YOK; `job_id` varlığı API katmanında kontrol edilir (404).
- Başarı status kodu **200** (envelope), 201 DEĞİL — kardeş `/api/jobs/{id}/cancel` konvansiyonu.
- 404 deseni birebir: `raise HTTPException(status_code=404, detail=f"Job with ID '{job_id}' not found.")` (global `http_exception_handler` bunu ErrorResponse envelope'una sarar).
- Pydantic cross-field: `@model_validator(mode="after")` (2.13.4 doğru API); uzunluk için `Field(..., max_length=N)`.
- Commit mesajlarına **Claude co-author / "Generated with" trailer'ı EKLENMEZ**.
- Test değişiklik kuralı: testler yalnız **kod string'i** değiştiği için değişebilir; bir assert'in **anlamı** değişiyorsa bu bir bug sinyalidir.
- Contract snapshot **kör bless edilmez**: yenilenir ve diff'in tam olarak feedback endpoint'i + yeni schema'lar olduğu doğrulanır; mevcut `required` listeleri değişmez.

---

### Task 1: Saf feedback taksonomisi paketi

**Files:**
- Create: `backend/app/feedback/__init__.py`
- Create: `backend/app/feedback/categories.py`
- Test: `backend/tests/feedback/test_feedback_categories.py`
- Test: `backend/tests/feedback/test_feedback_package_purity.py`

**Interfaces:**
- Consumes: yalnız stdlib (`enum.StrEnum`).
- Produces:
  - `from app.feedback import FeedbackVerdict, FeedbackCategory`
  - `FeedbackVerdict(StrEnum)`: `CORRECT="correct"`, `INCORRECT="incorrect"`
  - `FeedbackCategory(StrEnum)`: `WRONG_TABLE="wrong_table"`, `WRONG_COLUMN="wrong_column"`, `WRONG_FILTER="wrong_filter"`, `WRONG_JOIN="wrong_join"`, `WRONG_AGGREGATION="wrong_aggregation"`, `WRONG_ORDER_LIMIT="wrong_order_limit"`, `OTHER="other"`

- [ ] **Step 1: Test dizinini oluştur ve taksonomi değer testini yaz**

`backend/tests/feedback/test_feedback_categories.py`:

```python
"""Sprint 27.3 — feedback taksonomisi değerleri sabittir (27.2 StrEnum deseni)."""
from app.feedback import FeedbackVerdict, FeedbackCategory


def test_verdict_degerleri_sabit():
    assert FeedbackVerdict.CORRECT.value == "correct"
    assert FeedbackVerdict.INCORRECT.value == "incorrect"
    assert {v.value for v in FeedbackVerdict} == {"correct", "incorrect"}


def test_category_degerleri_sabit():
    assert {c.value for c in FeedbackCategory} == {
        "wrong_table", "wrong_column", "wrong_filter", "wrong_join",
        "wrong_aggregation", "wrong_order_limit", "other",
    }


def test_strenum_semantigi():
    # StrEnum üyesi str alt-tipidir ama tam `str` değildir; .value tam str'dir.
    # (27.2 error taksonomisiyle aynı sınır davranışı.)
    assert type(FeedbackCategory.OTHER) is not str
    assert type(FeedbackCategory.OTHER.value) is str
    assert FeedbackCategory.OTHER == "other"
```

- [ ] **Step 2: Testi çalıştır, import hatasıyla başarısız olduğunu doğrula**

Run: `cd backend && python -m pytest tests/feedback/test_feedback_categories.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'app.feedback'`

- [ ] **Step 3: Taksonomi paketini yaz**

`backend/app/feedback/categories.py`:

```python
"""Feedback taksonomisi (Sprint 27.3) — kullanıcı feedback'inin semantik ekseni.

Yaprak katman: stdlib dışına import YOKTUR. app.api ve 27.9 tüketicisi bunu
FastAPI/DB yüklemeden güvenle import eder (app.errors ile aynı disiplin).

Bu eksen 27.2'nin sistem/pipeline hata taksonomisinden FARKLIDIR: burada SQL
başarıyla üretilmiştir ama semantik olarak yanlış olabilir; kategori nesinin
yanlış olduğunu yakalar.
"""
from enum import StrEnum


class FeedbackVerdict(StrEnum):
    CORRECT = "correct"
    INCORRECT = "incorrect"


class FeedbackCategory(StrEnum):
    WRONG_TABLE = "wrong_table"
    WRONG_COLUMN = "wrong_column"
    WRONG_FILTER = "wrong_filter"
    WRONG_JOIN = "wrong_join"
    WRONG_AGGREGATION = "wrong_aggregation"
    WRONG_ORDER_LIMIT = "wrong_order_limit"
    OTHER = "other"
```

`backend/app/feedback/__init__.py`:

```python
"""Feedback taksonomisi (Sprint 27.3) — tek doğruluk kaynağı.

Yaprak katman: stdlib dışına import YOKTUR. app.api ve 27.9 bunu güvenle import eder.
"""
from app.feedback.categories import FeedbackCategory, FeedbackVerdict

__all__ = [
    "FeedbackCategory",
    "FeedbackVerdict",
]
```

- [ ] **Step 4: Değer testini çalıştır, geçtiğini doğrula**

Run: `cd backend && python -m pytest tests/feedback/test_feedback_categories.py -v`
Expected: PASS (3 passed)

- [ ] **Step 5: Purity testini yaz**

`backend/tests/feedback/test_feedback_package_purity.py`:

```python
"""app.feedback saflık guard'ı (Sprint 27.3 T1).

app.feedback yaprak katmandır: stdlib dışında hiçbir şey import etmez.
Bu, app.api ve 27.9 tüketicisinin onu FastAPI/DB yüklemeden import etmesini
güvenli kılar. Kardeş guard: test_errors_package_purity.py.
"""
import sys


def test_feedback_package_imports_no_app_modules_and_no_drivers():
    # app.feedback'i sys.modules'tan silip yeniden import ederek saflığı ölçer.
    # Orijinalleri try/finally ile geri yükle ki sonraki testler enum
    # sınıf-kimliğini korusun (test_errors_package_purity.py aynı deseni kullanır).
    saved = {m: sys.modules[m] for m in list(sys.modules)
             if m.startswith("app.feedback")}
    for mod in saved:
        del sys.modules[mod]
    try:
        before = set(sys.modules)
        import app.feedback  # noqa: F401
        newly_loaded = set(sys.modules) - before

        leaked_app = [m for m in newly_loaded
                      if m.startswith("app.") and not m.startswith("app.feedback")]
        assert leaked_app == [], f"app.feedback sızdırdı: {leaked_app}"

        for drv in ("psycopg", "psycopg2", "oracledb", "cx_Oracle", "sqlglot",
                    "sqlite3", "fastapi", "pydantic"):
            assert drv not in newly_loaded, f"app.feedback {drv} yükledi"
    finally:
        for m in [m for m in list(sys.modules) if m.startswith("app.feedback")]:
            del sys.modules[m]
        sys.modules.update(saved)
```

- [ ] **Step 6: Purity testini çalıştır, geçtiğini doğrula**

Run: `cd backend && python -m pytest tests/feedback/test_feedback_package_purity.py -v`
Expected: PASS (1 passed)

- [ ] **Step 7: Commit**

```bash
git add backend/app/feedback/ backend/tests/feedback/test_feedback_categories.py backend/tests/feedback/test_feedback_package_purity.py
git commit -m "feat(feedback): saf feedback taksonomisi paketi (verdict + category)"
```

---

### Task 2: Persistence — feedback tablosu + repo fonksiyonları

**Files:**
- Modify: `backend/app/database.py` (`init_db` içine tablo; dosya sonuna repo fonksiyonları)
- Test: `backend/tests/test_database_feedback.py`

**Interfaces:**
- Consumes: `get_db_connection()`, `init_db()` (mevcut).
- Produces:
  - `create_feedback(feedback_id: str, job_id: str, verdict: str, category: Optional[str] = None, note: Optional[str] = None, corrected_sql: Optional[str] = None) -> Dict[str, Any]` — INSERT eder, satırı dict döndürür.
  - `get_feedback(feedback_id: str) -> Optional[Dict[str, Any]]`
  - `get_feedback_for_job(job_id: str) -> List[Dict[str, Any]]` — `ORDER BY created_at ASC, rowid ASC`, yoksa `[]`.
  - Satır dict anahtarları: `id, job_id, verdict, category, note, corrected_sql, created_at`.

- [ ] **Step 1: DB testlerini yaz**

`backend/tests/test_database_feedback.py`:

```python
"""feedback tablosu persist davranışı (Sprint 27.3): append-only, job-bazlı okuma."""
import uuid

import pytest

from app.database import (
    create_feedback,
    create_job,
    get_feedback,
    get_feedback_for_job,
    init_db,
)


@pytest.fixture
def job_id():
    jid = f"test-fb-{uuid.uuid4()}"
    create_job(jid, natural_query="test sorgusu")
    return jid


def test_create_feedback_satiri_persist_eder_ve_doner(job_id):
    fid = str(uuid.uuid4())
    row = create_feedback(fid, job_id, "incorrect", category="wrong_filter",
                          note="tarih filtresi eksik")
    assert row["id"] == fid
    assert row["job_id"] == job_id
    assert row["verdict"] == "incorrect"
    assert row["category"] == "wrong_filter"
    assert row["note"] == "tarih filtresi eksik"
    assert row["corrected_sql"] is None
    assert row["created_at"]  # dolu
    # Ayrı okuma da aynı satırı görür.
    assert get_feedback(fid) == row


def test_correct_verdict_kategorisiz_saklanir(job_id):
    fid = str(uuid.uuid4())
    row = create_feedback(fid, job_id, "correct")
    assert row["verdict"] == "correct"
    assert row["category"] is None
    assert row["corrected_sql"] is None


def test_append_only_ayni_joba_coklu_satir_kronolojik(job_id):
    f1 = create_feedback(str(uuid.uuid4()), job_id, "incorrect",
                         category="wrong_table")
    f2 = create_feedback(str(uuid.uuid4()), job_id, "incorrect",
                         category="wrong_join", corrected_sql="SELECT 1")
    rows = get_feedback_for_job(job_id)
    assert len(rows) == 2
    # created_at ASC — ekleme sırası korunur.
    assert [r["id"] for r in rows] == [f1["id"], f2["id"]]


def test_bilinmeyen_job_bos_liste(job_id):
    assert get_feedback_for_job(f"yok-{uuid.uuid4()}") == []


def test_init_db_idempotent():
    # İki kez çağrılınca feedback tablosu/index bir kez kurulur, hata vermez.
    init_db()
    init_db()
    fid = str(uuid.uuid4())
    jid = f"test-fb-idem-{uuid.uuid4()}"
    create_job(jid, natural_query="x")
    create_feedback(fid, jid, "correct")
    assert get_feedback(fid)["verdict"] == "correct"
```

- [ ] **Step 2: Testi çalıştır, başarısız olduğunu doğrula**

Run: `cd backend && python -m pytest tests/test_database_feedback.py -v`
Expected: FAIL — `ImportError: cannot import name 'create_feedback' from 'app.database'`

- [ ] **Step 3: `feedback` tablosunu `init_db`'ye ekle**

`backend/app/database.py` — `init_db` içinde, `query_traces` `CREATE TABLE` bloğundan hemen sonra (mevcut `# Varsayılan bazı ayarları...` satırından ÖNCE) ekle:

```python
        # Feedback Tablosu (Sprint 27.3 — append-only)
        cursor.execute("""
            CREATE TABLE IF NOT EXISTS feedback (
                id TEXT PRIMARY KEY,
                job_id TEXT NOT NULL,
                verdict TEXT NOT NULL,
                category TEXT,
                note TEXT,
                corrected_sql TEXT,
                created_at TEXT NOT NULL
            )
        """)
        cursor.execute(
            "CREATE INDEX IF NOT EXISTS idx_feedback_job_id ON feedback (job_id)"
        )
```

- [ ] **Step 4: Repo fonksiyonlarını ekle**

`backend/app/database.py` — dosyanın SONUNA (`list_jobs`'tan sonra) ekle:

```python
# Feedback Helper Fonksiyonları (Sprint 27.3 — append-only)
def create_feedback(
    feedback_id: str,
    job_id: str,
    verdict: str,
    category: Optional[str] = None,
    note: Optional[str] = None,
    corrected_sql: Optional[str] = None,
) -> Dict[str, Any]:
    """feedback tablosuna append-only bir satır ekler ve döndürür.

    Enum değerleri sınırda (Pydantic) doğrulanmış PLAIN STRING olarak gelir;
    bu fonksiyon ince persister'dır ve app.feedback taksonomisini import ETMEZ.
    """
    import datetime
    now = datetime.datetime.utcnow().isoformat()
    with get_db_connection() as conn:
        cursor = conn.cursor()
        cursor.execute(
            """
            INSERT INTO feedback
                (id, job_id, verdict, category, note, corrected_sql, created_at)
            VALUES (?, ?, ?, ?, ?, ?, ?)
            """,
            (feedback_id, job_id, verdict, category, note, corrected_sql, now),
        )
        conn.commit()
    return get_feedback(feedback_id)


def get_feedback(feedback_id: str) -> Optional[Dict[str, Any]]:
    with get_db_connection() as conn:
        cursor = conn.cursor()
        cursor.execute("SELECT * FROM feedback WHERE id = ?", (feedback_id,))
        row = cursor.fetchone()
        return dict(row) if row else None


def get_feedback_for_job(job_id: str) -> List[Dict[str, Any]]:
    """Bir job'ın tüm feedback'ini kronolojik döndürür.

    27.9 tüketicisi bir job'ın tüm sinyal geçmişini bu fonksiyonla okur.
    Sıralama: created_at ASC, sonra rowid ASC. rowid tiebreak, aynı
    created_at (coarse clock) durumunda insertion-order'ı deterministik kılar
    — aksi halde eş-zaman-damgalı satırların sırası tanımsız kalırdı.
    Bilinmeyen job_id → boş liste (patlamaz).
    """
    with get_db_connection() as conn:
        cursor = conn.cursor()
        cursor.execute(
            "SELECT * FROM feedback WHERE job_id = ? "
            "ORDER BY created_at ASC, rowid ASC",
            (job_id,),
        )
        rows = cursor.fetchall()
        return [dict(row) for row in rows]
```

- [ ] **Step 5: Testi çalıştır, geçtiğini doğrula**

Run: `cd backend && python -m pytest tests/test_database_feedback.py -v`
Expected: PASS (5 passed)

> Not: `test_append_only_ayni_joba_coklu_satir_kronolojik` sıralaması `ORDER BY created_at ASC, rowid ASC` ile deterministiktir — aynı `created_at` (coarse clock) durumunda bile `rowid` insertion-order'ı garanti eder, test flaky olmaz. Sıralama assert'i gevşetilirse bu bir gerçek garanti-kaybı sinyalidir.

- [ ] **Step 6: Commit**

```bash
git add backend/app/database.py backend/tests/test_database_feedback.py
git commit -m "feat(feedback): append-only feedback tablosu + repo fonksiyonlari"
```

---

### Task 3: API — schema'lar + dedicated router + mount

**Files:**
- Modify: `backend/app/api/schemas.py` (yeni schema'lar + import'lar)
- Create: `backend/app/api/feedback.py` (router)
- Modify: `backend/app/main.py` (router import + include)
- Test: `backend/tests/test_api_feedback_contract.py`

**Interfaces:**
- Consumes: Task 1 (`FeedbackVerdict`, `FeedbackCategory`), Task 2 (`create_feedback`, `get_job`), `app.auth.verify_api_key`, `ErrorResponse` (schemas.py mevcut).
- Produces:
  - `FeedbackSubmitRequest(BaseModel)`: `verdict: FeedbackVerdict`, `category: Optional[FeedbackCategory]=None`, `note: Optional[str]=Field(None, max_length=2000)`, `corrected_sql: Optional[str]=Field(None, max_length=5000)` + `_check_invariants` model_validator.
  - `FeedbackDetailResponse(BaseModel)`: `id, job_id, verdict: str`; `category, note, corrected_sql: Optional[str]=None`; `created_at: str`.
  - `FeedbackEnvelopeResponse(BaseModel)`: `status: Literal["success"]`, `feedback: FeedbackDetailResponse`.
  - `POST /api/jobs/{job_id}/feedback` → 200 envelope | 404 | 422.

- [ ] **Step 1: API testlerini yaz**

`backend/tests/test_api_feedback_contract.py`:

```python
"""Sprint 27.3 — POST /api/jobs/{id}/feedback API sözleşmesi.

Mevcut test_api_job_error_code_contract.py TestClient desenini izler.
"""
import uuid

from fastapi.testclient import TestClient

from app.main import app
from app.database import create_job, get_feedback_for_job

client = TestClient(app)
HEADERS = {"X-API-Key": "sqlgen_secret_dev_key"}


def _new_job():
    return create_job(f"job-fb-{uuid.uuid4()}", natural_query="test")["id"]


def test_gecerli_incorrect_feedback_200_envelope():
    jid = _new_job()
    res = client.post(
        f"/api/jobs/{jid}/feedback",
        headers=HEADERS,
        json={"verdict": "incorrect", "category": "wrong_filter",
              "note": "tarih filtresi eksik"},
    )
    assert res.status_code == 200
    body = res.json()
    assert body["status"] == "success"
    fb = body["feedback"]
    assert fb["id"]
    assert fb["job_id"] == jid
    assert fb["verdict"] == "incorrect"
    assert fb["category"] == "wrong_filter"
    assert fb["note"] == "tarih filtresi eksik"
    assert fb["corrected_sql"] is None
    assert fb["created_at"]


def test_correct_verdict_kategorisiz_200():
    jid = _new_job()
    res = client.post(f"/api/jobs/{jid}/feedback", headers=HEADERS,
                      json={"verdict": "correct"})
    assert res.status_code == 200
    assert res.json()["feedback"]["verdict"] == "correct"


def test_var_olmayan_job_404():
    res = client.post(f"/api/jobs/yok-{uuid.uuid4()}/feedback", headers=HEADERS,
                      json={"verdict": "correct"})
    assert res.status_code == 404
    # Global handler ErrorResponse envelope'una sarar.
    assert "error" in res.json()


def test_gecersiz_verdict_422():
    jid = _new_job()
    res = client.post(f"/api/jobs/{jid}/feedback", headers=HEADERS,
                      json={"verdict": "maybe"})
    assert res.status_code == 422


def test_invariant2_correct_plus_category_422():
    jid = _new_job()
    res = client.post(f"/api/jobs/{jid}/feedback", headers=HEADERS,
                      json={"verdict": "correct", "category": "wrong_table"})
    assert res.status_code == 422


def test_invariant2_correct_plus_corrected_sql_422():
    jid = _new_job()
    res = client.post(f"/api/jobs/{jid}/feedback", headers=HEADERS,
                      json={"verdict": "correct", "corrected_sql": "SELECT 1"})
    assert res.status_code == 422


def test_invariant3_other_note_yok_422():
    jid = _new_job()
    res = client.post(f"/api/jobs/{jid}/feedback", headers=HEADERS,
                      json={"verdict": "incorrect", "category": "other"})
    assert res.status_code == 422


def test_invariant3_other_bos_note_422():
    jid = _new_job()
    res = client.post(f"/api/jobs/{jid}/feedback", headers=HEADERS,
                      json={"verdict": "incorrect", "category": "other",
                            "note": "   "})
    assert res.status_code == 422


def test_other_note_ile_200():
    jid = _new_job()
    res = client.post(f"/api/jobs/{jid}/feedback", headers=HEADERS,
                      json={"verdict": "incorrect", "category": "other",
                            "note": "başka bir sebep"})
    assert res.status_code == 200


def test_note_asiri_uzun_422():
    jid = _new_job()
    res = client.post(f"/api/jobs/{jid}/feedback", headers=HEADERS,
                      json={"verdict": "incorrect", "category": "wrong_table",
                            "note": "x" * 2001})
    assert res.status_code == 422


def test_corrected_sql_asiri_uzun_422():
    jid = _new_job()
    res = client.post(f"/api/jobs/{jid}/feedback", headers=HEADERS,
                      json={"verdict": "incorrect", "category": "wrong_join",
                            "corrected_sql": "x" * 5001})
    assert res.status_code == 422


def test_api_key_yoksa_403():
    jid = _new_job()
    res = client.post(f"/api/jobs/{jid}/feedback",
                      json={"verdict": "correct"})
    assert res.status_code == 403


def test_append_only_iki_post_iki_satir():
    jid = _new_job()
    client.post(f"/api/jobs/{jid}/feedback", headers=HEADERS,
                json={"verdict": "incorrect", "category": "wrong_table"})
    client.post(f"/api/jobs/{jid}/feedback", headers=HEADERS,
                json={"verdict": "incorrect", "category": "wrong_join"})
    rows = get_feedback_for_job(jid)
    assert len(rows) == 2
```

- [ ] **Step 2: Testi çalıştır, başarısız olduğunu doğrula**

Run: `cd backend && python -m pytest tests/test_api_feedback_contract.py -v`
Expected: FAIL — feedback endpoint yok (404 tüm başarı testlerinde, veya schema import hatası).

- [ ] **Step 3: Schema'ları ekle**

`backend/app/api/schemas.py` — üstteki import satırlarını güncelle:

```python
from pydantic import BaseModel, Field, model_validator
from typing import Optional, Dict, Any, List, Literal
from enum import Enum

from app.feedback import FeedbackVerdict, FeedbackCategory
```

Aynı dosyanın SONUNA (`ErrorResponse`'tan sonra) ekle:

```python
# Sprint 27.3 - Feedback capture schemas
class FeedbackSubmitRequest(BaseModel):
    verdict: FeedbackVerdict
    category: Optional[FeedbackCategory] = None
    note: Optional[str] = Field(default=None, max_length=2000)
    corrected_sql: Optional[str] = Field(default=None, max_length=5000)

    @model_validator(mode="after")
    def _check_invariants(self):
        # (2) doğru SQL'in hata kategorisi/düzeltmesi olmaz.
        if self.verdict == FeedbackVerdict.CORRECT:
            if self.category is not None or self.corrected_sql is not None:
                raise ValueError(
                    "verdict=correct ile category/corrected_sql verilemez"
                )
        # (3) 'other' serbest-metin gerekçe ister; aksi halde 27.9 için ölü sinyal.
        if self.category == FeedbackCategory.OTHER and not (
            self.note and self.note.strip()
        ):
            raise ValueError("category=other için note zorunludur")
        return self


class FeedbackDetailResponse(BaseModel):
    id: str
    job_id: str
    verdict: str
    category: Optional[str] = None
    note: Optional[str] = None
    corrected_sql: Optional[str] = None
    created_at: str


class FeedbackEnvelopeResponse(BaseModel):
    status: Literal["success"]
    feedback: FeedbackDetailResponse
```

- [ ] **Step 4: Router'ı oluştur**

`backend/app/api/feedback.py`:

```python
"""Feedback capture router (Sprint 27.3).

api/debug_traces.py deseni: kendi APIRouter'ı, main.py'de mount edilir.
Akış: Pydantic + cross-field invariant (422) → get_job yok ise 404 →
create_feedback (append-only) → 200 envelope.
"""
import uuid

from fastapi import APIRouter, Depends, HTTPException

from app.auth import verify_api_key
from app.database import create_feedback, get_job
from app.api.schemas import (
    ErrorResponse,
    FeedbackEnvelopeResponse,
    FeedbackSubmitRequest,
)

router = APIRouter(
    prefix="/api/jobs",
    tags=["feedback"],
    dependencies=[Depends(verify_api_key)],
)


@router.post(
    "/{job_id}/feedback",
    response_model=FeedbackEnvelopeResponse,
    responses={404: {"model": ErrorResponse}, 422: {"model": ErrorResponse}},
)
def submit_feedback(job_id: str, payload: FeedbackSubmitRequest):
    if get_job(job_id) is None:
        raise HTTPException(
            status_code=404, detail=f"Job with ID '{job_id}' not found."
        )
    feedback = create_feedback(
        feedback_id=str(uuid.uuid4()),
        job_id=job_id,
        # Sınırda PLAIN STRING taşınır (enum değil) — 27.2.1 error_code deseni.
        verdict=payload.verdict.value,
        category=payload.category.value if payload.category is not None else None,
        note=payload.note,
        corrected_sql=payload.corrected_sql,
    )
    return {"status": "success", "feedback": feedback}
```

- [ ] **Step 5: Router'ı main.py'de mount et**

`backend/app/main.py` — mevcut `from app.api.debug_traces import router as debug_traces_router` satırının hemen ALTINA ekle:

```python
from app.api.feedback import router as feedback_router
```

Mevcut `app.include_router(debug_traces_router)` satırının hemen ALTINA ekle:

```python
app.include_router(feedback_router)
```

- [ ] **Step 6: Testi çalıştır, geçtiğini doğrula**

Run: `cd backend && python -m pytest tests/test_api_feedback_contract.py -v`
Expected: PASS (13 passed)

- [ ] **Step 7: Purity testinin hâlâ geçtiğini doğrula (regresyon guard)**

schemas.py artık `app.feedback` import ediyor; bu, `app.feedback`'in kendi saflığını bozmaz (bağımlılık tek yönlü). Doğrula:

Run: `cd backend && python -m pytest tests/feedback/test_feedback_package_purity.py -v`
Expected: PASS (1 passed)

- [ ] **Step 8: Commit**

```bash
git add backend/app/api/schemas.py backend/app/api/feedback.py backend/app/main.py backend/tests/test_api_feedback_contract.py
git commit -m "feat(feedback): POST /api/jobs/{id}/feedback endpoint + schema invariantlari"
```

---

### Task 4: Contract snapshot yenileme + diff doğrulama

**Files:**
- Modify: `backend/tests/contracts/openapi_snapshot.json` (üretilir)
- Modify: `backend/tests/contracts/schema_contract_snapshot.json` (üretilir)
- Modify: `backend/tests/contracts/status_contract_snapshot.json` (değişmemeli — status semantiği aynı)

**Interfaces:**
- Consumes: Task 3'ün eklediği endpoint + schema'lar (OpenAPI'ye yansır).
- Produces: güncel snapshot'lar; `test_api_surface.py` / `test_response_contract.py` yeşil.

- [ ] **Step 1: Yenilemeden ÖNCE contract testlerinin kırmızı olduğunu gör (yüzey genişledi)**

Run: `cd backend && python -m pytest tests/test_api_surface.py tests/test_response_contract.py tests/test_status_contract.py -v`
Expected: FAIL — openapi/schema snapshot yeni feedback path'i + schema'ları içermiyor (yüzey gerçekten genişledi). status testi değişmemeli.

- [ ] **Step 2: Snapshot'ları yenile**

Run: `cd backend && python scripts/generate_contract_snapshots.py`
Expected çıktı: üç "Generated deterministic ... snapshot at:" satırı.

- [ ] **Step 3: Diff'in TAM OLARAK feedback eklemeleri olduğunu doğrula (kör bless değil)**

Run: `git diff --stat backend/tests/contracts/` — yalnız `openapi_snapshot.json` ve `schema_contract_snapshot.json` değişmeli; `status_contract_snapshot.json` DEĞİŞMEMELİ.

Run: `git diff backend/tests/contracts/schema_contract_snapshot.json`
Doğrula — diff yalnızca EKLEMEDİR:
- Yeni schema anahtarları: `FeedbackSubmitRequest`, `FeedbackDetailResponse`, `FeedbackEnvelopeResponse`, ayrıca `FeedbackVerdict` / `FeedbackCategory` enum şemaları.
- **Mevcut hiçbir schema'nın `required` veya `properties` listesi değişmemeli** (yalnız ekleme → eski client'lar etkilenmez).

Run: `git diff backend/tests/contracts/openapi_snapshot.json | grep -E '^\+.*"/api/jobs/\{job_id\}/feedback"'`
Expected: yeni feedback path'inin eklendiğini gösterir.

> ⚠️ Eğer diff yukarıdakiler DIŞINDA bir şey içeriyorsa (mevcut bir `required` listesi değişmiş, alakasız path oynamış) DUR — bu istenmeyen bir yüzey değişikliği sinyalidir; sessizce bless etme, kök nedeni bul.

- [ ] **Step 4: Contract testlerini tekrar çalıştır, yeşil olduğunu doğrula**

Run: `cd backend && python -m pytest tests/test_api_surface.py tests/test_response_contract.py tests/test_status_contract.py -v`
Expected: PASS

- [ ] **Step 5: Tam backend suite — 0 failed**

Run: `cd backend && python -m pytest -q`
Expected: 0 failed (yeni feedback testleri dahil).

- [ ] **Step 6: Commit**

```bash
git add backend/tests/contracts/
git commit -m "test(feedback): contract snapshotlari yenilendi (feedback endpoint yuzeyi)"
```

---

### Task 5: Yaşayan dokümanlar + kapanış

**Files:**
- Modify: `CHANGELOG.md`
- Modify: `ROADMAP.md`
- Modify: `docs/PLANNED-SPRINTS.md`
- Modify: `docs/SPRINT-PR-LOG.md`
- Modify: `C:\Users\furkan\.claude\projects\C--Users-furkan-Desktop-YLveProjveMeslek-SQLGen\memory\MEMORY.md` + yeni memory dosyası

**Interfaces:**
- Consumes: tamamlanan Task 1-4.
- Produces: senkron yaşayan dokümanlar (global kural: karar değişince aynı turda güncelle; sessiz düşürme yok).

- [ ] **Step 1: CHANGELOG güncelle**

`CHANGELOG.md` `## [Unreleased]` altındaki `### Added` bölümüne ekle (yoksa oluştur):

```markdown
- **Sprint 27.3 — User Feedback Capture:** Bir SQL üretimine (`job`) dair kullanıcı
  feedback'ini toplayan backend yüzeyi. `POST /api/jobs/{job_id}/feedback` endpoint'i,
  append-only `feedback` tablosu ve saf `FeedbackVerdict`/`FeedbackCategory` taksonomisi
  (`backend/app/feedback/`). Öğrenen sistemin tohumu; 27.9 (Feedback Review → Rule
  Suggestion) `get_feedback_for_job` ile tüketecek. Backend-only: HTTP GET listeleme
  (30.6), frontend UI (31.7) ve `rating` alanı bilinçli olarak kapsam dışı.
```

- [ ] **Step 2: ROADMAP + PLANNED-SPRINTS güncelle**

Önce mevcut format/satırları oku:

Run: `grep -n "27\.3\|27\.4\|Tamamlan\|Sıradaki\|Phase 8" ROADMAP.md docs/PLANNED-SPRINTS.md`

Sonra iki dosyada da:
- **27.3**'ü ✅ / DONE işaretle.
- "Tamamlandı" aralığını / "Sıradaki sprint"i **27.4 Query Replay System**'e çek.
- Phase 8 ilerleme satırını 27.3'ü içerecek şekilde güncelle.

(Kesin metin dosyanın mevcut biçimine göre; yalnız durum alanlarını değiştir, yapıyı yeniden yazma.)

- [ ] **Step 3: SPRINT-PR-LOG güncelle**

`docs/SPRINT-PR-LOG.md` — mevcut satır biçimini izleyerek 27.3 satırı ekle:

```markdown
| 27.3 | User Feedback Capture | feedback taksonomisi + append-only tablo + POST /api/jobs/{id}/feedback | PR #<TBD> |
```

(PR numarası merge sırasında güncellenir; şimdilik `#TBD` — sessiz boş bırakma yok.)

- [ ] **Step 4: Memory kapanış notu**

`memory/sprint-27-3-user-feedback-capture.md` oluştur (type: project); `feat/user-feedback-capture` branch, kapsam (backend-only capture), ertelenenler (30.6 GET/admin, 31.7 UI, rating, 27.9 tüketim), sıradaki sprint 27.4. `MEMORY.md`'ye tek satır pointer ekle ve önceki "next sprint is 27.3" satırını 27.3 DONE olacak şekilde güncelle.

- [ ] **Step 5: Commit**

```bash
git add CHANGELOG.md ROADMAP.md docs/PLANNED-SPRINTS.md docs/SPRINT-PR-LOG.md
git commit -m "docs: Sprint 27.3 kapanis — changelog, roadmap, PR-log senkronu"
```

(Memory dosyaları repo dışı; ayrıca Write ile güncellenir, commit'e girmez.)

---

## Self-Review Notları (yazım sonrası kontrol)

- **Spec coverage:** §3.1 taksonomi → Task 1; §3.2 persistence → Task 2; §3.3 API/schema → Task 3; §3.4 invariant'lar → Task 3 testleri (invariant 2/3, uzunluk); §5 test stratejisi → her task'ta TDD; §5 contract → Task 4; §6 yaşayan doküman → Task 5. §2 kapsam-dışı kalemler (rating, GET listeleme, frontend, attribution) hiçbir task'ta uygulanmaz (bilinçli).
- **Type consistency:** `create_feedback(feedback_id, job_id, verdict, category, note, corrected_sql)` imzası Task 2 (tanım) ↔ Task 3 (çağrı) arasında birebir; response alan adları `id/job_id/verdict/category/note/corrected_sql/created_at` DB satırı ↔ `FeedbackDetailResponse` ↔ API testleri arasında tutarlı.
- **Boundary string:** router `.value` geçer; DB string saklar; response `str` alanları — enum sınırı geçmez (Global Constraints).
