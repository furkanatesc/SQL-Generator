# Sprint 31.8 — Connection Onboarding / Schema Import UX Implementation Plan

> Frontend (Vue 3). Verify: `cd frontend && npm run build` → 0 errors, exit 0. Phase 12 SON sprint.
> Spec: `docs/superpowers/specs/2026-09-10-sprint-31.8-connection-schema-import-ux-design.md`

**Goal:** Surface schema import/refresh failures in SchemaManager (captured `schemaError` + retry),
and split the error state from the empty state. No backend change.

## Global Constraints
- Edit only `frontend/src/components/SchemaManager.vue` (+ docs). No new deps.
- Verify = build exit 0. Commit: plain, NO trailer / co-author / "Generated with".

---

### Task 1: Capture + surface schema load error

- [ ] **Step 1:** Add `const schemaError = ref<string | null>(null);`.
- [ ] **Step 2:** `loadSchema`: set `schemaError.value = null` at start; in catch set a Turkish message.
- [ ] **Step 3:** Template — split the single fallback into:
  - error (`v-else-if="schemaError"`): red card + message + "Tekrar dene" → `loadSchema(true)`.
  - empty (`v-else-if="!schema || Object.keys(schema.tables||{}).length===0"`): neutral card + "Şemayı içe aktar / Yenile" → `loadSchema(true)`.
- [ ] **Step 4: Verify** — `cd frontend && npm run build` → 0 errors, exit 0.
- [ ] **Step 5: Commit** — `git commit -m "feat(31.8): surface schema import/refresh errors with retry + distinct empty state in SchemaManager"`

### Task 2: Living docs + Phase 12 close

- [ ] Roadmap/planned-sprints/changelog/pr-log `#N` backfill; mark **Phase 12 complete**, next Phase 13.

## Self-Review
Spec §2 → Task 1; §3 verify → Step 4; §4 out-of-scope (no connection CRUD, no live test). Single file, no backend change. ✓

## Controller finishing flow
Self-review → push/PR → backend CI (frontend-only → green) → `#N` backfill (Phase 12 KAPANDI) → merge `--rebase --delete-branch` → main pull → memory: archive 31.8, flip `durum.md` to Phase 13 (read PLANNED-SPRINTS Phase 13 — likely backend/ops domain), update `MEMORY.md`. NO commit trailer.
