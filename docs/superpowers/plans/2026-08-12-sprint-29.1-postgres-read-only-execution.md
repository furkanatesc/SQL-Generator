# PostgreSQL Read-Only Execution Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 25.8'in inert PostgreSQL adapter'ını, mevcut dialect-agnostik execution kontratına (`SQLDatabaseExecutionAdapter`) bağlı, local-Docker-only read-only bir Postgres execution adapter'ına dönüştürmek.

**Architecture:** Yeni `PostgresDatabaseExecutionAdapter`, `SQLDatabaseExecutionAdapter` ABC'sini implemente eder; connection'ı 29.0 resolver'la çözer, gerçek SELECT'i 25.8 `SQLPostgresAdapterContract`'a delege eder, sonucu **saf bir normalize fonksiyonuyla** ortak `SQLDatabaseExecutionResult`'a çevirir. Dict satır üretmek için 25.8'e geriye-uyumlu `columns` alanı eklenir. Yan-etkisiz; canlı HTTP wiring bilinçli kapsam dışı (30.x).

**Tech Stack:** Python 3, pytest, psycopg2 (yalnız execution path'inde lazy import), Docker Postgres (gated integration).

## Global Constraints

- **Driver-isolation:** Yeni modülün import'u import-time'da `psycopg2` veya socket/network client YÜKLEMEZ. `psycopg2` yalnız 25.8'in `_execute_select`'inde lazy import edilir. Modül `app.evaluation` altında yaşar.
- **Local-Docker-only, read-only:** Uzak/production connection yok; yalnız `SQLExecutionMode.READ_ONLY`. EXPLAIN_ONLY → 29.2.
- **Secret-free:** Sonuçlar yalnız `sql_sha256` + sanitize edilmiş `execution_error` + kolon adları + satır değerleri taşır; connection detayı asla.
- **Yan-etkisiz:** Hiçbir store/persistence yazımı yok.
- **Determinizm:** `datetime.now()`/rasgelelik yok; `duration_ms` monotonic.
- **Commit trailer yok:** Düz commit mesajı; co-author / "Generated with" satırı yok.
- **Contract enum eşdeğerleri (verbatim):** `SQLDatabaseDialect.POSTGRESQL`, `SQLExecutionMode.READ_ONLY`, `SQLExecutionAdapterCapability.CONNECTION_REF`, `SQLExecutionAdapterCapability.READ_ONLY`. 25.8 dialect string'i `"postgresql"`, `SQLPostgresAdapterConfig.execution_mode="read_only"`.

---

### Task 1: 25.8 adapter'a geriye-uyumlu `columns` alanı

**Files:**
- Modify: `backend/app/evaluation/postgres_adapter.py`
- Test: `backend/tests/evaluation/test_postgres_adapter.py`

**Interfaces:**
- Produces: `SQLPostgresAdapterExecutionResult.columns: Tuple[str, ...]` (default `()`), `to_dict()` içinde `"columns": list(self.columns)`. `EXECUTED` sonucunda `cur.description`'dan doldurulur.

- [ ] **Step 1: Write the failing tests**

`backend/tests/evaluation/test_postgres_adapter.py` sonuna ekle:

```python
def test_execution_result_columns_defaults_empty():
    from app.evaluation.postgres_adapter import (
        SQLPostgresAdapterExecutionResult,
        SQL_POSTGRES_ADAPTER_CONTRACT_VERSION,
        SQLPostgresAdapterStatus,
    )
    import hashlib
    sql = "SELECT 1"
    res = SQLPostgresAdapterExecutionResult(
        version=SQL_POSTGRES_ADAPTER_CONTRACT_VERSION,
        case_id="c1",
        status=SQLPostgresAdapterStatus.NOT_IMPLEMENTED,
        sql_sha256=hashlib.sha256(sql.encode()).hexdigest(),
    )
    assert res.columns == ()
    assert res.to_dict()["columns"] == []


def test_execution_result_columns_roundtrip_and_validation():
    from app.evaluation.postgres_adapter import (
        SQLPostgresAdapterExecutionResult,
        SQL_POSTGRES_ADAPTER_CONTRACT_VERSION,
        SQLPostgresAdapterStatus,
        SQLPostgresAdapterContractError,
    )
    import hashlib, pytest
    sql = "SELECT id, name FROM customers"
    h = hashlib.sha256(sql.encode()).hexdigest()
    res = SQLPostgresAdapterExecutionResult(
        version=SQL_POSTGRES_ADAPTER_CONTRACT_VERSION,
        case_id="c1",
        status=SQLPostgresAdapterStatus.EXECUTED,
        sql_sha256=h,
        rows=((1, "a"),),
        row_count=1,
        columns=("id", "name"),
    )
    assert res.to_dict()["columns"] == ["id", "name"]
    with pytest.raises(SQLPostgresAdapterContractError):
        SQLPostgresAdapterExecutionResult(
            version=SQL_POSTGRES_ADAPTER_CONTRACT_VERSION, case_id="c1",
            status=SQLPostgresAdapterStatus.EXECUTED, sql_sha256=h,
            columns=("id", 5),  # non-str
        )
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `cd backend && python -m pytest tests/evaluation/test_postgres_adapter.py -k columns -v`
Expected: FAIL (`TypeError: unexpected keyword argument 'columns'`)

- [ ] **Step 3: Implement**

`SQLPostgresAdapterExecutionResult`'a alanı ekle (diğer `field(default_factory=tuple)` alanlarının yanına):

```python
    columns: Tuple[str, ...] = field(default_factory=tuple)
```

`__post_init__` içine (warnings validasyonunun yanına) ekle:

```python
        if not isinstance(self.columns, tuple):
            raise SQLPostgresAdapterContractError("columns must be a tuple")
        for c in self.columns:
            if not isinstance(c, str):
                raise SQLPostgresAdapterContractError("All columns must be strings")
```

`to_dict()` dönüşüne ekle (`"warnings"` satırından sonra):

```python
            "columns": list(self.columns),
```

`_execute_select` içinde başarılı fetch bloğunda, `db.rollback()` ÖNCESİNDE kolon adlarını yakala ve `EXECUTED` sonucuna geçir:

```python
                cur.execute(request.sql)
                fetched = cur.fetchmany(max_rows + 1)
                description = cur.description
            db.rollback()

            columns = tuple(d[0] for d in description) if description else ()
            truncated = len(fetched) > max_rows
            rows = tuple(_normalize_row(r) for r in fetched[:max_rows])
            duration_ms = (time.monotonic() - started) * 1000.0
            warnings = ("result truncated to max_rows",) if truncated else ()
            return self._result(
                request, sql_hash, SQLPostgresAdapterStatus.EXECUTED,
                rows=rows, row_count=len(rows), truncated=truncated,
                columns=columns, warnings=warnings, duration_ms=duration_ms,
            )
```

`_result` helper'ına `columns` parametresini ekle (keyword-only, default `()`):

```python
    def _result(
        self,
        request: SQLPostgresAdapterExecutionRequest,
        sql_hash: str,
        status: SQLPostgresAdapterStatus,
        *,
        rows: Tuple[Tuple[Any, ...], ...] = (),
        row_count: int = 0,
        truncated: bool = False,
        columns: Tuple[str, ...] = (),
        error: Optional[str] = None,
        warnings: Tuple[str, ...] = (),
        duration_ms: float = 0.0,
    ) -> SQLPostgresAdapterExecutionResult:
        return SQLPostgresAdapterExecutionResult(
            version=SQL_POSTGRES_ADAPTER_CONTRACT_VERSION,
            case_id=request.case_id,
            status=status,
            sql_sha256=sql_hash,
            rows=rows,
            row_count=row_count,
            truncated=truncated,
            columns=tuple(columns),
            error=error,
            warnings=tuple(warnings),
            duration_ms=duration_ms,
        )
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `cd backend && python -m pytest tests/evaluation/test_postgres_adapter.py -v`
Expected: PASS (tümü — mevcut testler de yeşil kalmalı, `columns` opsiyonel)

- [ ] **Step 5: Commit**

```bash
git add backend/app/evaluation/postgres_adapter.py backend/tests/evaluation/test_postgres_adapter.py
git commit -m "feat(29.1): capture column names in postgres adapter result (backward-compat)"
```

---

### Task 2: Saf normalize fonksiyonu (25.8 sonucu → ortak sözleşme)

**Files:**
- Create: `backend/app/evaluation/postgres_execution_adapter.py`
- Test: `backend/tests/evaluation/test_postgres_execution_adapter.py`

**Interfaces:**
- Consumes: Task 1'in `SQLPostgresAdapterExecutionResult` (+`columns`); `SQLDatabaseExecutionResult`, `SQLDatabaseExecutionRequest`, `SQLDatabaseDialect`, `SQL_MULTI_DATABASE_EXECUTION_VERSION` (`multi_database_execution`); `SQLPostgresAdapterStatus`.
- Produces: `normalize_postgres_execution_result(pg: SQLPostgresAdapterExecutionResult, request: SQLDatabaseExecutionRequest) -> SQLDatabaseExecutionResult`.

- [ ] **Step 1: Write the failing tests**

`backend/tests/evaluation/test_postgres_execution_adapter.py`:

```python
import hashlib
import pytest

from app.evaluation.multi_database_execution import (
    SQLDatabaseDialect,
    SQLExecutionMode,
    SQLDatabaseExecutionConfig,
    SQLDatabaseExecutionRequest,
    SQLDatabaseExecutionResult,
)
from app.evaluation.postgres_adapter import (
    SQLPostgresAdapterExecutionResult,
    SQLPostgresAdapterStatus,
    SQL_POSTGRES_ADAPTER_CONTRACT_VERSION,
)
from app.evaluation.postgres_execution_adapter import (
    normalize_postgres_execution_result,
)

SQL = "SELECT id, name FROM customers"


def _request(sql=SQL):
    cfg = SQLDatabaseExecutionConfig(dialect=SQLDatabaseDialect.POSTGRESQL)
    return SQLDatabaseExecutionRequest(
        case_id="c1", sql=sql, dialect=SQLDatabaseDialect.POSTGRESQL,
        fixture_ref=None, connection_ref="local_docker", config=cfg,
    )


def _pg(status, **kw):
    h = hashlib.sha256(SQL.encode()).hexdigest()
    return SQLPostgresAdapterExecutionResult(
        version=SQL_POSTGRES_ADAPTER_CONTRACT_VERSION, case_id="c1",
        status=status, sql_sha256=h, **kw,
    )


def test_normalize_executed_maps_rows_to_dicts():
    pg = _pg(SQLPostgresAdapterStatus.EXECUTED,
             rows=((1, "a"), (2, "b")), row_count=2,
             columns=("id", "name"), duration_ms=1.5)
    out = normalize_postgres_execution_result(pg, _request())
    assert isinstance(out, SQLDatabaseExecutionResult)
    assert out.dialect == SQLDatabaseDialect.POSTGRESQL
    assert out.rows == ({"id": 1, "name": "a"}, {"id": 2, "name": "b"})
    assert out.row_count == 2
    assert out.execution_error is None
    assert out.sql_sha256 == hashlib.sha256(SQL.encode()).hexdigest()


def test_normalize_executed_column_count_mismatch_falls_back_with_warning():
    pg = _pg(SQLPostgresAdapterStatus.EXECUTED,
             rows=((1, "a"),), row_count=1, columns=("id",))  # 1 col, 2 values
    out = normalize_postgres_execution_result(pg, _request())
    assert out.rows == ({"col_0": 1, "col_1": "a"},)
    assert any("column" in w.lower() for w in out.warnings)


@pytest.mark.parametrize("status", [
    SQLPostgresAdapterStatus.REJECTED,
    SQLPostgresAdapterStatus.EXECUTION_ERROR,
    SQLPostgresAdapterStatus.NOT_IMPLEMENTED,
])
def test_normalize_non_executed_sets_error_and_empty_rows(status):
    pg = _pg(status, error="boom")
    out = normalize_postgres_execution_result(pg, _request())
    assert out.rows == ()
    assert out.row_count == 0
    assert out.execution_error == "boom"


def test_normalize_carries_truncation_and_warnings():
    pg = _pg(SQLPostgresAdapterStatus.EXECUTED, rows=((1, "a"),), row_count=1,
             columns=("id", "name"), truncated=True,
             warnings=("result truncated to max_rows",))
    out = normalize_postgres_execution_result(pg, _request())
    assert out.truncated is True
    assert "result truncated to max_rows" in out.warnings
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `cd backend && python -m pytest tests/evaluation/test_postgres_execution_adapter.py -v`
Expected: FAIL (`ModuleNotFoundError: postgres_execution_adapter`)

- [ ] **Step 3: Implement the module (function only)**

`backend/app/evaluation/postgres_execution_adapter.py`:

```python
"""Sprint 29.1 — PostgreSQL Read-Only Execution adapter.

Bridges the Sprint 25.8 local-Docker read-only adapter to the dialect-agnostic
execution contract (``multi_database_execution``). Local-Docker-only, read-only,
side-effect-free. Driver-isolated: importing this module loads NO database driver
or network client; ``psycopg2`` is imported lazily only inside the 25.8 adapter's
real execution path.
"""

import hashlib
from typing import Any, Dict, Mapping, Optional, Tuple

from app.evaluation.multi_database_execution import (
    SQL_MULTI_DATABASE_EXECUTION_VERSION,
    SQLDatabaseDialect,
    SQLDatabaseExecutionRequest,
    SQLDatabaseExecutionResult,
)
from app.evaluation.postgres_adapter import (
    SQLPostgresAdapterExecutionResult,
    SQLPostgresAdapterStatus,
)


def _rows_to_dicts(
    rows: Tuple[Tuple[Any, ...], ...],
    columns: Tuple[str, ...],
) -> Tuple[Tuple[Mapping[str, Any], ...], Tuple[str, ...]]:
    """Map positional rows to dict rows using ``columns``.

    Returns (dict_rows, extra_warnings). If any row's arity does not match
    ``columns``, falls back to positional ``col_<i>`` keys and emits a warning
    (never silently drops data).
    """
    dict_rows = []
    warnings: Tuple[str, ...] = ()
    mismatch = any(len(row) != len(columns) for row in rows)
    if mismatch or not columns:
        for row in rows:
            dict_rows.append({f"col_{i}": v for i, v in enumerate(row)})
        if rows and mismatch:
            warnings = ("column names unavailable or arity mismatch; used positional col_<i> keys",)
    else:
        for row in rows:
            dict_rows.append(dict(zip(columns, row)))
    return tuple(dict_rows), warnings


def normalize_postgres_execution_result(
    pg: SQLPostgresAdapterExecutionResult,
    request: SQLDatabaseExecutionRequest,
) -> SQLDatabaseExecutionResult:
    """Normalize a Sprint 25.8 adapter result into the shared execution contract."""
    sql_sha256 = hashlib.sha256(request.sql.encode("utf-8")).hexdigest()

    if pg.status == SQLPostgresAdapterStatus.EXECUTED:
        dict_rows, extra_warnings = _rows_to_dicts(pg.rows, pg.columns)
        warnings = tuple(pg.warnings) + extra_warnings
        return SQLDatabaseExecutionResult(
            version=SQL_MULTI_DATABASE_EXECUTION_VERSION,
            case_id=request.case_id,
            dialect=SQLDatabaseDialect.POSTGRESQL,
            sql=request.sql,
            sql_sha256=sql_sha256,
            rows=dict_rows,
            row_count=len(dict_rows),
            truncated=pg.truncated,
            execution_error=None,
            duration_ms=pg.duration_ms,
            warnings=warnings,
        )

    return SQLDatabaseExecutionResult(
        version=SQL_MULTI_DATABASE_EXECUTION_VERSION,
        case_id=request.case_id,
        dialect=SQLDatabaseDialect.POSTGRESQL,
        sql=request.sql,
        sql_sha256=sql_sha256,
        rows=(),
        row_count=0,
        truncated=False,
        execution_error=pg.error or f"PostgreSQL execution not completed ({pg.status.value}).",
        duration_ms=pg.duration_ms,
        warnings=tuple(pg.warnings),
    )
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `cd backend && python -m pytest tests/evaluation/test_postgres_execution_adapter.py -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add backend/app/evaluation/postgres_execution_adapter.py backend/tests/evaluation/test_postgres_execution_adapter.py
git commit -m "feat(29.1): pure normalizer from 25.8 result to shared execution contract"
```

---

### Task 3: `PostgresDatabaseExecutionAdapter` (validasyon + resolver + delege)

**Files:**
- Modify: `backend/app/evaluation/postgres_execution_adapter.py`
- Modify: `backend/app/evaluation/__init__.py`
- Test: `backend/tests/evaluation/test_postgres_execution_adapter.py`

**Interfaces:**
- Consumes: Task 2 `normalize_postgres_execution_result`; `resolve_local_docker_connection` (29.0); `SQLPostgresAdapterConfig`, `SQLPostgresAdapterContract`, `SQLPostgresAdapterExecutionRequest` (25.8); `SQLDatabaseExecutionAdapter`, `SQLExecutionAdapterCapability`, `SQLExecutionMode`, `SQLMultiDatabaseExecutionContractError`.
- Produces: `PostgresDatabaseExecutionAdapter(connection_resolver=resolve_local_docker_connection)` with `dialect` → `POSTGRESQL`, `capabilities()` → `(CONNECTION_REF, READ_ONLY)`, `execute(request) -> SQLDatabaseExecutionResult`.

- [ ] **Step 1: Write the failing tests**

`test_postgres_execution_adapter.py`'a ekle:

```python
from app.evaluation.multi_database_execution import (
    SQLExecutionAdapterCapability,
    SQLMultiDatabaseExecutionContractError,
    SQLDatabaseExecutionRouter,
)
from app.evaluation.postgres_execution_adapter import PostgresDatabaseExecutionAdapter


def _adapter(resolver):
    return PostgresDatabaseExecutionAdapter(connection_resolver=resolver)


def test_adapter_dialect_and_capabilities():
    a = _adapter(lambda env=None: None)
    assert a.dialect == SQLDatabaseDialect.POSTGRESQL
    assert a.capabilities() == (
        SQLExecutionAdapterCapability.CONNECTION_REF,
        SQLExecutionAdapterCapability.READ_ONLY,
    )


def test_adapter_requires_connection_ref_rejects_fixture():
    a = _adapter(lambda env=None: None)
    cfg = SQLDatabaseExecutionConfig(dialect=SQLDatabaseDialect.POSTGRESQL)
    req = SQLDatabaseExecutionRequest(
        case_id="c1", sql=SQL, dialect=SQLDatabaseDialect.POSTGRESQL,
        fixture_ref="somefix", connection_ref=None, config=cfg,
    )
    with pytest.raises(SQLMultiDatabaseExecutionContractError):
        a.execute(req)


def test_adapter_rejects_explain_only_mode():
    a = _adapter(lambda env=None: None)
    cfg = SQLDatabaseExecutionConfig(
        dialect=SQLDatabaseDialect.POSTGRESQL,
        execution_mode=SQLExecutionMode.EXPLAIN_ONLY,
    )
    req = SQLDatabaseExecutionRequest(
        case_id="c1", sql=SQL, dialect=SQLDatabaseDialect.POSTGRESQL,
        fixture_ref=None, connection_ref="local_docker", config=cfg,
    )
    with pytest.raises(SQLMultiDatabaseExecutionContractError):
        a.execute(req)


def test_adapter_no_connection_returns_graceful_error():
    a = _adapter(lambda env=None: None)  # resolver yields no connection
    out = a.execute(_request())
    assert out.rows == ()
    assert out.row_count == 0
    assert out.execution_error is not None
    assert out.dialect == SQLDatabaseDialect.POSTGRESQL


def test_adapter_pluggable_into_router():
    a = _adapter(lambda env=None: None)
    router = SQLDatabaseExecutionRouter(adapters=(a,))
    assert router.get_adapter(SQLDatabaseDialect.POSTGRESQL) is a
```

(Not: `_request` Task 2'de tanımlı; aynı dosyada.)

- [ ] **Step 2: Run tests to verify they fail**

Run: `cd backend && python -m pytest tests/evaluation/test_postgres_execution_adapter.py -k adapter -v`
Expected: FAIL (`ImportError: PostgresDatabaseExecutionAdapter`)

- [ ] **Step 3: Implement the adapter class**

`postgres_execution_adapter.py` import bloğunu genişlet:

```python
from app.evaluation.multi_database_execution import (
    SQL_MULTI_DATABASE_EXECUTION_VERSION,
    SQLDatabaseDialect,
    SQLDatabaseExecutionAdapter,
    SQLDatabaseExecutionRequest,
    SQLDatabaseExecutionResult,
    SQLExecutionAdapterCapability,
    SQLExecutionMode,
    SQLMultiDatabaseExecutionContractError,
)
from app.evaluation.postgres_adapter import (
    SQLPostgresAdapterConfig,
    SQLPostgresAdapterContract,
    SQLPostgresAdapterExecutionRequest,
    SQLPostgresAdapterExecutionResult,
    SQLPostgresAdapterStatus,
)
from app.evaluation.postgres_connection_resolver import (
    resolve_local_docker_connection,
)
```

Modül sonuna sınıfı ekle:

```python
class PostgresDatabaseExecutionAdapter(SQLDatabaseExecutionAdapter):
    """Local-Docker-only, read-only PostgreSQL execution adapter (Sprint 29.1).

    Resolves a local-Docker connection via the Sprint 29.0 resolver and delegates
    the real ``SELECT`` to the Sprint 25.8 adapter, then normalizes the result to
    the shared ``SQLDatabaseExecutionResult`` contract. No connection resolved
    (no local Docker) -> graceful empty result with ``execution_error`` (never
    raises on that path), matching the inert-by-default posture of 25.8/29.0.
    """

    def __init__(self, connection_resolver=resolve_local_docker_connection):
        self._resolve = connection_resolver

    @property
    def dialect(self) -> SQLDatabaseDialect:
        return SQLDatabaseDialect.POSTGRESQL

    def capabilities(self):
        return (
            SQLExecutionAdapterCapability.CONNECTION_REF,
            SQLExecutionAdapterCapability.READ_ONLY,
        )

    def execute(self, request: SQLDatabaseExecutionRequest) -> SQLDatabaseExecutionResult:
        if not isinstance(request, SQLDatabaseExecutionRequest):
            raise SQLMultiDatabaseExecutionContractError(
                "request must be a SQLDatabaseExecutionRequest"
            )
        if request.dialect != SQLDatabaseDialect.POSTGRESQL:
            raise SQLMultiDatabaseExecutionContractError(
                "PostgreSQL adapter only supports POSTGRESQL dialect requests"
            )
        if request.connection_ref is None:
            raise SQLMultiDatabaseExecutionContractError(
                "PostgreSQL adapter requires connection_ref"
            )
        if request.fixture_ref is not None:
            raise SQLMultiDatabaseExecutionContractError(
                "PostgreSQL adapter does not support fixture_ref"
            )
        if request.config.execution_mode != SQLExecutionMode.READ_ONLY:
            raise SQLMultiDatabaseExecutionContractError(
                "PostgreSQL adapter currently supports only read_only execution mode"
            )

        connection = self._resolve()
        if connection is None:
            return SQLDatabaseExecutionResult(
                version=SQL_MULTI_DATABASE_EXECUTION_VERSION,
                case_id=request.case_id,
                dialect=SQLDatabaseDialect.POSTGRESQL,
                sql=request.sql,
                sql_sha256=hashlib.sha256(request.sql.encode("utf-8")).hexdigest(),
                rows=(),
                row_count=0,
                truncated=False,
                execution_error="No local Docker PostgreSQL connection available",
                duration_ms=0.0,
                warnings=(),
            )

        pg_config = SQLPostgresAdapterConfig(
            timeout_seconds=request.config.timeout_seconds,
            max_rows=request.config.max_rows,
            execution_mode="read_only",
        )
        pg_request = SQLPostgresAdapterExecutionRequest(
            case_id=request.case_id,
            sql=request.sql,
            dialect="postgresql",
            config=pg_config,
            connection_ref=request.connection_ref,
        )
        pg_result = SQLPostgresAdapterContract(connection=connection).execute(pg_request)
        return normalize_postgres_execution_result(pg_result, request)
```

`backend/app/evaluation/__init__.py`'a export ekle (mevcut `resolve_local_docker_connection` export'unun yanına, hem import hem `__all__`):

```python
from app.evaluation.postgres_execution_adapter import (
    PostgresDatabaseExecutionAdapter,
    normalize_postgres_execution_result,
)
```
ve `__all__` listesine `"PostgresDatabaseExecutionAdapter"`, `"normalize_postgres_execution_result"`.

- [ ] **Step 4: Run tests to verify they pass**

Run: `cd backend && python -m pytest tests/evaluation/test_postgres_execution_adapter.py -v`
Expected: PASS (tümü)

- [ ] **Step 5: Commit**

```bash
git add backend/app/evaluation/postgres_execution_adapter.py backend/app/evaluation/__init__.py backend/tests/evaluation/test_postgres_execution_adapter.py
git commit -m "feat(29.1): PostgresDatabaseExecutionAdapter over shared execution contract"
```

---

### Task 4: Import-time driver-isolation testi (subprocess)

**Files:**
- Test: `backend/tests/evaluation/test_postgres_execution_adapter.py`

**Interfaces:**
- Consumes: Task 3 modülü. 25.8'in `test_postgres_adapter.py` içindeki subprocess-isolation testinin desenini aynalar.

- [ ] **Step 1: Write the failing test**

```python
import subprocess
import sys
import textwrap


def test_import_does_not_load_db_driver_or_socket_client():
    script = textwrap.dedent(
        """
        import sys
        import app.evaluation.postgres_execution_adapter  # noqa: F401
        forbidden = [m for m in ("psycopg2", "psycopg") if m in sys.modules]
        if forbidden:
            print("FORBIDDEN:" + ",".join(forbidden))
        else:
            print("CLEAN")
        """
    )
    res = subprocess.run(
        [sys.executable, "-c", script],
        capture_output=True, text=True, cwd="backend",
    )
    assert "FORBIDDEN" not in res.stdout, (
        f"Importing postgres_execution_adapter loaded forbidden driver: {res.stdout}"
    )
    assert "CLEAN" in res.stdout, res.stdout + res.stderr
```

> Not: `cwd="backend"` — mevcut 25.8 isolation testleri de backend kökünden koşuyor. Test bu dosyada `backend/tests/...` altında; `cwd` ayarı `app.*` import'unun çözülmesi için gerekli (25.8 testi de böyle yapıyor — çalıştırmadan önce oradaki `cwd`/`env` kalıbını birebir teyit et ve gerekiyorsa aynen kopyala).

- [ ] **Step 2: Run test to verify it passes immediately (isolation zaten sağlanmış olmalı)**

Run: `cd backend && python -m pytest tests/evaluation/test_postgres_execution_adapter.py -k isolation -v`
Expected: PASS. FAIL olursa modül import zincirinde bir yerde eager driver import'u var demektir — kök nedeni düzelt (lazy import'a taşı), testi zayıflatma.

- [ ] **Step 3: Commit**

```bash
git add backend/tests/evaluation/test_postgres_execution_adapter.py
git commit -m "test(29.1): lock import-time driver isolation for execution adapter"
```

---

### Task 5: Gated integration testleri (Docker, safe-skip)

**Files:**
- Create: `backend/tests/evaluation/test_postgres_execution_adapter_integration.py`

**Interfaces:**
- Consumes: Task 3 `PostgresDatabaseExecutionAdapter`; 29.0 seed şeması (`customers`/`orders`/`order_items`); `resolve_local_docker_connection`. `test_postgres_schema_adapter_integration.py`'daki `_local_docker_postgres_available()` + `pytest.skip` desenini aynalar.

- [ ] **Step 1: Write the integration tests**

```python
"""Sprint 29.1 — read-only execution integration tests (seeded schema).

Skipped safely when no local Docker PostgreSQL is reachable; runs in CI and
locally via `docker compose up -d postgres`.
"""
import pytest

from app.evaluation.postgres_connection_resolver import resolve_local_docker_connection
from app.evaluation.multi_database_execution import (
    SQLDatabaseDialect,
    SQLExecutionMode,
    SQLDatabaseExecutionConfig,
    SQLDatabaseExecutionRequest,
)
from app.evaluation.postgres_execution_adapter import PostgresDatabaseExecutionAdapter

pytestmark = [pytest.mark.integration, pytest.mark.postgres]


def _local_docker_postgres_available():
    try:
        import psycopg2
    except ImportError:
        return False
    conn = resolve_local_docker_connection()
    if conn is None:
        return False
    try:
        db = psycopg2.connect(
            host=conn.host, port=conn.port, dbname=conn.dbname,
            user=conn.user, password=conn.password, connect_timeout=2,
        )
        db.close()
        return True
    except Exception:
        return False


@pytest.fixture(scope="module")
def adapter():
    if not _local_docker_postgres_available():
        pytest.skip("local Docker PostgreSQL not available")
    return PostgresDatabaseExecutionAdapter()


def _request(sql, timeout=2.0, max_rows=1000):
    cfg = SQLDatabaseExecutionConfig(
        dialect=SQLDatabaseDialect.POSTGRESQL,
        timeout_seconds=timeout, max_rows=max_rows,
        execution_mode=SQLExecutionMode.READ_ONLY,
    )
    return SQLDatabaseExecutionRequest(
        case_id="c1", sql=sql, dialect=SQLDatabaseDialect.POSTGRESQL,
        fixture_ref=None, connection_ref="local_docker", config=cfg,
    )


def test_select_returns_named_dict_rows(adapter):
    out = adapter.execute(_request("SELECT id, name FROM customers ORDER BY id"))
    assert out.execution_error is None
    assert out.row_count >= 1
    first = out.rows[0]
    assert set(first.keys()) == {"id", "name"}


def test_multi_table_join(adapter):
    sql = (
        "SELECT c.name, o.id AS order_id "
        "FROM customers c JOIN orders o ON o.customer_id = c.id "
        "ORDER BY o.id"
    )
    out = adapter.execute(_request(sql))
    assert out.execution_error is None
    assert out.row_count >= 1
    assert set(out.rows[0].keys()) == {"name", "order_id"}


def test_max_rows_truncation(adapter):
    out = adapter.execute(_request("SELECT id FROM customers ORDER BY id", max_rows=1))
    assert out.truncated is True
    assert out.row_count == 1
    assert any("truncat" in w.lower() for w in out.warnings)


def test_timeout_yields_execution_error(adapter):
    out = adapter.execute(_request("SELECT pg_sleep(2)", timeout=0.05))
    assert out.execution_error is not None
    assert out.rows == ()
```

> Not: Seed'de `customers`'ta en az 2 satır olduğunu 29.0 `seed.sql`'inden teyit et; `test_max_rows_truncation` `max_rows=1` ile `truncated=True` bekliyor (yani ≥2 satır gerek). Değilse `orders`/`order_items` gibi ≥2 satırlı bir tabloya çevir.

- [ ] **Step 2: Run locally with Docker**

Run:
```bash
docker compose up -d postgres
# seed CI'da otomatik; lokal için gerekiyorsa:
# psql -h localhost -U sqlgen -d sqlgen_test -f backend/tests/fixtures/postgres/seed.sql
cd backend && python -m pytest tests/evaluation/test_postgres_execution_adapter_integration.py -v
```
Expected: PASS (Docker varsa) / SKIPPED (Docker yoksa). Docker yoksa CI paritesi için en azından collection'ın hatasız olduğunu ve testlerin SKIPPED alındığını doğrula.

- [ ] **Step 3: Commit**

```bash
git add backend/tests/evaluation/test_postgres_execution_adapter_integration.py
git commit -m "test(29.1): gated integration tests for real read-only execution"
```

---

### Task 6: Yaşayan doküman senkronu

**Files:**
- Modify: `CHANGELOG.md`
- Modify: `docs/PLANNED-SPRINTS.md` (29.1 satırı ✅ + özet)
- Modify: `docs/SPRINT-PR-LOG.md`
- Modify: `docs/TECH-DEBT.md` (§18 scope-out'ları 29.1'e göre güncelle)

**Interfaces:** Yalnız dokümantasyon; kod davranışı değişmez.

- [ ] **Step 1: CHANGELOG'a 29.1 girişi ekle**

En üste (mevcut biçime uyarak) 29.1 özeti: yeni `PostgresDatabaseExecutionAdapter` (shared execution contract'a bağlı, local-docker-only read-only, driver-izole, yan-etkisiz), 25.8'e geriye-uyumlu `columns` alanı, saf normalize fonksiyonu, gated integration testleri. **Bilinen sınır:** canlı HTTP pipeline wiring yok (30.2/30.4).

- [ ] **Step 2: PLANNED-SPRINTS.md 29.1 satırını güncelle**

`| 29.1 | PostgreSQL Read-Only Execution ... |` satırını `✅ Tamam · *...özet...*` yap (29.0 satırının biçimini aynala). PR no başta `TBD` bırak; merge sonrası `#N` follow-up commit ile güncellenecek.

- [ ] **Step 3: SPRINT-PR-LOG.md'ye satır ekle**

Mevcut tablo biçimine uyan bir 29.1 satırı (PR no `TBD`).

- [ ] **Step 4: TECH-DEBT §18 güncelle**

29.0'ın §18 scope-out'larından "gerçek execution wiring (→ 29.1)" artık kapandı — işaretle; kalan scope-out'ları (canlı HTTP pipeline wiring → 30.2/30.4, 26.3/26.4 gate wiring, EXPLAIN-only → 29.2) net bırak. Sessiz düşürme yok.

- [ ] **Step 5: Commit**

```bash
git add CHANGELOG.md docs/PLANNED-SPRINTS.md docs/SPRINT-PR-LOG.md docs/TECH-DEBT.md
git commit -m "docs(29.1): living-doc sync (changelog, planned-sprints, pr-log, tech-debt)"
```

---

## Self-Review Notları

- **Spec coverage:** Ana bileşen (Task 2+3), 25.8 `columns` (Task 1), driver-isolation (Task 4), gated integration — real SELECT/JOIN/truncation/timeout (Task 5), read-only/graceful-None (Task 3), doküman kapsam-dışı işaretleme (Task 6). Router pluggability (Task 3, `test_adapter_pluggable_into_router`). Tümü karşılanıyor.
- **Type consistency:** `normalize_postgres_execution_result` imzası Task 2↔3↔testlerde aynı; `PostgresDatabaseExecutionAdapter(connection_resolver=...)`, `.dialect`, `.capabilities()`, `.execute()` tutarlı; enum eşdeğerleri Global Constraints'te sabit.
- **Full-suite gate:** Son adımda (Task 6 sonrası, PR öncesi) tüm suite koşulacak — fastapi pin drift kontrolü ([[sqlgen-fastapi-pin-drift]]) full-suite öncesi yapılır.
