# Sprint 31.5 — Error / Warning UX Implementation Plan

> Frontend (Vue 3). Verify: `cd frontend && npm run build` → 0 errors, exit 0.
> Spec: `docs/superpowers/specs/2026-09-10-sprint-31.5-error-warning-ux-design.md`

**Goal:** Surface the backend error taxonomy (`Job.error_code`, already carried) as a user-readable
category + label + hint, with tone split (EXECUTION = warning/amber, others = error/red). No backend change.

## Global Constraints
- New pure util `frontend/src/utils/errorInfo.ts` + edit `ChatView.vue` (+ docs).
- Mirror backend `_CATEGORY_BY_CODE` exactly; unknown/null code → graceful fallback. No new deps.
- Verify = build exit 0. Commit messages: plain, NO trailer / co-author / "Generated with".

---

### Task 1: `errorInfo.ts` pure util

- [ ] **Step 1:** `ErrorCategory` union + `CODE_TO_CATEGORY` (mirror backend registry), `CODE_LABEL` (Turkish label + optional hint per code), `CATEGORY_LABEL` (label + tone: EXECUTION→'warning', else 'error').
- [ ] **Step 2:** `describeError(code?, fallbackMessage?)` → `{ code, category, categoryLabel, label, hint?, tone }`; unknown/null → fallback (tone 'error', label = fallbackMessage ?? 'Hata').

### Task 2: ChatView integration

- [ ] **Step 1:** Add `errorCode?: string` to `ChatMessage`; in `checkJobCompletion` failed branch set `activeMsg.errorCode = job.error_code ?? undefined`.
- [ ] **Step 2:** Enrich the error box via `describeError(msg.errorCode, msg.error)`: tone-based color (error=red, warning=amber), category badge + label heading, raw `msg.error` below, optional hint line. `role="alert"`. Fallback ≈ current plain view when no code.

- [ ] **Step 3: Verify** — `cd frontend && npm run build` → 0 errors, exit 0; node-test `errorInfo` on a few codes.
- [ ] **Step 4: Commit** — `git commit -m "feat(31.5): surface backend error taxonomy in the error UX (category/label/hint + tone split)"`

### Task 3: Living docs — roadmap/planned-sprints/changelog/pr-log `#N` backfill.

## Self-Review
Spec §2 → Tasks 1-2; §3 verify → Task 2 Step 3; §4 out-of-scope honored. Pure util mirrors backend taxonomy; graceful fallback. ✓

## Controller finishing flow
Self-review → push/PR → backend CI (frontend-only → green) → `#N` backfill → merge `--rebase --delete-branch` → main pull → memory: archive 31.5, flip `durum.md` to 31.6 Query History UX, update `MEMORY.md`. NO commit trailer.
