# Sprint 31.2 — Relationship Graph UX Implementation Plan

> Frontend (Vue 3 + D3). Verify: `cd frontend && npm run build` → 0 errors, exit 0.
> Spec: `docs/superpowers/specs/2026-09-10-sprint-31.2-relationship-graph-ux-design.md`

**Goal:** Bidirectional selection link between the table list and the relationship graph
(click a table card → frame + ring its graph node), plus a persistent selected-node ring — both
driven by a single `watch(expandedTable)`. No force/layout changes.

## Global Constraints
- Touch only `frontend/src/components/SchemaManager.vue` (+ living docs).
- Reuse the existing hover palette (indigo `#818cf8` + `url(#glow)`); no new deps.
- Verify = build exit 0, no new type errors. Commit messages: plain, NO trailer / co-author / "Generated with".

---

### Task 1: Bidirectional selection + persistent ring

**File:** `frontend/src/components/SchemaManager.vue`

- [ ] **Step 1:** Add module-scope `let repaintGraphSelection: (() => void) | null = null;` near `zoomBehavior` declaration.
- [ ] **Step 2:** Add `zoomToGraphNode(tableName: string)` — look up node in `simulation.nodes()` by id; guard `svgRef`/`zoomBehavior`/`simulation`; if node missing → no-op; else apply the center+scale(1.2)/750ms transform (same as current `focusNode` zoom).
- [ ] **Step 3:** Slim `focusNode(node)` → set `expandedTable.value = node.id` + `scrollIntoView` only (drop its inline zoom; watcher handles it).
- [ ] **Step 4:** Inside `initGraph`, after node circles/texts are created, define `paintSelectedRing()` that sets each `nodeCircles` stroke/stroke-width/filter from `expandedTable.value`; assign `repaintGraphSelection = paintSelectedRing` and call it once.
- [ ] **Step 5:** Append `paintSelectedRing();` to the end of the `mouseout` handler (restore ring after hover leaves).
- [ ] **Step 6:** Add `watch(expandedTable, (id) => { repaintGraphSelection?.(); if (id) zoomToGraphNode(id); })`.
- [ ] **Step 7: Verify** — `cd frontend && npm run build` → 0 errors, exit 0. Fix any new type error minimally (no `any` widening on typed refs).
- [ ] **Step 8: Commit** — `git commit -m "feat(31.2): bidirectional table<->graph selection link + persistent selected-node ring"`

---

### Task 2: Living docs
- [ ] Roadmap / planned-sprints / changelog / pr-log `#N` backfill (after PR number known); TECH-DEBT §35 unchanged (still open: test infra/CI/toast).

## Self-Review
Spec §2 → Task 1 Steps 1-6; §3 verify → Step 7; §4 out-of-scope honored (node ring only, no edge choreography, no layout change). Single file, reuses hover palette, no deps. ✓

## Controller finishing flow
Whole-branch self-review → push/PR (`feat/sprint-31.2-relationship-graph-ux`) → backend CI (frontend-only → trivially green) → `#N` backfill → merge `--rebase --delete-branch` → main pull → memory: archive 31.2, flip `durum.md` to 31.3 SQL Explanation Panel, update `MEMORY.md`. NO commit trailer.
