# Sprint 31.1 — Schema Explorer UX Implementation Plan

> Frontend (Vue 3). Verify: `cd frontend && npm run build` → 0 errors, exit 0.
> Spec: `docs/superpowers/specs/2026-09-09-sprint-31.1-schema-explorer-ux-design.md`

**Goal:** Type the Schema Explorer's schema so the whole frontend build goes green (fixes all of
TECH-DEBT §35's 6 errors), and revive the dead `validCustomRelations` as a real stale-relation guard.

## Global Constraints
- Touch only `frontend/src/components/SchemaManager.vue` (+ living docs / TECH-DEBT §35).
- No runtime behavior change beyond the stale-custom-relation guard; no visual redesign.
- Verify = build EXIT 0 (strict improvement over 31.0's error-set delta). Commit messages: plain (trailer per session rule).

---

### Task 1: Type the schema + revive validCustomRelations

**File:** `frontend/src/components/SchemaManager.vue`

- [ ] **Step 1:** Import `GraphEdgeInput` as a type from `../utils/graphSelection` (extend the existing import).
- [ ] **Step 2:** Add `SchemaColumn` / `SchemaForeignKey` / `SchemaTable` / `DbSchema` interfaces near the top of `<script setup>` (after the existing `NodeItem`/`LinkItem` interfaces).
- [ ] **Step 3:** Change `const schema = ref<any>(null)` → `const schema = ref<DbSchema | null>(null)`.
- [ ] **Step 4:** Move `validCustomRelations` computed ABOVE `allRelations`; in `allRelations` build the custom-key set from `validCustomRelations.value` instead of `customRelations.value`.
- [ ] **Step 5:** Simplify `incomingRelationsMap`'s `(meta as any).foreign_keys` → `meta.foreign_keys ?? []` (now typed).
- [ ] **Step 6: Verify** — `cd frontend && npm run build` → expect 0 errors, exit 0. If any NEW error surfaces (a `schema.value.*` access not covered by `DbSchema`), widen the interface minimally to match the runtime shape; do not re-introduce `any` on `schema`.

- [ ] **Step 7: Commit** — `git commit -m "feat(31.1): type the schema explorer (green frontend build) + revive validCustomRelations guard"`

---

### Task 2: TECH-DEBT §35 update + living docs

- [ ] **Step 1:** Update TECH-DEBT §35 — SchemaManager build errors RESOLVED (build now green); note remaining open items (frontend test infra, frontend CI, toast) still deferred but now unblocked.
- [ ] **Step 2:** Commit `docs(31.1)`.

## Self-Review
Spec §2 → Task 1 Steps 1-5; §3 verify → Step 6; §4 out-of-scope → Task 2. Single file, typed guard, no visual change. ✓

## Controller finishing flow
Whole-branch self-review → push/PR (`feat/sprint-31.1-schema-explorer-ux`) → backend CI (frontend-only → trivially green) → `#N` backfill (ROADMAP/CHANGELOG/PLANNED-SPRINTS/PR-LOG) → merge `--rebase --delete-branch` → main pull → memory: archive 31.1, flip `durum.md` to 31.2 Relationship Graph UX, update `MEMORY.md`.
