# Sprint 31.7 — Feedback UX Implementation Plan

> Frontend (Vue 3). Verify: `cd frontend && npm run build` → 0 errors, exit 0.
> Spec: `docs/superpowers/specs/2026-09-10-sprint-31.7-feedback-ux-design.md`

**Goal:** 👍/👎 feedback on completed SQL in ChatView, POSTing to the legacy 27.3 job-scoped endpoint,
with a 👎 category+note mini-form honoring backend invariants. Add the missing `submitFeedback` to api.ts.

## Global Constraints
- Edit `frontend/src/services/api.ts` + `frontend/src/components/ChatView.vue` (+ docs). No new deps.
- Client-side invariants: correct → no category/note; category=other → note required.
- Verify = build exit 0. Commit: plain, NO trailer / co-author / "Generated with".

---

### Task 1: api.ts feedback types + method

- [ ] **Step 1:** `FeedbackVerdict`, `FeedbackCategory` (7 values mirroring backend), `FeedbackPayload { verdict; category?; note?; corrected_sql? }`.
- [ ] **Step 2:** `submitFeedback(jobId, payload)` → `POST /api/jobs/{jobId}/feedback` (fetch + getHeaders + JSON body); throw on !ok.

### Task 2: ChatView feedback UI

- [ ] **Step 1:** `feedbackUi = ref<Record<number, {...}>>({})`; helpers `giveThumbsUp(msg)`, `openThumbsDown(msg)`, `submitThumbsDown(msg)`.
- [ ] **Step 2:** Under the SQL card, when `msg.status==='completed' && msg.sql && msg.jobId`: 👍/👎 buttons; 👎 opens category `<select>` (Turkish labels) + note `<textarea>` + Gönder; validate other→note; sending/submitted/error states with `aria-live`.

- [ ] **Step 3: Verify** — `cd frontend && npm run build` → 0 errors, exit 0.
- [ ] **Step 4: Commit** — `git commit -m "feat(31.7): SQL feedback UX in the composer (thumbs up/down + category/note) wired to the 27.3 feedback endpoint"`

### Task 3: Living docs — roadmap/planned-sprints/changelog/pr-log `#N` backfill.

## Self-Review
Spec §2 → Tasks 1-2; §3 verify → Task 2 Step 3; §4 out-of-scope (legacy endpoint, no corrected_sql, no history). Invariants enforced client-side. ✓

## Controller finishing flow
Self-review → push/PR → backend CI (frontend-only → green) → `#N` backfill → merge `--rebase --delete-branch` → main pull → memory: archive 31.7, flip `durum.md` to 31.8 (Phase 12 son), update `MEMORY.md`. NO commit trailer.
