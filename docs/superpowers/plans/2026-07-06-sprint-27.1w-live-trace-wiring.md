# Sprint 27.1w — Live Trace Wiring Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Saf `EndToEndTrace` sözleşmesini canlı pipeline'a örmek: request_id plumbing, per-stage perf_counter, dual-emit trace kaydı, orta doz stage refactor'u + 27.1'den devreden 3 Minor.

**Architecture:** `run_pipeline` üç stage metoduna bölünür (davranış birebir korunur); yeni saf modül `app/trace/live_trace_assembly.py` legacy pipeline verilerinden 7 span'lik `EndToEndTrace` kurar; `_capture_trace_on_exit` legacy `NL2SQLTrace`'in yanına `TraceRecord(trace_type="end_to_end")` emit eder.

**Tech Stack:** Python 3.11+, FastAPI BackgroundTasks, dataclasses, pytest. Spec: `docs/superpowers/specs/2026-07-06-sprint-27.1w-live-trace-wiring-design.md`.

## Global Constraints

- Çalışma branch'i: `feat/live-trace-wiring` (main'den; Task 1 Step 0 açar). PUSH YOK — kullanıcı onayı olmadan push/PR yapılmaz.
- Commit mesajlarına **Claude co-author trailer'ı EKLENMEZ** (kullanıcı talimatı).
- `app/trace/live_trace_assembly.py` SAF kalır: `app/trace/*` import yüzeyi stdlib + `app.trace.*` + `app.errors.*`
  (Sprint 27.2 ile genişletildi: `app.errors` yaprak katmandır, stdlib dışına import etmez ve driver taşımaz —
  invariant'ın amacı olan driver izolasyonu korunur. Guard testleri: `tests/errors/test_errors_package_purity.py` ve
  `tests/trace/test_live_trace_assembly.py`). `Math.random`/`datetime.now` yok — trace_id üretimi caller'dan
  (uuid `sql_pipeline` tarafında).
- Mevcut pipeline testleri (`tests/test_sql_pipeline_llm_provider_integration.py`, `test_sql_pipeline_sandbox.py`, `test_pipeline_error_envelope_contract.py`) **DEĞİŞTİRİLMEDEN** geçmelidir — refactor davranış korumasının kanıtı. (Yeni assert eklemek serbest, mevcut assert değiştirmek yasak.)
- Trace emisyonu pipeline sonucunu ASLA etkilemez: e2e kurulum/kayıt hataları `logger.error` ile yutulur.
- Testler: `cd backend && venv\Scripts\python.exe -m pytest <dosya> -q`. Full suite taban çizgisi: **2024 passed / 9 skipped**.
- SQL asla ham taşınmaz e2e payload'a: yalnız sha256. Serbest metinler `redact_sensitive`/`redact_sensitive_text`'ten geçer.

---

### Task 1: 27.1'den devreden 3 Minor builder düzeltmesi

**Files:**
- Modify: `backend/app/trace/end_to_end_trace_builders.py:190` (`_GENERATION_ERROR_FINISH`), `:222-224`, `:149-155` (retrieval candidates), `:84`, `:250` (reason_code)
- Test: `backend/tests/trace/test_end_to_end_trace_builders.py` (mevcut dosyaya ek)

**Interfaces:**
- Consumes: mevcut builder'lar (27.0/27.1).
- Produces: `build_generation_span` — output var + finish_reason yok → **OK** (Task 2/6 buna dayanır); `build_retrieval_span` — score/rank eksik candidate → `score=0.0`, `rank=<sıra indeksi>`; `reason_code` her zaman `str | None`.

- [ ] **Step 0: Branch aç**

```bash
cd C:\Users\furkan\Desktop\SQLGen
git checkout main
git checkout -b feat/live-trace-wiring
```

- [ ] **Step 1: Failing testleri yaz** — `backend/tests/trace/test_end_to_end_trace_builders.py` dosyasının SONUNA ekle:

```python
# ---- Sprint 27.1w: carried Minor fixes ----
from types import SimpleNamespace


def test_generation_span_ok_when_output_present_but_finish_reason_missing():
    result = SimpleNamespace(
        response=SimpleNamespace(provider_id="p", model_id="m", finish_reason=None,
                                 latency_ms=12, token_usage=None),
        output_sha256="abc123", prompt_sha256="def456")
    span = build_generation_span(result)
    assert span.status is TraceSpanStatus.OK


def test_generation_span_error_when_output_missing():
    result = SimpleNamespace(
        response=SimpleNamespace(provider_id="p", model_id="m", finish_reason=None,
                                 latency_ms=None, token_usage=None),
        output_sha256=None, prompt_sha256=None)
    span = build_generation_span(result)
    assert span.status is TraceSpanStatus.ERROR


def test_generation_span_error_when_finish_reason_is_error():
    result = SimpleNamespace(
        response=SimpleNamespace(provider_id="p", model_id="m", finish_reason="error",
                                 latency_ms=None, token_usage=None),
        output_sha256="abc123", prompt_sha256=None)
    span = build_generation_span(result)
    assert span.status is TraceSpanStatus.ERROR


def test_retrieval_candidate_missing_score_rank_gets_typed_fallbacks():
    result = SimpleNamespace(
        k_requested=2, k_returned=2, retrieval_version="v1",
        candidates=[SimpleNamespace(object_id="t1", object_type="table"),
                    SimpleNamespace(object_id="t2", object_type="table")])
    span = build_retrieval_span(result)
    c0, c1 = span.detail.candidates
    assert c0.score == 0.0 and isinstance(c0.score, float)
    assert c0.rank == 0 and isinstance(c0.rank, int)
    assert c1.rank == 1


def test_security_and_validation_reason_code_normalized_from_enum():
    import enum

    class _RC(enum.Enum):
        POLICY_X = "policy_x"

    event = SimpleNamespace(category="policy", outcome="denied", severity="high",
                            reason_code=_RC.POLICY_X, entry_hash=None)
    sec = build_security_span([event])
    assert sec.detail.checks[0].reason_code == "policy_x"

    issue = SimpleNamespace(category="semantic", stage="semantic_validation",
                            severity="error", reason_code=_RC.POLICY_X)
    val = build_validation_span([issue], valid=False)
    assert val.detail.issues[0].reason_code == "policy_x"
```

(Dosyanın başındaki mevcut import bloğu `build_generation_span`, `build_retrieval_span`, `build_security_span`, `build_validation_span`, `TraceSpanStatus`'u zaten içermiyorsa import satırlarına ekle.)

- [ ] **Step 2: Testleri koş, yenilerin FAIL ettiğini gör**

Run: `cd backend && venv\Scripts\python.exe -m pytest tests/trace/test_end_to_end_trace_builders.py -q`
Expected: yeni 5 testten en az 3'ü FAIL (`ok_when_output_present`, `missing_score_rank`, `reason_code_normalized`); mevcutlar PASS.

- [ ] **Step 3: Düzeltmeleri uygula** — `end_to_end_trace_builders.py`:

(a) Satır 190: `""` set'ten çıkar:
```python
_GENERATION_ERROR_FINISH = {"error", "content_filter", "null", "none"}
```
(Satır 222-224'teki mantık aynen kalır — `fr` boşsa artık set'e takılmaz, karar `not output_sha256`'ya düşer.)

(b) Satır 148-157 candidate kurulumunu şöyle değiştir (enumerate + tipli fallback):
```python
    candidates = tuple(
        RetrievalCandidateRef(
            object_id=_status_str(getattr(c, "object_id", None)),
            object_type=_status_str(getattr(c, "object_type", None)),
            score=float(getattr(c, "score", None) if getattr(c, "score", None) is not None else 0.0),
            rank=int(getattr(c, "rank", None) if getattr(c, "rank", None) is not None else idx),
            schema_hash=getattr(c, "schema_hash", None),
        )
        for idx, c in enumerate(getattr(result, "candidates", ()) or ())
    )
```

(c) Satır 84: `reason_code=_status_str(getattr(e, "reason_code", None)),`
Satır 250: `reason_code=_status_str(getattr(e, "reason_code", None) or getattr(e, "type", None)),`
Not: `_status_str(None)` `None` döndürür (mevcut davranış) — `str | None` sözleşmesi korunur.

- [ ] **Step 4: Testlerin PASS ettiğini doğrula**

Run: `cd backend && venv\Scripts\python.exe -m pytest tests/trace/ -q`
Expected: hepsi PASS (mevcut 27.0/27.1 testleri dahil — davranış değişikliği yalnız düzeltilen 3 noktada).

- [ ] **Step 5: Commit**

```bash
git add backend/app/trace/end_to_end_trace_builders.py backend/tests/trace/test_end_to_end_trace_builders.py
git commit -m "fix(trace): close 27.1 carried minors in span builders"
```

---

### Task 2: Saf canlı-trace montaj modülü (`live_trace_assembly.py`)

**Files:**
- Create: `backend/app/trace/live_trace_assembly.py`
- Test: `backend/tests/trace/test_live_trace_assembly.py`

**Interfaces:**
- Consumes: `app.trace.end_to_end_trace` (TraceSpan, TraceStageKind, TraceSpanStatus, build_end_to_end_trace, EndToEndTrace), `app.trace.end_to_end_trace_builders` (5 builder), Task 1 düzeltmeleri.
- Produces (Task 6 bunu çağırır):
  ```python
  def assemble_live_end_to_end_trace(
      *, trace_id: str, request_id: str, job_id: str, dialect: str,
      natural_query_redacted: str, total_duration_ms: int,
      stage_timings: Mapping[str, int] | None,   # anahtarlar: intent/retrieval/prompt/generation/validation/security
      error_type: str | None,                     # legacy error_type (excel_parse_error, input_error, schema_pruning_error, schema_pruning_exception, schema_context_selection_exception, sql_generation_failed, None)
      success: bool,
      schema_selection_trace: Mapping | None, pruned_tables: Sequence[str],
      prompt_sha256: str | None, prompt_char_count: int | None,
      attempts_redacted: Sequence[Mapping], last_generated_sql_redacted: str | None,
  ) -> EndToEndTrace
  ```
  Ayrıca yardımcı: `def sha256_text(text: str | None) -> str | None` (boş/None → None).

**Span kuralları (spec §1.4 + terminal tutarlılığı — modül docstring'ine de yazılır):**

| Stage | Kaynak | Statü kuralı |
|---|---|---|
| INTENT | canlı yolda intent extraction YOK | `error_type ∈ {excel_parse_error, input_error}` → doğrudan `TraceSpan(INTENT, ERROR, attributes={"reason_code": error_type})`; aksi halde `build_intent_span(None)` → SKIPPED |
| RETRIEVAL | `schema_selection_trace` + `pruned_tables` | `error_type ∈ {schema_pruning_error, schema_pruning_exception, schema_context_selection_exception}` → `TraceSpan(RETRIEVAL, ERROR, attributes={"reason_code": error_type})`; retrieval hiç koşmadıysa (`schema_selection_trace is None` ve pruned_tables boş ve hata da retrieval'da değil) → `build_retrieval_span(None)`; aksi halde duck nesneyle `build_retrieval_span`: `candidates=[NS(object_id=t, object_type="table") for t in selected_tables veya pruned_tables]`, `k_requested=None`, `k_returned=len(candidates)`, `retrieval_version="legacy_context_selector_v1"` |
| PROMPT | `prompt_sha256/prompt_char_count` (writer-critic'ten) | `prompt_sha256 is None` → `build_prompt_span(None)`; aksi halde `NS(prompt_sha256=..., prompt_char_count=..., intent_type=None, target_dialect=dialect, source_section_types=(), source_item_ids=(), input_version="legacy_prompt_template_manager")` |
| GENERATION | `attempts_redacted` + `last_generated_sql_redacted` | attempts boş → `build_generation_span(None)`; aksi halde `NS(response=NS(provider_id="legacy_pipeline", model_id=None, finish_reason=None, latency_ms=None, token_usage=None), output_sha256=sha256_text(last_generated_sql_redacted), prompt_sha256=prompt_sha256)` — Task 1 sayesinde output varsa OK, yoksa ERROR |
| VALIDATION | attempts'lerdeki `validation_errors` | attempts boş → `build_validation_span(None, valid=False)` yerine `build_validation_span(None, ...)`... DİKKAT: `issues=None` → SKIPPED, `valid` yok sayılır. Aksi halde: `issues` = TÜM attempts'lerin `validation_errors`'larından `stage ∉ {"sql_guardrail", "sql_sandbox_safety"}` olanlar (duck: dict'ler `SimpleNamespace(**err)` ile sarılır — builder `getattr` kullanır, dict `.type`'a cevap vermez); `valid` = `success or guardrail_or_safety_final` (final attempt'in hatası güvenlikse AST/semantik doğrulama SQL'i reddetmedi); `sql_sha256=sha256_text(last_generated_sql_redacted)` |
| SECURITY | attempts'lerdeki guardrail/safety hataları | attempts boş → `build_security_span(())` → SKIPPED; guardrail/safety hatası varsa her biri için `NS(category=err["stage"], outcome="denied", severity="error", reason_code=err.get("type"), entry_hash=None)` → ERROR; attempts var ama güvenlik hatası yoksa tek `NS(category="sql_guardrail", outcome="allowed", severity="info", reason_code=None, entry_hash=None)` → OK |
| EXECUTION | Faz-1'de yok | HER ZAMAN doğrudan `TraceSpan(stage=TraceStageKind.EXECUTION, status=TraceSpanStatus.SKIPPED)` (`build_execution_span(None)` KULLANILMAZ — EndToEndTraceError fırlatır) |

- `guardrail_or_safety_final` = attempts'in SON elemanının `validation_errors` listesinde `stage ∈ {"sql_guardrail", "sql_sandbox_safety"}` olan en az bir kayıt var.
- `duration_ms`: her builder'a `stage_timings.get(<stage adı>)` verilir (None kabul).
- Dönüş: `build_end_to_end_trace(trace_id=trace_id, request_id=request_id, spans=(intent, retrieval, prompt, generation, validation, security, execution), job_id=job_id, dialect=dialect, nl_query_sha256=sha256_text(natural_query_redacted), total_duration_ms=total_duration_ms)`.
- `sha256_text`: `hashlib.sha256(text.encode("utf-8")).hexdigest()`; `text` None/boş → None.
- Modül importları YALNIZ: `hashlib`, `types.SimpleNamespace`, `typing`, `app.trace.end_to_end_trace`, `app.trace.end_to_end_trace_builders`.

- [ ] **Step 1: Failing testleri yaz** — `backend/tests/trace/test_live_trace_assembly.py` (yeni dosya):

```python
from app.trace.end_to_end_trace import TraceSpanStatus, TraceStageKind, TraceTerminalStatus
from app.trace.live_trace_assembly import assemble_live_end_to_end_trace, sha256_text

BASE = dict(trace_id="e2e_t", request_id="req_r", job_id="j1", dialect="postgres",
            natural_query_redacted="soru", total_duration_ms=100, stage_timings=None,
            schema_selection_trace=None, pruned_tables=[], prompt_sha256=None,
            prompt_char_count=None, attempts_redacted=[], last_generated_sql_redacted=None)


def _span(trace, kind):
    return next(s for s in trace.spans if s.stage is kind)


def test_happy_path_seven_spans_completed():
    ok_attempt = {"attempt": 1, "action": "generate", "sql": "SELECT 1",
                  "valid": True, "error": None}
    t = assemble_live_end_to_end_trace(**{**BASE,
        "error_type": None, "success": True,
        "schema_selection_trace": {"selected_tables": ["users", "orders"]},
        "pruned_tables": ["users", "orders", "extra"],
        "prompt_sha256": "p" * 64, "prompt_char_count": 1200,
        "attempts_redacted": [ok_attempt],
        "last_generated_sql_redacted": "SELECT 1"})
    assert len(t.spans) == 7
    assert t.terminal_status is TraceTerminalStatus.COMPLETED
    assert _span(t, TraceStageKind.INTENT).status is TraceSpanStatus.SKIPPED
    assert _span(t, TraceStageKind.RETRIEVAL).status is TraceSpanStatus.OK
    assert _span(t, TraceStageKind.RETRIEVAL).detail.k_returned == 2  # selected_tables önceliklidir
    assert _span(t, TraceStageKind.PROMPT).status is TraceSpanStatus.OK
    assert _span(t, TraceStageKind.GENERATION).status is TraceSpanStatus.OK
    assert _span(t, TraceStageKind.VALIDATION).status is TraceSpanStatus.OK
    assert _span(t, TraceStageKind.SECURITY).status is TraceSpanStatus.OK
    assert _span(t, TraceStageKind.EXECUTION).status is TraceSpanStatus.SKIPPED
    assert t.nl_query_sha256 == sha256_text("soru")


def test_excel_parse_error_terminal_intent():
    t = assemble_live_end_to_end_trace(**{**BASE, "error_type": "excel_parse_error",
                                          "success": False})
    assert t.terminal_status is TraceTerminalStatus.FAILED
    assert t.terminal_stage is TraceStageKind.INTENT
    assert _span(t, TraceStageKind.RETRIEVAL).status is TraceSpanStatus.SKIPPED
    assert _span(t, TraceStageKind.GENERATION).status is TraceSpanStatus.SKIPPED


def test_retrieval_error_terminal_retrieval():
    t = assemble_live_end_to_end_trace(**{**BASE, "error_type": "schema_pruning_error",
                                          "success": False})
    assert t.terminal_status is TraceTerminalStatus.FAILED
    assert t.terminal_stage is TraceStageKind.RETRIEVAL
    assert _span(t, TraceStageKind.INTENT).status is TraceSpanStatus.SKIPPED


def test_guardrail_block_terminal_security_not_validation():
    bad = {"attempt": 1, "action": "generate", "sql": "", "valid": False,
           "error": "guardrail", "validation_errors": [
               {"type": "forbidden_statement", "stage": "sql_guardrail",
                "message": "DDL yasak"}]}
    t = assemble_live_end_to_end_trace(**{**BASE, "error_type": "sql_generation_failed",
        "success": False, "prompt_sha256": "p" * 64, "prompt_char_count": 10,
        "attempts_redacted": [bad], "last_generated_sql_redacted": "DROP TABLE x"})
    assert _span(t, TraceStageKind.SECURITY).status is TraceSpanStatus.ERROR
    assert _span(t, TraceStageKind.VALIDATION).status is not TraceSpanStatus.ERROR
    assert t.terminal_status is TraceTerminalStatus.FAILED
    assert t.terminal_stage is TraceStageKind.SECURITY
    sec = _span(t, TraceStageKind.SECURITY)
    assert sec.detail.checks[0].reason_code == "forbidden_statement"


def test_semantic_failure_terminal_validation():
    bad = {"attempt": 1, "action": "generate", "sql": "SELECT x", "valid": False,
           "error": "sem", "validation_errors": [
               {"type": "missing_column", "stage": "semantic_validation",
                "message": "kolon yok"}]}
    t = assemble_live_end_to_end_trace(**{**BASE, "error_type": "sql_generation_failed",
        "success": False, "prompt_sha256": "p" * 64, "prompt_char_count": 10,
        "attempts_redacted": [bad], "last_generated_sql_redacted": "SELECT x"})
    assert t.terminal_stage is TraceStageKind.VALIDATION
    assert _span(t, TraceStageKind.SECURITY).status is TraceSpanStatus.OK
    assert _span(t, TraceStageKind.GENERATION).status is TraceSpanStatus.OK


def test_llm_error_no_output_terminal_generation():
    bad = {"attempt": 1, "action": "generate", "sql": "", "valid": False,
           "error": "LLM API", "validation_errors": [
               {"type": "llm_api_error", "stage": "llm_generation", "message": "boom"}]}
    t = assemble_live_end_to_end_trace(**{**BASE, "error_type": "sql_generation_failed",
        "success": False, "prompt_sha256": "p" * 64, "prompt_char_count": 10,
        "attempts_redacted": [bad], "last_generated_sql_redacted": None})
    assert t.terminal_stage is TraceStageKind.GENERATION


def test_stage_timings_land_on_spans():
    ok_attempt = {"attempt": 1, "action": "generate", "sql": "SELECT 1",
                  "valid": True, "error": None}
    t = assemble_live_end_to_end_trace(**{**BASE, "error_type": None, "success": True,
        "schema_selection_trace": {"selected_tables": ["u"]}, "pruned_tables": ["u"],
        "prompt_sha256": "p" * 64, "prompt_char_count": 5,
        "attempts_redacted": [ok_attempt], "last_generated_sql_redacted": "SELECT 1",
        "stage_timings": {"retrieval": 30, "prompt": 5, "generation": 800,
                          "validation": 20, "security": 3}})
    assert _span(t, TraceStageKind.RETRIEVAL).duration_ms == 30
    assert _span(t, TraceStageKind.GENERATION).duration_ms == 800


def test_payload_is_json_safe():
    import json
    t = assemble_live_end_to_end_trace(**{**BASE, "error_type": "input_error",
                                          "success": False})
    json.dumps(t.to_payload())
```

- [ ] **Step 2: FAIL doğrula**

Run: `cd backend && venv\Scripts\python.exe -m pytest tests/trace/test_live_trace_assembly.py -q`
Expected: FAIL — `ModuleNotFoundError: app.trace.live_trace_assembly`.

- [ ] **Step 3: Modülü yaz** — `backend/app/trace/live_trace_assembly.py`:

```python
"""Canlı pipeline verilerinden EndToEndTrace montajı (Sprint 27.1w).

Saf ve I/O-free: legacy run_pipeline'ın ürettiği redakte edilmiş verilerden
(attempts, schema_selection_trace, prompt sha'ları) 7 sabit span kurar.
Span/terminal kuralları spec §1.4'tedir. Import yüzeyi: stdlib + app.trace.*.
"""
import hashlib
from types import SimpleNamespace
from typing import Mapping, Optional, Sequence

from app.trace.end_to_end_trace import (
    EndToEndTrace, TraceSpan, TraceSpanStatus, TraceStageKind,
    build_end_to_end_trace,
)
from app.trace.end_to_end_trace_builders import (
    build_generation_span, build_intent_span, build_prompt_span,
    build_retrieval_span, build_security_span, build_validation_span,
)

_INPUT_ERROR_TYPES = frozenset({"excel_parse_error", "input_error"})
_RETRIEVAL_ERROR_TYPES = frozenset({
    "schema_pruning_error", "schema_pruning_exception",
    "schema_context_selection_exception",
})
_SECURITY_STAGES = frozenset({"sql_guardrail", "sql_sandbox_safety"})


def sha256_text(text: Optional[str]) -> Optional[str]:
    if not text:
        return None
    return hashlib.sha256(text.encode("utf-8")).hexdigest()


def _attempt_issues(attempts: Sequence[Mapping]) -> list[dict]:
    issues = []
    for att in attempts or ():
        issues.extend(att.get("validation_errors") or [])
    return issues


def assemble_live_end_to_end_trace(
    *, trace_id: str, request_id: str, job_id: str, dialect: str,
    natural_query_redacted: str, total_duration_ms: int,
    stage_timings: Optional[Mapping[str, int]],
    error_type: Optional[str], success: bool,
    schema_selection_trace: Optional[Mapping], pruned_tables: Sequence[str],
    prompt_sha256: Optional[str], prompt_char_count: Optional[int],
    attempts_redacted: Sequence[Mapping],
    last_generated_sql_redacted: Optional[str],
) -> EndToEndTrace:
    timings = dict(stage_timings or {})
    attempts = list(attempts_redacted or [])
    all_issues = _attempt_issues(attempts)
    security_issues = [e for e in all_issues if e.get("stage") in _SECURITY_STAGES]
    validation_issues = [e for e in all_issues if e.get("stage") not in _SECURITY_STAGES]
    final_issues = (attempts[-1].get("validation_errors") or []) if attempts else []
    guardrail_or_safety_final = any(
        e.get("stage") in _SECURITY_STAGES for e in final_issues)

    # INTENT — canlı yolda intent extraction koşmaz.
    if error_type in _INPUT_ERROR_TYPES:
        intent_span = TraceSpan(stage=TraceStageKind.INTENT,
                                status=TraceSpanStatus.ERROR,
                                attributes={"reason_code": error_type})
    else:
        intent_span = build_intent_span(None)

    # RETRIEVAL
    if error_type in _RETRIEVAL_ERROR_TYPES:
        retrieval_span = TraceSpan(stage=TraceStageKind.RETRIEVAL,
                                   status=TraceSpanStatus.ERROR,
                                   attributes={"reason_code": error_type})
    else:
        table_names = list((schema_selection_trace or {}).get("selected_tables")
                           or pruned_tables or [])
        if not table_names and schema_selection_trace is None:
            retrieval_span = build_retrieval_span(
                None, duration_ms=timings.get("retrieval"))
        else:
            duck = SimpleNamespace(
                k_requested=None, k_returned=len(table_names),
                retrieval_version="legacy_context_selector_v1",
                candidates=[SimpleNamespace(object_id=t, object_type="table")
                            for t in table_names])
            retrieval_span = build_retrieval_span(
                duck, duration_ms=timings.get("retrieval"))

    # PROMPT
    if prompt_sha256 is None:
        prompt_span = build_prompt_span(None)
    else:
        prompt_span = build_prompt_span(
            SimpleNamespace(prompt_sha256=prompt_sha256,
                            prompt_char_count=prompt_char_count,
                            intent_type=None, target_dialect=dialect,
                            source_section_types=(), source_item_ids=(),
                            input_version="legacy_prompt_template_manager"),
            duration_ms=timings.get("prompt"))

    # GENERATION
    if not attempts:
        generation_span = build_generation_span(None)
    else:
        generation_span = build_generation_span(
            SimpleNamespace(
                response=SimpleNamespace(provider_id="legacy_pipeline",
                                         model_id=None, finish_reason=None,
                                         latency_ms=None, token_usage=None),
                output_sha256=sha256_text(last_generated_sql_redacted),
                prompt_sha256=prompt_sha256),
            duration_ms=timings.get("generation"))

    # VALIDATION
    if not attempts:
        validation_span = build_validation_span(None, valid=False)
    else:
        validation_span = build_validation_span(
            [SimpleNamespace(**e) for e in validation_issues],
            valid=bool(success or guardrail_or_safety_final),
            sql_sha256=sha256_text(last_generated_sql_redacted),
            duration_ms=timings.get("validation"))

    # SECURITY
    if not attempts:
        security_span = build_security_span(())
    elif security_issues:
        security_span = build_security_span([
            SimpleNamespace(category=e.get("stage"), outcome="denied",
                            severity="error", reason_code=e.get("type"),
                            entry_hash=None)
            for e in security_issues])
    else:
        security_span = build_security_span([
            SimpleNamespace(category="sql_guardrail", outcome="allowed",
                            severity="info", reason_code=None, entry_hash=None)])

    # EXECUTION — Faz-1'de canlı çalıştırma yok (spec §1.4).
    execution_span = TraceSpan(stage=TraceStageKind.EXECUTION,
                               status=TraceSpanStatus.SKIPPED)

    return build_end_to_end_trace(
        trace_id=trace_id, request_id=request_id,
        spans=(intent_span, retrieval_span, prompt_span, generation_span,
               validation_span, security_span, execution_span),
        job_id=job_id, dialect=dialect,
        nl_query_sha256=sha256_text(natural_query_redacted),
        total_duration_ms=total_duration_ms)
```

Not: `TraceSpan`'ın `attributes` parametre adı/varsayılanı ve `status=ERROR` + `detail=None` kombinasyonunun kabul edildiği `end_to_end_trace.py:142` civarındaki `__post_init__`'e göre doğrulanmalı; farklıysa (ör. attributes boş dict zorunluysa ya da ERROR span detail istiyorsa) çağrı tanıma uyarlanır — anlam (INTENT/RETRIEVAL hata işareti + reason_code) aynı kalır. `build_validation_span(None, valid=False)` çağrısında `issues=None` → SKIPPED (valid yok sayılır — builder sözleşmesi).

- [ ] **Step 4: PASS doğrula**

Run: `cd backend && venv\Scripts\python.exe -m pytest tests/trace/ -q`
Expected: hepsi PASS (driver-isolation testi dahil).

- [ ] **Step 5: Commit**

```bash
git add backend/app/trace/live_trace_assembly.py backend/tests/trace/test_live_trace_assembly.py
git commit -m "feat(trace): pure live-trace assembly for the legacy pipeline path"
```

---

### Task 3: `_stage_intent` + `_stage_retrieval` çıkarımı (davranış-koruyan refactor)

**Files:**
- Modify: `backend/app/sql_pipeline.py:174-241` (intent bloğu), `:243-352` (retrieval bloğu), `run_pipeline` gövdesi
- Test: mevcut suite (yeni test yok — koruma kanıtı mevcut testler)

**Interfaces:**
- Produces (Task 4/6 kullanır):
  ```python
  def _stage_intent(self, *, excel_file_path, natural_query, log_callback
                    ) -> tuple[Optional[dict], Optional[dict], int]
  # dönüş: (aqr, error, elapsed_ms); error = {"message": str, "error_type": str} | None

  def _stage_retrieval(self, *, aqr, natural_query, dialect, log_callback
                       ) -> tuple[dict, Optional[str], Optional[dict], Optional[dict], int]
  # dönüş: (pruned_schema, prompt_schema_context, schema_selection_trace, error, elapsed_ms)
  # error = {"message": str, "error_type": str, "pruned_schema_for_trace": dict,
  #          "pruned_tables": list[str]} | None
  ```

- [ ] **Step 1: `_stage_intent` metodunu ekle** (sınıf içine, `run_pipeline`'dan önce). Gövde = mevcut 174-241 satırlarının TAŞINMASI; değişen TEK şey: `result["error"] = ...` + `_capture_trace_on_exit(...)` + `return redact_sensitive(...)` üçlüsü yerine `(None, {"message": ..., "error_type": "excel_parse_error"|"input_error"}, elapsed_ms)` dönülür; başarıda `(aqr, None, elapsed_ms)`. Metodun ilk satırı `t0 = time.perf_counter()`, her dönüşte `elapsed_ms = int((time.perf_counter() - t0) * 1000)`. `log_callback` çağrıları TAŞINDIĞI YERDE AYNEN kalır (mesaj metinleri değişmez).

- [ ] **Step 2: `_stage_retrieval` metodunu ekle.** Gövde = mevcut 243-352 satırlarının taşınması; aynı dönüşüm: üç hata dalı (`schema_pruning_error`, `schema_context_selection_exception`, `schema_pruning_exception`) `error` dict'i döner (`pruned_schema_for_trace` alanına o dalda `_capture`'a giden `pruned_schema` değeri konur: ör. `{"error": msg}` ya da gerçek `pruned_schema`), başarıda `(pruned_schema, prompt_schema_context, schema_selection_trace, None, elapsed_ms)`. `schema_selection_trace` hata dallarında da (mevcut davranıştaki gibi) dönüşe dahil edilir — context-selection hatasında dolu dict döner.

- [ ] **Step 3: `run_pipeline` gövdesinde iki bloğu çağrılara indirge:**

```python
        # 1. Girdi yorumlama (Excel AQR / doğal dil)
        aqr, intent_error, intent_ms = self._stage_intent(
            excel_file_path=excel_file_path, natural_query=natural_query,
            log_callback=log_callback)
        if intent_error:
            result["error"] = intent_error["message"]
            self._capture_trace_on_exit(
                start_time=start_time, job_id=job_id, dialect=dialect,
                natural_query=natural_query or "",
                pruned_schema={"error": result["error"]},
                generated_sql=None, last_generated_sql=None, sql_valid=None,
                sql_validation_errors=[], attempts=[],
                error_message=result["error"],
                error_type=intent_error["error_type"],
                known_secrets=known_secrets,
                schema_selection_trace=None)
            return redact_sensitive(result, known_secrets)
        result["aqr"] = aqr

        # 2. Şema yükleme + budama + context selection
        pruned_schema, prompt_schema_context, schema_selection_trace, retr_error, retrieval_ms = \
            self._stage_retrieval(aqr=aqr, natural_query=natural_query,
                                  dialect=dialect, log_callback=log_callback)
        if retr_error:
            result["error"] = retr_error["message"]
            self._capture_trace_on_exit(
                start_time=start_time, job_id=job_id, dialect=dialect,
                natural_query=natural_query or aqr.get("natural_query", ""),
                pruned_schema=retr_error["pruned_schema_for_trace"],
                generated_sql=None, last_generated_sql=None, sql_valid=None,
                sql_validation_errors=[], attempts=[],
                error_message=result["error"],
                error_type=retr_error["error_type"],
                known_secrets=known_secrets,
                schema_selection_trace=schema_selection_trace)
            return redact_sensitive(result, known_secrets)
        result["pruned_schema_tables"] = list(pruned_schema.get("tables", {}).keys())
```

Dikkat: mevcut kodda `result["pruned_schema_tables"]` budama BAŞARISINDA, context-selection'dan ÖNCE set ediliyor (satır 271) — bu davranış korunmalı: `_stage_retrieval` başarılı budamadan sonra context-selection hatası dönerse bile orkestratör `result["pruned_schema_tables"]`'ı DOLDURMAZ (mevcut kodda da context-selection hatasında dolu kalıyordu — satır 271 hata dalından önce çalışıyor!). Birebir korumak için: `_stage_retrieval` error dict'ine `"pruned_tables": list(...)` alanı eklenir ve orkestratör hata dalında `result["pruned_schema_tables"] = retr_error.get("pruned_tables", [])` yapar (budama hatasında boş, context-selection hatasında dolu).

- [ ] **Step 4: Mevcut testlerle koruma kanıtı**

Run: `cd backend && venv\Scripts\python.exe -m pytest tests/test_sql_pipeline_llm_provider_integration.py tests/test_sql_pipeline_sandbox.py tests/test_pipeline_error_envelope_contract.py -q`
Expected: hepsi PASS, test dosyalarında sıfır değişiklik.

- [ ] **Step 5: Commit**

```bash
git add backend/app/sql_pipeline.py
git commit -m "refactor(pipeline): extract intent and retrieval stage methods with timings"
```

---

### Task 4: `_stage_writer_critic` çıkarımı + döngü içi zamanlama

**Files:**
- Modify: `backend/app/sql_pipeline.py:354-537` (writer-critic bloğu)
- Test: mevcut suite + `backend/tests/test_sql_pipeline_stage_timings.py` (yeni)

**Interfaces:**
- Produces (Task 6 kullanır):
  ```python
  def _stage_writer_critic(self, *, aqr, natural_query, previous_sql,
                           prompt_schema_context, pruned_schema, dialect,
                           api_key, max_attempts, log_callback, result
                           ) -> tuple[dict, dict[str, int]]
  # result dict'ini AYNEN bugünkü gibi yerinde mutasyona uğratır
  # (attempts/success/generated_sql). Dönüş:
  # wc = {"last_generated_sql": str|None, "validation_errors": list,
  #       "prompt_sha256": str|None, "prompt_char_count": int|None,
  #       "natural_query": str}
  # timings = {"prompt": int, "generation": int, "validation": int, "security": int}
  ```

- [ ] **Step 1: Metodu ekle.** Gövde = mevcut 354-537 satırlarının taşınması (attempt döngüsü + post-loop success/failure çözümü DAHİL). Değişiklikler:
  - Metod başı: `natural_query = aqr.get("natural_query", "")` (mevcut satır 355) ve `timings = {"prompt": 0, "generation": 0, "validation": 0, "security": 0}`.
  - Prompt kurulumu (`PromptTemplateManager.get_*_prompt`) `t = time.perf_counter()` / `timings["prompt"] += int((time.perf_counter() - t) * 1000)` ile sarılır.
  - `self._generate_sql(...)` çağrısı aynı kalıpla `timings["generation"]`'a eklenir.
  - Guardrail (`SQLGuardrailValidator.validate`) + sandbox (`SqlSafetyValidator().ensure_read_only`) blokları BİRLİKTE `timings["security"]`'ye eklenir (iki ayrı sarma, aynı sayaca `+=`).
  - AST (`sqlglot.parse_one` doğrulama bloğu) + semantik (`SQLValidator.validate`) `timings["validation"]`'a eklenir.
  - Prompt üretiminden hemen sonra: `prompt_sha256 = hashlib.sha256(prompt.encode("utf-8")).hexdigest()`, `prompt_char_count = len(prompt)` (her attempt'te güncellenir — SON attempt'inki döner). `import hashlib` dosya başına eklenir.
  - Dönüş: yukarıdaki `wc` + `timings`.
  - `log_callback` mesajları ve TÜM kontrol akışı (continue/break dalları) AYNEN korunur.

- [ ] **Step 2: `run_pipeline`'da bloğu çağrıya indirge:**

```python
        # 3. İteratif üretim döngüsü (writer-critic)
        wc, wc_timings = self._stage_writer_critic(
            aqr=aqr, natural_query=natural_query, previous_sql=previous_sql,
            prompt_schema_context=prompt_schema_context,
            pruned_schema=pruned_schema, dialect=dialect, api_key=api_key,
            max_attempts=max_attempts, log_callback=log_callback, result=result)
        natural_query = wc["natural_query"]
        last_generated_sql = wc["last_generated_sql"]
        validation_errors = wc["validation_errors"]
```

Sonraki mevcut blok (539-569, final trace kaydı) `pruned_schema_ref = locals().get(...)` yerine artık doğrudan `pruned_schema` kullanır (retrieval başarılıysa her zaman tanımlı — davranış aynı).

- [ ] **Step 3: Yeni timing testi** — `backend/tests/test_sql_pipeline_stage_timings.py`:

```python
from unittest.mock import MagicMock
from app.llm.fake_provider import DeterministicFakeLLMProvider
from app.sql_pipeline import SQLGenerationPipeline


def _mocked_pipeline():
    pipeline = SQLGenerationPipeline(
        schema_manager=MagicMock(),
        llm_provider=DeterministicFakeLLMProvider(sql="SELECT 1"),
        trace_store=None)
    pipeline.schema_pruner = MagicMock()
    pipeline.schema_pruner.prune_schema.return_value = {
        "tables": {"users": {"columns": [{"name": "id", "type": "int"}]}},
        "estimated_tokens": 10}
    return pipeline


def test_writer_critic_returns_timings_and_prompt_hash():
    pipeline = _mocked_pipeline()
    result = {"success": False, "aqr": None, "pruned_schema_tables": [],
              "generated_sql": "", "attempts": [], "error": None}
    wc, timings = pipeline._stage_writer_critic(
        aqr={"natural_query": "kaç kullanıcı var"}, natural_query="kaç kullanıcı var",
        previous_sql=None, prompt_schema_context="TABLE users(id int)",
        pruned_schema={"tables": {"users": {"columns": [{"name": "id", "type": "int"}]}}},
        dialect="postgres", api_key=None, max_attempts=3,
        log_callback=None, result=result)
    assert result["success"] is True
    assert set(timings) == {"prompt", "generation", "validation", "security"}
    assert all(isinstance(v, int) and v >= 0 for v in timings.values())
    assert isinstance(wc["prompt_sha256"], str) and len(wc["prompt_sha256"]) == 64
    assert wc["prompt_char_count"] > 0
    assert wc["last_generated_sql"]
```

- [ ] **Step 4: FAIL → implement → PASS döngüsü**

Run (önce): `cd backend && venv\Scripts\python.exe -m pytest tests/test_sql_pipeline_stage_timings.py -q` → FAIL (`_stage_writer_critic` yok). Step 1-2 uygulanır, sonra:
Run: `cd backend && venv\Scripts\python.exe -m pytest tests/test_sql_pipeline_stage_timings.py tests/test_sql_pipeline_llm_provider_integration.py tests/test_sql_pipeline_sandbox.py tests/test_pipeline_error_envelope_contract.py -q`
Expected: hepsi PASS.

- [ ] **Step 5: Commit**

```bash
git add backend/app/sql_pipeline.py backend/tests/test_sql_pipeline_stage_timings.py
git commit -m "refactor(pipeline): extract writer-critic stage with per-phase timing breakdown"
```

---

### Task 5: request_id plumbing (HTTP → background job → run_pipeline)

**Files:**
- Modify: `backend/app/main.py:163-167` (upload imzası), `:195`, `:204-208` (no-file imzası), `:218`, `:328` (`process_job_pipeline` imzası), `:374-382`
- Modify: `backend/app/sql_pipeline.py` (`run_pipeline` imzası)
- Test: `backend/tests/test_request_id_plumbing.py` (yeni)

**Interfaces:**
- Produces: `run_pipeline(..., request_id: Optional[str] = None)`; içeride `rid = request_id or f"req_{uuid.uuid4().hex}"`; `rid` `_capture_trace_on_exit(..., request_id=rid)` olarak geçirilir (parametre bu task'ta eklenir, Task 6'ya kadar legacy yol onu KULLANMAZ). `process_job_pipeline(job_id: str, request_id: Optional[str] = None)`.

- [ ] **Step 1: Failing test yaz** — `backend/tests/test_request_id_plumbing.py`:

```python
from unittest.mock import MagicMock
from app.llm.fake_provider import DeterministicFakeLLMProvider
from app.sql_pipeline import SQLGenerationPipeline


def _pipeline_with_spy():
    pipeline = SQLGenerationPipeline(
        schema_manager=MagicMock(),
        llm_provider=DeterministicFakeLLMProvider(sql="SELECT 1"),
        trace_store=MagicMock(spec=["save"]))
    pipeline.schema_pruner = MagicMock()
    pipeline.schema_pruner.prune_schema.return_value = {
        "tables": {"users": {"columns": [{"name": "id", "type": "int"}]}},
        "estimated_tokens": 10}
    captured = {}
    original = pipeline._capture_trace_on_exit

    def spy(**kwargs):
        captured.update(kwargs)
        return original(**kwargs)

    pipeline._capture_trace_on_exit = spy
    return pipeline, captured


def test_given_request_id_reaches_trace_capture():
    pipeline, captured = _pipeline_with_spy()
    pipeline.run_pipeline(job_id="j1", natural_query="kaç kullanıcı",
                          dialect="postgres", request_id="req_verilen")
    assert captured["request_id"] == "req_verilen"


def test_missing_request_id_is_minted_with_prefix():
    pipeline, captured = _pipeline_with_spy()
    pipeline.run_pipeline(job_id="j1", natural_query="kaç kullanıcı",
                          dialect="postgres")
    assert captured["request_id"].startswith("req_")
    assert len(captured["request_id"]) > 8


def test_background_task_receives_request_id_from_http_state():
    import inspect
    from app import main as app_main
    sig = inspect.signature(app_main.process_job_pipeline)
    assert "request_id" in sig.parameters
```

- [ ] **Step 2: FAIL doğrula**

Run: `cd backend && venv\Scripts\python.exe -m pytest tests/test_request_id_plumbing.py -q`
Expected: FAIL (`unexpected keyword argument 'request_id'`).

- [ ] **Step 3: Uygula**

(a) `sql_pipeline.py` — `run_pipeline` imzasına `request_id: Optional[str] = None,` ekle (max_attempts'ten sonra). Gövdede `start_time = time.perf_counter()` satırından hemen önce:
```python
        import uuid as _uuid
        rid = request_id or f"req_{_uuid.uuid4().hex}"
```
(Dosya başına `import uuid` eklemek de kabul — tek kural: `Math`/global state yok.) `run_pipeline` içindeki TÜM `_capture_trace_on_exit` çağrılarına (Task 3 sonrası 3 adet: intent hata dalı, retrieval hata dalı, final) `request_id=rid,` eklenir. `_capture_trace_on_exit` imzasına `request_id: Optional[str] = None,` eklenir (şimdilik gövdede kullanılmaz — Task 6 kullanacak).

(b) `main.py` —
- `from fastapi import Request` import'unun mevcut fastapi import satırında olduğunu doğrula, yoksa ekle.
- `upload_excel_file` imzasına ilk parametre olarak `request: Request,` ekle; `background_tasks.add_task(process_job_pipeline, job_id, request_id=getattr(request.state, "request_id", None))`.
- `start_job_without_file`: gövde modeli `request: JobCreateRequest` adını kullanıyor — HTTP nesnesi için `http_request: Request,` parametresi eklenir (isim çakışması nedeniyle); `background_tasks.add_task(process_job_pipeline, job_id, request_id=getattr(http_request.state, "request_id", None))`.
- `process_job_pipeline(job_id: str, request_id: Optional[str] = None)`; `pipeline.run_pipeline(...)` çağrısına `request_id=request_id,` eklenir.

- [ ] **Step 4: PASS + API sözleşme regresyonu**

Run: `cd backend && venv\Scripts\python.exe -m pytest tests/test_request_id_plumbing.py tests/test_api_pipeline_integration_contract.py tests/test_sql_pipeline_llm_provider_integration.py -q`
Expected: hepsi PASS (FastAPI `Request` parametresi response şemasını etkilemez).

- [ ] **Step 5: Commit**

```bash
git add backend/app/main.py backend/app/sql_pipeline.py backend/tests/test_request_id_plumbing.py
git commit -m "feat(pipeline): thread http request_id into the job pipeline with minted fallback"
```

---

### Task 6: Dual-emit — `_capture_trace_on_exit`'te EndToEndTrace emisyonu

**Files:**
- Modify: `backend/app/sql_pipeline.py:571-629` (`_capture_trace_on_exit`), `run_pipeline` çağrıları
- Test: `backend/tests/test_live_trace_emission.py` (yeni)

**Interfaces:**
- Consumes: Task 2 `assemble_live_end_to_end_trace`, Task 3-5 stage dönüşleri (`intent_ms`, `retrieval_ms`, `wc_timings`, `wc["prompt_sha256"]`, `rid`).
- Produces: her `run_pipeline` çıkışında trace_store'a İKİ kayıt: legacy `NL2SQLTrace` + `TraceRecord(trace_type="end_to_end", request_id=..., job_id=..., payload=e2e.to_payload())`.

- [ ] **Step 1: Failing testleri yaz** — `backend/tests/test_live_trace_emission.py`:

```python
from unittest.mock import MagicMock
from app.llm.fake_provider import DeterministicFakeLLMProvider
from app.sql_pipeline import SQLGenerationPipeline
from app.trace.models import TraceRecord


class RecordingStore:
    def __init__(self):
        self.records = []
        self.legacy = []

    def save(self, trace):
        self.records.append(trace)

    def save_legacy(self, trace):
        self.legacy.append(trace)


def _pipeline(store):
    pipeline = SQLGenerationPipeline(
        schema_manager=MagicMock(),
        llm_provider=DeterministicFakeLLMProvider(sql="SELECT 1"),
        trace_store=store)
    pipeline.schema_pruner = MagicMock()
    pipeline.schema_pruner.prune_schema.return_value = {
        "tables": {"users": {"columns": [{"name": "id", "type": "int"}]}},
        "estimated_tokens": 10}
    return pipeline


def _e2e_records(store):
    return [r for r in store.records
            if isinstance(r, TraceRecord) and r.trace_type == "end_to_end"]


def test_dual_emit_legacy_plus_end_to_end():
    store = RecordingStore()
    _pipeline(store).run_pipeline(job_id="j1", natural_query="kaç kullanıcı",
                                  dialect="postgres", request_id="req_x")
    assert len(store.legacy) == 1
    e2e = _e2e_records(store)
    assert len(e2e) == 1
    rec = e2e[0]
    assert rec.request_id == "req_x" and rec.job_id == "j1"
    payload = rec.payload
    assert len(payload["spans"]) == 7
    assert payload["terminal_status"] == "completed"
    by_stage = {s["stage"]: s for s in payload["spans"]}
    assert by_stage["execution"]["status"] == "skipped"
    assert by_stage["generation"]["status"] == "ok"
    assert isinstance(payload["total_duration_ms"], int)


def test_early_input_error_emits_failed_at_intent():
    store = RecordingStore()
    _pipeline(store).run_pipeline(job_id="j2", dialect="postgres",
                                  request_id="req_y")  # ne excel ne query
    rec = _e2e_records(store)[0]
    assert rec.payload["terminal_status"] == "failed"
    assert rec.payload["terminal_stage"] == "intent"
    by_stage = {s["stage"]: s for s in rec.payload["spans"]}
    assert by_stage["retrieval"]["status"] == "skipped"


def test_emission_failure_does_not_change_pipeline_result(monkeypatch):
    store = RecordingStore()
    pipeline = _pipeline(store)
    import app.sql_pipeline as sp

    def boom(**kwargs):
        raise RuntimeError("assembly exploded")

    monkeypatch.setattr(sp, "assemble_live_end_to_end_trace", boom)
    res = pipeline.run_pipeline(job_id="j3", natural_query="kaç kullanıcı",
                                dialect="postgres")
    assert res["success"] is True  # e2e patlasa da sonuç değişmez
    assert len(store.legacy) == 1  # legacy kayıt yine atıldı


def test_no_raw_sql_in_e2e_payload():
    store = RecordingStore()
    _pipeline(store).run_pipeline(job_id="j4", natural_query="kaç kullanıcı",
                                  dialect="postgres")
    import json
    text = json.dumps(_e2e_records(store)[0].payload)
    assert "SELECT 1" not in text  # yalnız sha256 taşınır
```

- [ ] **Step 2: FAIL doğrula**

Run: `cd backend && venv\Scripts\python.exe -m pytest tests/test_live_trace_emission.py -q`
Expected: FAIL (e2e kayıt yok).

- [ ] **Step 3: Uygula**

(a) `sql_pipeline.py` importlarına ekle:
```python
from app.trace.live_trace_assembly import assemble_live_end_to_end_trace
from app.trace.models import TraceRecord
import uuid
```
(`TraceRecord`'un `datetime`/`created_at` zorunluluğu varsa `app/trace/models.py:22-38`'e bakılıp kurucusu ona göre çağrılır — auto-default varsa dokunulmaz.)

(b) `_capture_trace_on_exit` imzasına ekle (hepsi opsiyonel — mevcut 5 çağrı bozulmaz):
```python
        request_id: Optional[str] = None,
        success: bool = False,
        stage_timings: Optional[Dict[str, int]] = None,
        prompt_sha256: Optional[str] = None,
        prompt_char_count: Optional[int] = None,
```

(c) `_capture_trace_on_exit` gövdesinin SONUNA (mevcut `self._save_trace_safely(trace)` satırından sonra) ekle:
```python
        # --- Sprint 27.1w: EndToEndTrace dual-emit (asla pipeline'ı düşürmez) ---
        try:
            e2e = assemble_live_end_to_end_trace(
                trace_id=f"e2e_{uuid.uuid4().hex}",
                request_id=request_id or f"req_{uuid.uuid4().hex}",
                job_id=job_id, dialect=dialect,
                natural_query_redacted=natural_query,   # yukarıda redakte edildi
                total_duration_ms=total_ms,
                stage_timings=stage_timings,
                error_type=error_type, success=success,
                schema_selection_trace=schema_selection_trace,
                pruned_tables=list((pruned_schema or {}).get("tables", {}).keys()),
                prompt_sha256=prompt_sha256, prompt_char_count=prompt_char_count,
                attempts_redacted=attempts or [],
                last_generated_sql_redacted=last_generated_sql)
            record = TraceRecord(trace_type="end_to_end",
                                 request_id=e2e.request_id, job_id=job_id,
                                 payload=e2e.to_payload())
            self._save_trace_safely(record)
        except Exception as e:
            logger.error(f"Failed to emit end-to-end trace: {e}")
```

(d) `run_pipeline`'daki 4 `_capture_trace_on_exit` çağrısı güncellenir:
- intent hata dalı: `request_id=rid, success=False, stage_timings={"intent": intent_ms}`
- retrieval hata dalı: `request_id=rid, success=False, stage_timings={"intent": intent_ms, "retrieval": retrieval_ms}`
- (retrieval'ın üçüncü/genel exception dalı Task 3'te tek dala indirildi — aynı kwargs)
- final çağrı: `request_id=rid, success=result["success"], stage_timings={"intent": intent_ms, "retrieval": retrieval_ms, **wc_timings}, prompt_sha256=wc["prompt_sha256"], prompt_char_count=wc["prompt_char_count"]`

- [ ] **Step 4: PASS + tam pipeline regresyonu**

Run: `cd backend && venv\Scripts\python.exe -m pytest tests/test_live_trace_emission.py tests/test_sql_pipeline_llm_provider_integration.py tests/test_sql_pipeline_sandbox.py tests/test_pipeline_error_envelope_contract.py tests/test_trace_store_contract.py tests/trace/ -q`
Expected: hepsi PASS.

- [ ] **Step 5: Commit**

```bash
git add backend/app/sql_pipeline.py backend/tests/test_live_trace_emission.py
git commit -m "feat(trace): dual-emit end-to-end trace from the live pipeline"
```

---

### Task 7: CHANGELOG + tam süpürme

**Files:**
- Modify: `CHANGELOG.md` (Unreleased → `### Added`)

**Interfaces:** —

- [ ] **Step 1: CHANGELOG girdisi ekle** — Unreleased bölümündeki `### Added` altına (yoksa `### Changed`'den önce oluştur):

```markdown
- **Observability: Canlı Uçtan Uca Trace (Sprint 27.1w)**: 27.0/27.1'in saf
  `EndToEndTrace` sözleşmesi canlı pipeline'a örüldü — HTTP `X-Request-ID`
  arka plan job'ına taşınır (yoksa `req_` önekiyle üretilir), her stage
  `perf_counter` ile ölçülür ve `run_pipeline` çıkışında legacy trace'in
  YANINA `trace_type="end_to_end"` kaydı (7 sabit span: INTENT/RETRIEVAL/
  PROMPT/GENERATION/VALIDATION/SECURITY/EXECUTION; koşmayan stage SKIPPED,
  EXECUTION Faz-1'de hep SKIPPED) emit edilir. `run_pipeline` üç stage
  metoduna ayrıldı (davranış birebir korunur). Trace emisyon hatası pipeline
  sonucunu asla etkilemez; SQL payload'a asla ham girmez (yalnız sha256).
  27.1'den devreden 3 Minor builder düzeltmesi kapatıldı. Yeni saf modül:
  `backend/app/trace/live_trace_assembly.py`. Spec:
  `docs/superpowers/specs/2026-07-06-sprint-27.1w-live-trace-wiring-design.md`.
```

- [ ] **Step 2: Tam doğrulama süiti**

```bash
cd backend && venv\Scripts\python.exe -m pytest -q
```
Expected: taban çizgisi 2024 + yeni testler, 0 fail, 9 skipped.

- [ ] **Step 3: Commit**

```bash
git add CHANGELOG.md
git commit -m "docs: changelog entry for sprint 27.1w live trace wiring"
```

Plan biter; merge/PR kararı kullanıcıya sorulur (`superpowers:finishing-a-development-branch`). PUSH/PR kullanıcı onayı olmadan YAPILMAZ.
