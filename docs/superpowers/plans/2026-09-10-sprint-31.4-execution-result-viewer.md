# Sprint 31.4 — Execution Result Viewer Implementation Plan

> Frontend (Vue 3). Verify: `cd frontend && npm run build` → 0 errors, exit 0.
> Spec: `docs/superpowers/specs/2026-09-10-sprint-31.4-execution-result-viewer-design.md`

**Goal:** Reusable read-only `ResultTable.vue` (30.4 `QueryRunResult` contract) + honest ChatView
integration point. No backend change, no fake data (live execution is §30, out of scope).

## Global Constraints
- New `frontend/src/components/ResultTable.vue`, `QueryResult` type in `api.ts`, edit `ChatView.vue` (+ docs).
- No new deps; read-only; additive; no runtime behavior change.
- Verify = build exit 0. Commit messages: plain, NO trailer / co-author / "Generated with".

---

### Task 1: QueryResult type + ResultTable.vue

- [ ] **Step 1:** Add `export interface QueryResult { columns: string[]; rows: unknown[][]; truncated?: boolean; duration_ms?: number | null }` to `api.ts` (mirrors 30.4 backend `QueryRunResult`).
- [ ] **Step 2:** `ResultTable.vue` — prop `result: QueryResult`; top bar (row count + duration_ms if set + "kesildi" badge when truncated); scrollable `<table>` with sticky `<th scope="col">` headers = columns, body rows; `null` cell → italic `NULL`; empty rows → "Sonuç boş" state. `overflow-x-auto`.

### Task 2: ChatView integration (honest)

- [ ] **Step 1:** Add `result?: QueryResult` to `ChatMessage`; `import ResultTable`.
- [ ] **Step 2:** Under the SQL card, a "Sonuç" section: `v-if="msg.result"` → `<ResultTable :result="msg.result" />`; else (when `msg.sql`) a one-line honest note that execution isn't wired (§30). No fake table.

- [ ] **Step 3: Verify** — `cd frontend && npm run build` → 0 errors, exit 0.
- [ ] **Step 4: Commit** — `git commit -m "feat(31.4): reusable ResultTable viewer (30.4 result contract) + honest ChatView integration (live execution deferred, TECH-DEBT 30)"`

### Task 3: Living docs
- [ ] Roadmap / planned-sprints / changelog / pr-log `#N` backfill. TECH-DEBT §30 note: presentation ready, live population still deferred.

## Self-Review
Spec §2 → Tasks 1-2; §3 verify → Task 2 Step 3; §4 out-of-scope honored (no live execution, no fake data — honest note instead). Component mirrors 30.4 contract for future wiring. ✓

## Controller finishing flow
Self-review → push/PR → backend CI (frontend-only → green) → `#N` backfill → merge `--rebase --delete-branch` → main pull → memory: archive 31.4, flip `durum.md` to 31.5 Error/Warning UX, update `MEMORY.md`. NO commit trailer.
