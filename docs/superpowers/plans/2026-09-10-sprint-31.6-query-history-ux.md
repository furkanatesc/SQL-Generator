# Sprint 31.6 — Query History UX Implementation Plan

> Frontend (Vue 3). Verify: `cd frontend && npm run build` → 0 errors, exit 0.
> Spec: `docs/superpowers/specs/2026-09-10-sprint-31.6-query-history-ux-design.md`

**Goal:** Client-side text search + status filter over the loaded job history, filtered-empty state,
and reuse of 31.5 `describeError` in the failed-detail modal. No new API call, no backend change.

## Global Constraints
- Edit only `frontend/src/components/History.vue` (+ docs). Reuse `errorInfo.ts`. No new deps.
- Preserve existing table/modal/cancel/copy flow. Verify = build exit 0. Commit: plain, NO trailer.

---

### Task 1: Search + status filter

- [ ] **Step 1:** `import { computed }` + `import { describeError }`; `searchText = ref('')`, `statusFilter = ref<'all' | Job['status']>('all')`.
- [ ] **Step 2:** `filteredJobs` computed — status match (unless 'all') AND case-insensitive substring of `searchText` in `natural_query` || `result_sql`.
- [ ] **Step 3:** Filter bar above the table: search input (with clear button) + status `<select>` + "N / M sorgu" counter. Table `v-for` uses `filteredJobs`.
- [ ] **Step 4:** Filtered-empty state (`jobs.length>0 && filteredJobs.length===0`) distinct from no-history.

### Task 2: describeError in failed modal

- [ ] **Step 1:** In the `status === 'failed'` modal panel, add a `describeError(selectedJob.error_code, selectedJob.error_message)` header (category badge + label + hint, tone color) above the raw message block.

- [ ] **Step 2: Verify** — `cd frontend && npm run build` → 0 errors, exit 0.
- [ ] **Step 3: Commit** — `git commit -m "feat(31.6): query history search + status filter + consistent error taxonomy in the detail modal"`

### Task 3: Living docs — roadmap/planned-sprints/changelog/pr-log `#N` backfill.

## Self-Review
Spec §2 → Tasks 1-2; §3 verify → Task 2 Step 2; §4 out-of-scope (client-side only, no server /query-history, no date range). Reuses 31.5. ✓

## Controller finishing flow
Self-review → push/PR → backend CI (frontend-only → green) → `#N` backfill → merge `--rebase --delete-branch` → main pull → memory: archive 31.6, flip `durum.md` to 31.7 Feedback UX, update `MEMORY.md`. NO commit trailer.
