# Sprint & PR Günlüğü (Baştan Sona)

Bu dosya, projenin **tüm geliştirme adımlarını baştan sona** (ilk commit → bugün)
sprint ve PR bazında listeler. `main` branch git geçmişinden üretilmiştir
(177 commit, #1–#121 PR + numarasız erken PR'lar).

- Sürüm/özet görünümü için: `CHANGELOG.md`
- İleriye dönük plan ve "şu an neredeyiz": `ROADMAP.md`
- İleriye dönük (henüz yapılmamış) sprintler: `docs/PLANNED-SPRINTS.md`
- Eski sürüm-tasarım notları (arşiv): `docs/archive/`

> ⚠️ **Numara tutarsızlıkları (faithful):** Gerçek geçmişte sprint numaraları
> yer yer tekrar ediyor (ör. iki ayrı "Sprint 21.0", iki "21.1") ve bazı PR
> numaraları atlanmış/kapatılmış (#8, #23, #32, #36 merge edilmemiş). Aşağıdaki
> tablo geçmişi olduğu gibi yansıtır, düzeltmez.

---

## Faz 0 — Bootstrap & Çekirdek Pipeline · 2026-05-23
| Adım | Açıklama |
|---|---|
| Initial commit | Proje iskeleti |
| Çekirdek | schema lexicon, trace store, SQL validator |
| Patch 0–5.1 | TextNormalizer, tokenizer & generic token yönetimi, RAG skor eşiği + tracing, CandidateStore + sinyal toplama, hibrit synonym repository, mini golden query & regression testleri, test hijyeni |

## Sprint 2 — Graph Module Extraction · 2026-05-23 → 05-24
| PR | Açıklama |
|---|---|
| PR 2.1 | Graph modüllerinin ayrıştırılması (`schema_graph/`) |
| PR 2.2 / 2.2.1 | Token budget + hub detector; path mode, hub reasons, object traces |
| (refactor) | Candidate modelleri `schema_candidates.py`'ye; `GraphPruner` + subgraph seçimi; `SchemaPruner` delegasyonu |
| PR 2.4 | Sprint 2 Final Hardening |

## Sprint 3 — Trace & Debug API · 2026-05-24
| PR | Açıklama |
|---|---|
| PR 3.1 | Trace Model + InMemoryStore |
| PR 3.2 / 3.2.1 | `SQLiteTraceStore` + connection lifecycle sağlamlaştırma |
| PR 3.3 | Debug Trace API endpoint'leri (+ `verify_api_key`) |
| PR 3.4 | Trace capture entegrasyonu |
| PR 3.5 | SQL validation result capture |
| PR 3.6 | SQL guardrails |
| PR 3.7 | Debug trace API filtreleme + pagination |
| (test) | Sprint 3 trace/debug API sözleşmelerinin kilitlenmesi |

## Sprint 4 — Evaluation Harness · 2026-05-24 → 05-25
| PR | Açıklama |
|---|---|
| PR 4.1 / 4.1.1 | Eval harness iskeleti + mini golden dataset; Backend CI workflow |
| PR 4.2 / 4.2.1 | Golden dataset genişletme + SQL feature checks; test DB izolasyonu |
| (eval) | Suite result + stable JSON reporting, smoke/large profilleri, fake pipeline, CLI exit-code semantiği |
| #1–#3 | CI'da smoke eval + log netleştirme + Sprint 4 eval sözleşme kilidi |

## Sprint 5–6 — LLM Provider Seam · 2026-05-25
| PR | Açıklama |
|---|---|
| #4 | Provider interface sözleşmesi |
| #5 | SQL generation request/response sözleşmesi |
| #6 | Deterministik fake provider seam |
| #7 | Manuel NVIDIA provider adaptörü |

## Sprint 7–10 — SQL Dialect & Guardrails · 2026-05-25
| PR | Açıklama |
|---|---|
| #9 | Dialect validation sözleşmesi |
| #10–#11 | CTE read-only + comment injection guardrail sözleşmeleri |
| #12 | Tehlikeli komut fonksiyonlarının reddi |
| #13–#14 | Guardrail denylist regression; pipeline'da unsafe SQL reddi |
| #15–#16 | Guardrail hatasında SQL sanitize; fail-fast |

## Sprint 11 — API & Contract Freeze · 2026-05-25 → 05-27
| PR | Açıklama |
|---|---|
| #17–#22 | Job response shape, pipeline error envelope, job request validation, safe failure, API pipeline integration, error type taxonomy sözleşmeleri |
| #24–#27 | Trace failure/success consistency, attempt metadata debug, redaction/no-secret trace sözleşmeleri |
| #28–#41 | Eval sözleşmeleri: case schema, runner determinism, SQL equivalence normalization, report, release gate, CLI exit code, golden fixture/equivalence/report + CI gate |
| #42–#46 | API request/response schema, pagination, jobs filter/sort sözleşmeleri |

## Sprint 12 — Read-only SQL Execution · 2026-05-27
| PR | Açıklama |
|---|---|
| #47 | Read-only execution sandbox |
| #48 | Query timeout enforcement |
| #49 | Row limit guard |
| #50 | Result shape validation + execution error taxonomy |

## Sprint 13 — Graph Pruning Lock · 2026-06-01
| PR | Açıklama |
|---|---|
| #51 | Graph pruning mimarisinin kabul testleriyle kilitlenmesi |

## Sprint 14 — Trace Contract + Frontend Perf · 2026-06-01
| PR | Açıklama |
|---|---|
| #52 | Trace contract normalization |
| (frontend) | Büyük DB'lerde scroll/hover jitter, paint lag, occlusion culling, ReferenceError düzeltmeleri |
| #53–#55 | `TraceStore`/`InMemoryTraceStore`; `SQLiteTraceStore` kanonik sözleşmeye normalize; debug trace API'nin strict DTO + validation + redaction ile stabilize edilmesi |

## Sprint 15 — Golden NL2SQL Eval · 2026-06-03
| PR | Açıklama |
|---|---|
| #56 | Golden NL2SQL Eval Harness |
| #57 | Kolon-düzeyi eval kontrolleri |
| #58 | Eval failure reporting & check visibility |

## Sprint 16 — Runtime / Health / Docker / Release-Verify · 2026-06-03
| PR | Açıklama |
|---|---|
| #59 | `pydantic-settings` ile merkezî runtime ayarları |
| #60 | Güvenli health diagnostics endpoint |
| #61 | Startup validation warnings |
| #62–#63 | Backend Docker baseline + container runtime smoke testi |
| #64–#65 | Production runbook, ortam değişkeni sözleşmesi, deployment acceptance checklist |
| #67 | Release verification checklist |

## Sprint 17 — API Surface Freeze · 2026-06-03 → 06-04
| PR | Açıklama |
|---|---|
| #68 | API surface envanteri + OpenAPI snapshot |
| #69 | Error responses + response envelope şeması dondurma |
| #70 | API key / debug erişim sözleşmesi |
| #71 | HTTP status semantiği dondurma |
| #72 | Sözleşme yeniden üretim araçları + reviewer guardrail |

## Sprint 18 — Observability · 2026-06-04
| PR | Açıklama |
|---|---|
| #73 | Request logging |
| #74 | Request correlation |
| #75 | Performance telemetry |
| #76 | Readiness + startup diagnostics |

## Sprint 19 — Startup Config Gate · 2026-06-05
| PR | Açıklama |
|---|---|
| #77 | Startup configuration validation gate |

## 🚩 v1 Release Gate · 2026-06-05
| PR | Açıklama |
|---|---|
| #78 | Production release runbook + smoke gate |
| #79 | V1 release candidate gate |
| #80 | V1 RC verification evidence (`5ce60ae`) |
| #81 | V1 release readiness ilanı |

> ⚠️ `v1.0.0` git tag'i **atılmadı**; `docs/product-v1-rc-verification.md`
> içindeki metrikler doğrulanamıyor (bkz. CHANGELOG uyarısı).

## Sprint 20 — Schema Contract & Relations · 2026-06-05 → 06-10
| PR | Açıklama |
|---|---|
| #82 | Şema sözleşmesi (20.0) |
| #83 | Explicit FK sözleşmesi + cross-db tutarlılık (20.1) |
| #84 | Örtük ilişki tespiti sağlamlaştırma (20.2) |
| #85–#86 | Graph traversal (20.3) |
| #87 | Şema serileştirme (20.4) |

## Sprint 21 — Retrieval / Context Selection · 2026-06-10 → 06-13
| PR | Açıklama |
|---|---|
| #88–#90 | Şema context seçimi sağlamlaştırma + selector entegrasyonu + deterministik context golden gate |
| #91–#93 | SQL generation / execution result golden case'leri; e2e golden eval |
| #94 | Şema özetleme sözleşmesi |
| #95 | Embedding pipeline güvenilirliği |
| #96 | Top-k retrieval sözleşmesi |
| #97 | Context ranking |
| #98 | Token budget yöneticisi |

## Sprint 22 — Intent & Prompt Planning · 2026-06-13 → 06-14
| PR | Açıklama |
|---|---|
| #99 | Intent extraction sözleşmesi |
| #100 | Intent → context köprüsü |
| #101 | Prompt planning sözleşmesi |
| #102 | Prompt rendering sözleşmesi |
| #103 | SQL generation input assembly |
| #104 | SQL generation provider sınırı |

## Sprint 23 — Prompt Builder v2 · 2026-06-14
| PR | Açıklama |
|---|---|
| #105 | Prompt builder v2 |
| #106–#107 | Few-shot örnek seçimi (deterministik + dinamik) |
| #108 | Structured output üretimi |
| #109 | Self-check üretimi |

## Sprint 24 — Golden Dataset V2 & Execution Accuracy · 2026-06-15
| PR | Açıklama |
|---|---|
| #110 | Golden Dataset V2 sözleşmesi + loader |
| #111 | `SQLExecutionAccuracyHarness` + comparator |
| #112 | Failure analytics sözleşmesi |
| #113 | Eval gate aggregator |
| #114 | Regression dashboard sözleşmesi |

## Sprint 25 — Multi-DB / Connection / PostgreSQL · 2026-06-15 → 06-17
| PR | Açıklama |
|---|---|
| #115 | Çoklu veritabanı execution sözleşmesi |
| #116 | Connection abstraction katmanı |
| #117 | Connection-aware execution planner |
| #118 | Connection-aware execution orchestrator |
| #119 | Execution accuracy orchestrator entegrasyonu (25.4) |
| #120 | Execution outcome failure analytics entegrasyonu (25.5) |
| #121 | Execution trace / audit sözleşme entegrasyonu (25.6) |
| (25.7) | **PostgreSQL adapter contract STUB** + strict False capability testleri — ⚠️ `NOT_IMPLEMENTED`, gerçek execution yok |
| #125 | **PostgreSQL Read-Only Adapter — yalnızca local Docker** — 25.7 stub'ı, local Docker'a karşı gerçek read-only SELECT çalıştıran kontrollü adapter'a dönüştürüldü (SELECT-only/tek-statement gate, read-only tx + `statement_timeout`, `max_rows` truncation, credential-safe hatalar, lazy driver import). live/remote/production capability'leri hard-False; bağlantı wire edilmediğinden orchestrator default'u inert (`NOT_IMPLEMENTED`). CI'da `postgres:16` servisi. |
| #126 | **Oracle Adapter Contract STUB** — Oracle adapter'ın gerçek implementasyonu **yok**; sadece uyacağı sözleşme kilitlendi (`SQLOracleAdapterCapability` tüm execution flag'leri `False`, immutable contract, deterministic `NOT_IMPLEMENTED`/`REJECTED`, secret-safe result — sadece `sql_sha256`). cx_Oracle/oracledb/SQLAlchemy/JDBC/socket/DSN/TNS/wallet/env-secret **yasak** (testlerle kilitli). ⚠️ Gerçek Oracle execution Phase 10'da (29.3 adapter, 29.4 Docker/test harness). **Phase 6 (adapter stub'ları) burada kapanır.** |

## Phase 7 — Security & Governance · Sprint 26.x · 2026-06-18 → …
| PR | Açıklama |
|---|---|
| (26.0) | **SQL Permission Policy Contract** — backend'in ilk güvenlik/governance sözleşmesi. Bir SQL çalışmadan önce "izinli mi / deny mi / approval mı, neden?" sorusuna deterministik cevap veren contract. `SQLPermissionPolicyContract.evaluate()` → ALLOW/DENY/REQUIRES_APPROVAL + audit reason code (`default_deny`, `unknown_action`, `missing_subject`, …). **Fail-closed**: bilinmeyen action/resource, eksik subject/resource, konfigüre edilmemiş policy → hep DENY. Immutable request/result, secret-free result (context — connection string/password/token — sonuca sızmaz). Enforcement engine / tenant / RBAC / AuthN **yok** (sırasıyla 26.1–26.11). `backend/app/security/`. (#127) |
| (26.1) | **Tenant / Workspace Boundary Contract** — her güvenlik/policy kararının hangi tenant/workspace sınırında geçerli olduğunu temsil eden contract; cross-tenant leakage'a karşı izolasyon boyutu. `TenantWorkspaceBoundaryContract.validate()` → ALLOW/DENY + audit reason code (`boundary_match`, `missing_tenant`, `missing_workspace`, `tenant_mismatch`, `workspace_mismatch`; `unknown_boundary`/`invalid_boundary` ileriye dönük rezerve). **Fail-closed**: eksik tenant/workspace, tenant/workspace mismatch → hep DENY; tenant kontrolü workspace'ten önce. Immutable request/result, JSON-safe `to_dict()`; request'te `context` yok, deny'de tenant/workspace echo edilmez (secret-free). RBAC/AuthN/persistence/API/UI ve 26.0 composition **yok**. `backend/app/security/tenant_workspace_boundary.py`. (#128) |
| (26.2) | **Read-Only Enforcement Hardening** — "bu SQL gerçekten read-only mi?" sorusunu dağınık string check'lerden çıkarıp tek, deterministik, contract-first enforcement katmanına bağlar (güvenlik üçgeninin SQL ayağı). `SQLReadOnlyEnforcementContract.enforce()` → ALLOW/DENY + audit reason code (`read_only_select`, `empty_sql`, `multi_statement`, `non_select_statement`, `forbidden_keyword`, `unsafe_procedure`, `unsafe_data_movement`, `invalid_sql_type`). **Fail-closed**: tek-statement SELECT (veya read-only `WITH … SELECT`) dışında her şey DENY. Sınıflandırmadan önce SQL lex edilir (yorumlar sökülür, string literal + tırnaklı identifier maskelenir); sadece çalıştırılabilir kod taranır — `SELECT comment FROM t` / `SELECT * FROM "merge"` yanlış reddedilmez, ama gerçek konumdaki `;`/write verb (injection kuyruğu dâhil) yakalanır. `SELECT … INTO` ve CTE içi `INSERT/UPDATE/DELETE` her yerde; DDL/`COPY`/`MERGE`/procedure leading verb. Bilinen sınır: fonksiyonla yazma (`setval`/`lo_export`) string katmanda ALLOW; savunması DB read-only session. Result ham SQL taşımaz — yalnızca `sql_sha256` + leading keyword (`normalized_prefix`). **PostgreSQL ve Oracle** adapter'ları artık bu ortak gate'e delege eder (cross-adapter divergence kapalı). PostgreSQL adapter'ın `validate_read_only_select` gate'i artık bu ortak contract'a **delege** eder (forbidden keyword listesi tek yerde; mevcut rejection davranışı korunur, yalnızca sıkılaşır). Risk classifier / sensitive policy / PII / audit / approval, yeni adapter, gerçek execution, API/UI, tenant/RBAC/AuthN **yok**. `backend/app/security/sql_read_only_enforcement.py`. (#129) |
| (26.3) | **Query Risk Classifier** — bir SQL sorgusunun *statik* (çalıştırmadan) risk seviyesini deterministik sınıflandıran contract; **gate değil, sinyal üreticisidir** (allow/deny vermez). `SQLQueryRiskClassifier.classify()` → risk_level `LOW/MEDIUM/HIGH/CRITICAL` + tetiklenen signal listesi (`select_star`, `no_where_filter`, `no_row_limit`, `cartesian_join`, `high_join_count`, `side_effecting_function`, `unbounded_result`, `invalid_or_unparseable`). **Belirsizlikte yukarı yuvarlar**: boş/non-string/comment-only/non-read → CRITICAL. `risk_level` = tetiklenen sinyallerin max severity'si; hiç yoksa LOW. 26.2'nin `_sql_text` sanitize'ını paylaşır (literal/yorum içindeki keyword/fonksiyon sinyal tetiklemez). 26.2'nin bilinen sınırını (`SELECT setval(...)`/`lo_export`/`pg_terminate_backend` gibi fonksiyonla yazma) **CRITICAL** olarak görünür kılar (engellemez). Result ham SQL taşımaz — `sql_sha256` + leading keyword. Refactor: 26.2/26.3 ortak `app/security/_sql_text.py` helper'ına bağlandı. Sensitive/PII policy (26.4+), audit, approval, gerçek parser/EXPLAIN, execution, API/UI, RBAC/AuthN **yok**. `backend/app/security/sql_query_risk_classifier.py`. *(PR pending)* |
| (26.4) | **Sensitive Table / Column Policy** — beyan-tabanlı (declaration-driven) hassas tablo/kolon gate'i; **gate** kararı + sinyal. `evaluate()` → ALLOW/DENY/REQUIRES_APPROVAL + eşleşen (matched) tablo/kolon ve hassasiyet seviyesi sinyali. **Hibrit referans**: explicit (beyan edilen) eşleşme = sound; SQL'den tablo/kolon çıkarımı = best-effort. Immutable request/result, secret-free. `backend/app/security/sql_sensitive_data_policy.py`. *(local squash-merge)* |
| (26.5) | **PII / PHI Detection Contract** — heuristik PII/PHI **detector** (sinyal, gate değil): kategori + PII/PHI veri-sınıfı + güven (confidence). **3 katman**: declared = sound; identifier-isim + literal-değer/Luhn = best-effort. Secret-free sonuç (ham değer sızmaz). `backend/app/security/sql_pii_phi_detection.py`. *(local squash-merge)* |
| (26.6) | **Audit Event Contract** — deterministik, secret-free audit kaydı + tamper-evident SHA-256 hash-chain. 26.0–26.5 sonuçlarını tek immutable `AuditEvent`'e normalize eden 6 builder (OWASP-style kategoriler, NIST SP 800-92 6-soru çekirdeği); zaman/ID caller-supplied. `verify_chain` mutation/reorder/insert/delete yakalar (integrity-evident; non-repudiation Phase 13). `backend/app/security/audit_event.py`. *(local squash-merge)* |
| (26.7) | **Approval Workflow Contract** — saf deterministik approval state machine (I/O & clock yok, time/id caller-supplied, frozen dataclass, secret-free). PENDING + terminal (APPROVED/REJECTED/EXPIRED/CANCELLED); fail-closed `from_permission` (N=1) / `from_sensitive` (CONFIDENTIAL→1, RESTRICTED→2); approve/reject quorum N-of-M + SoD (requester≠approver) + duplicate-vote guard + single-reject veto + terminal immutability; caller-driven expire; audit interlock (`AuditCategory.APPROVAL` + `from_approval`). `backend/app/security/approval_workflow.py`. *(local squash-merge)* |
| (26.8) | **Prompt-Injection / NL Abuse Defense** — Phase 7'nin **doğal dili** (SQL değil) inceleyen ilk contract'ı; kaynak-duyarlı (source-aware) detector (sinyal + advisory disposition, gate değil; secret-free, no-I/O, stdlib-only, frozen). 6 `InjectionCategory` (INSTRUCTION_OVERRIDE, SYSTEM_PROMPT_EXFIL, ROLE_HIJACK, DELIMITER_BREAKOUT, OBFUSCATION_EVASION, SQL_ABUSE_INTENT) + DIRECT/INDIRECT kaynak + confidence LOW<MEDIUM<HIGH + disposition ALLOW<REVIEW<BLOCK; per-segment tespit; NFKC normalize + **Türkçe-güvenli** obfuscation sinyali (homoglyph yalnız Cyrillic/Greek code point'leri). `evaluate`/`_aggregate`: any HIGH veya any INDIRECT≥MEDIUM → BLOCK; herhangi eşleşme → REVIEW; aksi ALLOW. Audit interlock (`AuditCategory.PROMPT_INJECTION` + `from_prompt_injection`). OWASP LLM01:2025 temelli. `backend/app/security/prompt_injection_defense.py`. *(local squash-merge → main `c733be4`)* |
| (26.9) | **Result-Set Privacy & Row/Size Limits** — Phase 7'nin **post-execution** sonuç kümesi (gerçek dönen satır/kolon) üzerinde çalışan ilk contract'ı; hibrit: limit **GATE** (ALLOW/TRUNCATE/DENY — caller policy `max_rows`/`max_bytes`/`max_columns` + `truncate_allowed`) + advisory privacy **SIGNAL** (26.5 value-scan'i tekrar kullanan kolon-seviyesi PII/PHI özeti; secret-free). Audit interlock (`AuditCategory.RESULT_SET_PRIVACY` + `from_result_set`) + public exports. Paylaşılan tarayıcı `_pii_value_scan.py`'ye çıkarıldı. PHI yolu yapısal olarak erişilemez (yalnız PII kategorileri üretilir) → bilinçli olarak inert iskele, spec §11/§12 + docstring'de belgeli. `backend/app/security/result_set_privacy_limits.py`. *(local squash-merge → main `6886288`)* |
| (26.10) | **Connection Credential Vault** — saf, deterministik, **secret-free**, I/O-free governance contract: bir call context'in bir connection `secret_ref`'i **resolve** edip edemeyeceğine karar veren gate + reference-only alanlara ham-secret sızıntısını işaretleyen advisory sinyal. Ham parola/connection-string/token'ı asla tutmaz/saklamaz/çözmez — yalnız *referansı* ve *kararı* yönetir. Gate = 6 erişilebilir boyut, deterministik öncelik (tek reason_code kazanır): leak → auth_mode → provider allowlist → environment → tenant → caller purpose → ALLOW. 7 reason code. `app.evaluation.connection_abstraction` profil tiplerini girdi olarak tekrar kullanır; leak taraması marker adı döner (asla değer). Audit interlock (`AuditCategory.CONNECTION_CREDENTIAL` + `from_credential_vault`; ALLOW→ALLOWED/INFO, DENY→DENIED + HIGH-if-leak-else-MEDIUM). Public API PEP 562 lazy `__getattr__` ile export edilir → driver-isolation invariant'ı korunur (`import app.security` driver yüklemez). `backend/app/security/connection_credential_vault.py` + `_secret_pattern_scan.py`. *(local squash-merge → main `f9a815c`)* |
| (26.11) | **Policy / Security Eval** — Phase 7'nin **son** sprint'i; saf, deterministik, **secret-free**, I/O-free **meta-evaluation harness** 26.0–26.10 güvenlik contract'ları üzerinde. `backend/app/evaluation/`'da yaşar → `app.security` driver-free kalır (driver-isolation invariant'ı doğrulandı: `import app.security` `urllib` yüklemez). Bileşenler: normalize `SecurityEvalOutcome(verdict, codes)` + exact-match `run_security_eval` runner; 11 per-contract **projector** + `PROJECTOR_REGISTRY` (**enum-purity invariant**: yalnız enum `.value` üretir); 22 in-code typed **blessed golden case** (1-2/contract, her biri gerçek contract'ı çağırır); enum-introspection **coverage** (`COVERAGE_UNIVERSE` − 8 declared exclusion → gaps); PASS/WARN/FAIL **gate aggregator** (`SQLEvalGate` şeklini aynalar; case mismatch→FAIL, coverage gap→WARN by default); entegrasyon meta testi (baseline green + `covered ⊆ universe`). Bilinçli kapsam-dışı: audit interlock yok, JSON/YAML loader yok. SDD ile inşa edildi (6 görev, her biri TDD + task review; opus whole-branch review = merge-ready, 0 Critical/Important). Full suite 1965 passed/9 skipped. `backend/app/evaluation/security_policy_eval{,_projectors,_cases,_coverage,_gate}.py`. *(local squash-merge → main `69d86c3`)* |

## Phase 8 — Observability & Debuggability · Sprint 27.x · 2026-06-30 → …
| PR | Açıklama |
|---|---|
| (27.0) | **End-to-End Trace Contract** — Phase 8'in **ilk** sprint'i; saf, deterministik, **secret-free**, I/O-free `EndToEndTrace` sözleşmesi: SQLGen pipeline'ın per-stage çıktılarını (intent→retrieval→prompt→generation→validation→security→execution) tek `request_id` altında ilişkilendirir. **Hibrit model**: typed envelope `EndToEndTrace` + ordered `TraceSpan` tuple (stage başına typed `detail` + generic `attributes` str→str escape hatch). Bileşenler: enums (`TraceStageKind`/`TraceSpanStatus`/`TraceTerminalStatus`) + frozen records (`TraceSpanDetail`/`ExecutionSpanDetail`/`SecurityCheck`/`SecuritySpanDetail`/`TraceSpan`/`EndToEndTrace`) + cross-field `__post_init__` invariant'lar (version eşitliği, non-empty id, duplicate-stage red, COMPLETED⇒no-ERROR, non-COMPLETED⇒terminal_stage span ERROR) + JSON-safe `to_payload`; `derive_terminal` precedence (execution blocked/rejected > ilk ERROR span > COMPLETED) + `build_end_to_end_trace` assembler; **duck-typed** `build_execution_span` + `build_security_span` (attrs off `Any` via `getattr`; empty/`None`→SKIPPED) → yalnız `app.trace.*` import eder, **driver-isolation** korunur (`import app.trace.end_to_end_trace` hiçbir `app.evaluation`/DB driver yüklemez; testle kilitli). **Secret-free**: yalnız `sql_sha256` (asla ham SQL) + reason/symbol string + `redact_sensitive_text`'ten geçmiş error mesajı payload'a ulaşır. Bilinçli kapsam-dışı (27.1): kalan stage builder'ları, live pipeline wiring, persistence. SDD ile inşa edildi (5 görev + 2 pre-merge minor fix `audit_events or ()` None-guard & `derive_terminal` annotation; her görev TDD + sonnet task review; opus whole-branch review = merge-ready, 0 Critical/Important). Full suite 1998 passed/9 skipped. `backend/app/trace/end_to_end_trace{,_builders}.py`. *(local squash-merge → main `48ebad6`)* |
| (27.1) | **Remaining Stage Span Builders** — 27.0 `EndToEndTrace` çatısının builder setini **tamamlar**: kalan 5 stage'in (INTENT/RETRIEVAL/PROMPT/GENERATION/VALIDATION) saf, **duck-typed**, secret-free span builder'ları + typed `*SpanDetail` frozen record'ları (`IntentSpanDetail`/`RetrievalSpanDetail`+`RetrievalCandidateRef`/`PromptSpanDetail`/`GenerationSpanDetail`/`ValidationSpanDetail`). Builder'lar attr'ları `Any` üzerinden `getattr` ile okur; empty/`None` girdi → SKIPPED span; `derive_terminal`/`build_end_to_end_trace`/`to_payload` **değişmedi**. **Driver-isolation** korunur ve full-set assembly + izolasyon testleriyle kilitlidir (`app.trace.*` importu hiçbir `app.evaluation`/DB driver yüklemez). **Secret-free**: ham SQL/prompt/secret yok — yalnız hash/sayaç/symbol/reason string'leri. Canlı pipeline telçekimi (request_id plumbing, per-stage `perf_counter`, `_capture_trace_on_exit` emission, modüler stage routing) bilinçli olarak **27.1w Live Trace Wiring**'e ertelendi (sessiz düşürme yok; PLANNED-SPRINTS'te ⏳→▶️). SDD ile inşa edildi (6 görev, her biri TDD + sonnet task review; opus whole-branch review = merge-ready, 0 Critical/0 Important, 3 Minor → 27.1w). Full suite 2024 passed/9 skipped. `backend/app/trace/end_to_end_trace{,_builders}.py`. *(local squash-merge → main `650caf7`)* |
| #134 | **Ara fix (sprint dışı) — Şema ilişki graph'ı görünürlük düzeltmesi** (2026-07-02) — `SchemaManager.vue` D3 graph'ı top-N hub seçimini *izole tablolar dahil tüm tablolar* üzerinden yapıp yalnızca iki ucu da seçimde kalan edge'leri çizdiğinden, varsayılan `maxNodesLimit=5` ile örnek şemada (2175 tablo/93 FK) ilişkilerin yalnızca **3/93**'ü görünüyordu; "limitsiz" ise 2073'ü izole 2175 node render edip tarayıcıyı donduruyordu (TECH-DEBT #2'nin kökeni). Seçim mantığı saf `frontend/src/utils/graphSelection.ts` modülüne çıkarıldı: **izole tablolar hiç çizilmez**, limit yalnızca bağlantılı tablolar arasında uygulanır, varsayılan `0 = tüm bağlantılı` (örnek şemada 102 tablo + **93/93** ilişki), edge'ler `d3.forceLink` mutasyonuna karşı kopyalanır; panele render istatistiği rozeti eklendi. TDD: `frontend/tests/graphSelection.test.ts` (8 test, `node --experimental-strip-types`; frontend'de test framework yok). Kalan borç (çok büyük *bağlantılı* graph'lar → WebGL/virtualized, Phase 12 adayı) TECH-DEBT #2'de. *(GitHub PR squash-merge → main `8211790`)* |
| #135 | **(27.1w) Live Trace Wiring** — 27.0/27.1'in saf `EndToEndTrace` sözleşmesini **canlı pipeline'a örer** (27.1'den bilinçli ertelenmişti; sessiz düşürme yok). **`request_id` plumbing**: HTTP `X-Request-ID` → `upload_excel_file`/`start_job_without_file` → `process_job_pipeline` → `run_pipeline`; yoksa `req_` önekiyle mint edilir (`RequestLoggingMiddleware` zaten `request.state.request_id` set ediyor). **Per-stage timing**: her stage `perf_counter` ile ölçülür; writer-critic'te per-phase (prompt/generation/validation/security) `try/finally` ile — `continue`/`break` dallarında da birikir. **Dual-emit**: `_capture_trace_on_exit`'te legacy `NL2SQLTrace`'in **yanına** `TraceRecord(trace_type="end_to_end")` emit edilir (7 sabit span; koşmayan stage SKIPPED; EXECUTION Faz-1'de hep SKIPPED). **Stage refactor**: `run_pipeline` → `_stage_intent`/`_stage_retrieval`/`_stage_writer_critic`; **davranış birebir korundu** — kanıt: branch genelinde tüm test dosyaları `+N/-0`, hiçbir mevcut assert değiştirilmedi/silinmedi; `pruned_schema_tables` invariant'ı (pruning_error'da boş, context_selection_exception'da dolu) 8 commit sonunda da holds. Yeni **saf** modül `backend/app/trace/live_trace_assembly.py` (yalnız `app.trace.*` + stdlib; purity artık kendi guard testine sahip). 27.1'den devreden **3 Minor kapatıldı**. **Invariant'lar**: emisyon hatası pipeline sonucunu asla etkilemez (assembly + `TraceRecord.__post_init__`'in serialization raise'i + save tek `try/except Exception` içinde; legacy save bu bloktan önce ve koşulsuz); ham SQL payload'a girmez (yalnız sha256). **Bilinen sınır (kasıtlı, kullanıcı onaylı, CHANGELOG'da yazılı)**: dual-emit yalnız `save_legacy` expose eden store'larda çalışır (`_save_trace_safely`'nin mevcut feature-detect deseniyle tutarlı) — prod `SQLiteTraceStore` tam emit alır; eski tekil-`save()` store'lar yalnız legacy görür. SDD ile inşa edildi (8 görev, her biri TDD + sonnet task review). **Opus whole-branch review "merge after fixes" verdi ve task review'ların yapısal olarak göremediği gerçek bir bug yakaladı**: intent/security timing'leri her run'da ölçülüp sessizce düşürülüyordu — kök neden planın kendi içinde çelişmesiydi (düzyazı kuralı satır 187 "her builder'a duration_ms verilir" derken planın kendi referans kodu INTENT/SECURITY'ye geçmiyordu; implementer kodu izlemişti). Task 8 ile düzeltildi (+ purity guard testi + `TraceRecord.trace_id` artık payload id'siyle aynı). Full suite 2055 passed/9 skipped. Kalan borç: **TECH-DEBT §3** (SKIPPED span'lerde `duration_ms` düşüyor → canlı yolda INTENT süresi yalnız hata dalında iniyor; "koşmamış stage süre taşımalı mı?" semantik kararı bekliyor). `backend/app/{sql_pipeline,main}.py`, `backend/app/trace/{live_trace_assembly,end_to_end_trace_builders}.py`. *(GitHub PR rebase-merge → main `466d9ee`)* |
| #136 | **(27.2) Error Taxonomy v2** — hata taksonomisini tek registry'de topladı: saf `backend/app/errors/` yaprak paketi (`StrEnum` `ErrorCode`/`ErrorCategory` + `DESCRIPTORS`/`describe`/`category_of`; stdlib-dışı importsuz, purity testli). Kategori (hatanın *doğası*, koda ait sabit) ile stage (nerede *yakalandığı*, çalışma-zamanı verisi) eksenlerini ayırdı (v1'in "semantic_validation hem stage hem type" kusurunun düzeltmesi); `run_pipeline` sonucuna `error_code` ekledi ama **bilinçli olarak pipeline katmanında durdu** — iş/API/frontend sınır geçişi 27.2.1'e ertelendi (sessiz düşürme yok). SDD ile inşa edildi; opus whole-branch review. *(GitHub PR rebase-merge → main `fff5691`)* |
| #138 | **(27.2.1) Error Taxonomy Boundary** — 27.2'nin `error_code`'unu iş/API/frontend sınırından geçirdi: `jobs.error_code` kolonu (idempotent `ALTER` migration) + `update_job_status` **dinamik-SET refactor** (sabit whitelist'ten kurulan SET cümlesi → injection-safe; mevcut testler değişmeden geçti = davranış korundu), `JobDetailResponse.error_code`, frontend `Job.error_code` + saf `extractApiErrorMessage` helper (RAG/indexing endpoint'leri artık backend'in gerçek hata mesajını gösteriyor — `errData.detail` bug'ı düzeltildi). `ExecutionSpanDetail.error_code` beslemesi **bilinçli ertelendi** (canlı execution fazına bağımlı; SKIPPED span'e kod iliştirmek anlamsız — TECH-DEBT §4.3). Contract snapshot'ları yenilendi (yalnız `JobDetailResponse` genişledi; `required` listeleri değişmedi). SDD (5 görev, her biri TDD + task review); opus whole-branch review = merge-ready (3 Important = hepsi doküman, tek commit ile giderildi). *(GitHub PR rebase-merge → main `c1e3335`)* |
| #139 | **(27.3) User Feedback Capture** — bir SQL üretimine (`job`) dair kullanıcı feedback'ini toplayan backend yüzeyi. Saf taksonomi katmanı `backend/app/feedback/categories.py`: `FeedbackVerdict` (`correct`/`incorrect`) + `FeedbackCategory` (`wrong_table`/`wrong_column`/`wrong_filter`/`wrong_join`/`wrong_aggregation`/`wrong_order_limit`/`other`) — 27.2'nin sistem/pipeline hata taksonomisinden ayrı bir eksen (SQL başarıyla üretilmiş ama semantik olarak yanlış olabilir). Append-only `feedback` tablosu (`id`/`job_id`/`verdict`/`category`/`note`/`corrected_sql`/`created_at`, `job_id` üzerinde index) + repo fonksiyonları `create_feedback`/`get_feedback`/`get_feedback_for_job` (`backend/app/database.py`; `get_feedback_for_job` `created_at ASC, rowid ASC` ile deterministik sıralar, bilinmeyen `job_id` → boş liste). `POST /api/jobs/{job_id}/feedback` endpoint'i (`backend/app/api/feedback.py` + `schemas.py` invariant'ları) — router enum'ları `.value` olarak DB'ye geçirir, DB string saklar, response `str` alanları taşır (enum sınırı geçmez). Contract snapshot'ları yenilendi. Öğrenen sistemin tohumu: `27.9` (Feedback Review → Rule Suggestion) `get_feedback_for_job` ile tüketecek. **Bilinçli olarak backend-only**: HTTP GET listeleme/admin görünümü (`30.6`), frontend UI (`31.7`) ve `rating` alanı bu sprintte kapsam dışı. SDD ile inşa edildi (4 görev — taksonomi/persistence/API/contract-snapshot —, her biri TDD + task review; Task 5 yaşayan doküman kapanışı). *(GitHub PR rebase-merge → main `6142797`)* |
| #141 | **(27.4) Query Replay System** — gecmis bir `job`'i bugunun kodu ve semasiyla **deterministik** olarak yeniden kosturup (LLM'e gidilmez) o gunku `end_to_end` trace baseline'iyla karsilastiran, **yan etkisiz** bir replay yuzeyi. Saf yaprak paket `backend/app/replay/`: `ReplayVerdict` (`input_unavailable`/`baseline_unavailable`/`replay_failed`/`security_regression`/`validation_regression`/`retrieval_drift`/`validation_recovery`/`security_recovery`/`identical`, oncelik sirali) + frozen `RetrievalDelta`/`ValidationDelta`/`SecurityDelta`/`ReplayResult` + saf `extract_baseline` (end_to_end payload dict'i uzerinde, nesne kurmadan) ve `compare_replay`. Tum tuple ciktilari sirali → ayni girdi ayni payload. **Seam refactor:** dogrulama zinciri (guardrail → sandbox safety → sqlglot AST → semantik → pretty-print) LLM retry dongusunun govdesinden `SQLGenerationPipeline.validate_sql`'e ayiklandi; replay artik uretimin **ayni** kodunu kosuyor (kopya mantik yalan soylerdi). Davranis birebir korundu — `retry_disposition` alani donguydeki `continue`/`break` ayrimini **veri olarak** tasir, `log_callback` seam'e gecirilerek SSE mesajlari korunur, `pretty_printed` bayragi `last_generated_sql`'in yalniz pretty-print basarili oldugunda guncellenmesini korur; kanit: mevcut test dosyalari `+N/-0`. Kirli adaptor `app/replay_service.py` job satirini + en yeni `end_to_end` trace'i okur, uretimin `_stage_intent`/`_stage_retrieval`/`validate_sql`'ini kosturur, saf karsilastiriciyi cagirir. `POST /api/debug/jobs/{job_id}/replay` (API key + `debug_endpoints_enabled` ile gated, 200 envelope; job yoksa 404). **Bilincli kapsam disi:** LLM'li tam re-run, replay sonuclarinin kaliciligi, toplu replay (27.10/28.x), frontend UI (31.x), 27.1w oncesi joblar icin legacy `nl2sql_traces` fallback'i (acikca `baseline_unavailable` doner). *(GitHub PR rebase-merge -> main `d57d86e`)* |
| #143 | **Debug Bundle Export** — bir `job`'in tum debug baglamini (o gunku `end_to_end` trace + REDAKTE SQL/attempts/validation_errors + 27.4 replay'i INLINE kosturarak + secilen sema ozeti + surum metadata) tek bir JSON envelope'da toplayan, hata raporuna eklenebilir, **yan etkisiz** export. Saf yaprak paket `backend/app/debug_bundle/` (allow-list projektorler + saf `compose_bundle` + frozen record'lar; `app.trace.*` import etmez — redaksiyon kirli adaptorde kosar) + kirli adaptor `app/bundle_service.py` (iki trace turunu okur + 27.4'un replay'ini inline cagirir) + `GET /api/debug/jobs/{job_id}/bundle` (API key + `debug_endpoints_enabled` ile gated, 200 envelope). **Iki trace ayrimi bilincli:** `end_to_end` trace secret-free (hash-only) → envelope'un `trace`+`schema` bolumlerini besler; debug trace (redakte SQL tasiyan) → envelope'un `sql` bolumunu besler; ikisi karistirilmaz. Sema+meta+job allow-list ile projekte edilir; SQL/attempts/validation_errors mevcut redaksiyon gecislerinden gecer. **Bilincli kapsam disi:** ZIP/Markdown export formati, bundle kaliciligi (persistence), toplu export, tam DDL snapshot (yalnizca secili tablo adlari + `schema_hash`), frontend UI (Phase 12). *(PR #143)* |
| #144 | **(27.6) Metrics Contract** — bir zaman penceresindeki `end_to_end` trace'leri toplayan **versiyonlu** metrik raporu. Dort bolum: hacim&sonuc (`terminal_status` dagilimi + `success_rate=completed/total`), hata taksonomisi (`by_code` + `by_category`; bilinmeyen kod → `unknown` kovasi; 27.2 registry'sine dayanir), latency (**nearest-rank** `p50`/`p95`/`p99`; `None` degerler atlanir), asama kirilimi (stage → span status sayaclari). Saf yaprak paket `backend/app/metrics/` (`app.errors` izinli istisna; frozen record'lar + saf `compute_metrics`; `app.trace.*` import etmez — purity guard'li) + kirli adaptor `app/metrics_service.py` (RAW trace store'dan bounded fetch + `truncated` bayragi) + `GET /api/debug/metrics` (API key + `debug_endpoints_enabled` ile gated, 200 envelope). **Metrikler hassas DEGIL** → 27.5'in aksine redaksiyon uygulanmaz. Bos pencere → 200 + sifirlanmis alanlar (404 degil). **Bilincli kapsam disi:** cost/LLM usage telemetrisi (27.8), zaman-serisi/bucketing (27.7), metrik kaliciligi, `scan_cap`'in query parametresinden ayarlanabilir olmasi, frontend UI (Phase 12). *(PR #144)* |
| #145 | **(27.7) Admin Observability Dashboard Backend** — bir zaman penceresi icin 27.6 metrik snapshot'ini **zaman-serisi bucketing** (hour/day, floor'lanmis damga, nearest-rank p95, en-yeni-N cap + `timeseries_truncated`) + **top hatalar** (27.6 aggregate'inden count DESC/kod ASC, bilinmeyen→`unknown`) + **feedback ozeti** (verdict/category sayaci, bos kategori atlanir) + **son aktivite** ile tek kompozit JSON'da birlestiren, **yan etkisiz** `GET /api/debug/dashboard`. Uc katman: saf yaprak paket `backend/app/dashboard/` (`contract.py` frozen record'lar + SIRALI `to_payload` + `compose.py` saf builder'lar & `compose_dashboard`) — **27.6 `compute_metrics`'i reuse eder**, span'leri yeniden okumaz (27.6'nin per-stage kod-okuma tuzagi yapisal olarak tekrarlanamaz); purity yalniz stdlib + `app.errors` + `app.metrics`, `app.trace.*` import etmez (taze-alt-surec guard'li). Kirli adaptor `app/dashboard_service.py` **tek** `TraceQuery(trace_type=end_to_end, limit=scan_cap+1)` fetch'i metrics+timeseries+recent'i besler + `database.list_feedback` (yeni, pencere-filtreli, created_at DESC/rowid DESC). Router `app/api/dashboard_api.py` (API key + `debug_endpoints_enabled` ile gated, 200 envelope `{"status":"success","dashboard":{...}}`; `bucket` Literal → gecersizde 422; `top_n`/`recent_limit` 1..100). **Determinist:** `datetime.now()` ASLA cagrilmaz — yalnizca var olan `created_at` floor'lanir; ayni girdi ayni payload. **Sessiz kesme yok:** trace `truncated` + bucket `timeseries_truncated`. **Bos pencere → 200** bos bolumlerle (404 degil). **Dashboard verisi hassas DEGIL** → redaksiyon yok; RAW store + feedback DB okunur. Store/DB hatasi YUTULMAZ (propagate). Opus whole-branch review = **SHIP** (0 Critical, 0 bloke; 1 Important cross-section pencere tutarliligi + 4 Minor → TECH-DEBT §8). Full suite 2323 passed/9 skipped. **Bilincli kapsam disi:** cost/LLM usage (27.8), feedback→rule (27.9), zaman-serisi zero-fill, DB-side tam-populasyon aggregation, frontend UI (Phase 12). *(GitHub PR #145)* |
| #146 | **(27.8) Cost & LLM Usage Telemetry** — bir zaman penceresindeki `end_to_end` trace'lerin **GENERATION span**'lerinden LLM kullanimini (request sayisi, prompt/completion/total token, latency, finish_reason) toplayan ve **konfigure edilebilir fiyat tablosuyla** tahmini maliyet ureten, **yan etkisiz** `GET /api/debug/llm-usage`. Yeni pipeline enstrumantasyonu YOK — veri 27.1w'den beri GENERATION span payload'inin `detail`'inde (`provider_id/model_id/finish_reason/prompt_tokens/completion_tokens/total_tokens` + span `duration_ms`) zaten yakaliniyor; 27.8 yalnizca aggregation + fiyatlandirma. Uc katman: saf yaprak paket `backend/app/llm_usage/` (`contract.py` frozen record'lar + SIRALI `to_payload`; `pricing.py` toleransli `parse_price_table`+`cost_for`; `compute.py` saf section builder'lar + `compute_llm_usage`) — purity **yalniz stdlib** (27.6/27.7'den katiligi; `app.errors` bile yok, `app.trace.*` asla; taze-alt-surec guard'li). Kirli adaptor `app/llm_usage_service.py` **tek** `TraceQuery(trace_type=end_to_end, limit=scan_cap+1)` fetch'i totals+breakdowns+latency+finish_reason+timeseries'i besler + fiyat tablosunu `get_config('llm_pricing')` JSON'undan okur (gecersiz → bos tablo, cost null). Router `app/api/llm_usage_api.py` (API key + `debug_endpoints_enabled` ile gated, 200 envelope `{"status":"success","usage":{...}}`; `bucket` Literal → 422). **Maliyet:** model basi `token × per-1M fiyat`; fiyatsiz model → cost `null` + `models_missing_price` + `unpriced_request_count` (sessiz bosluk yok). **Determinist** (`datetime.now()` ASLA; nearest-rank; sirali sozlukler). **Sessiz kesme yok** (`truncated` + `timeseries_truncated`). **Bos pencere → 200**. Kullanim/maliyet hassas DEGIL → redaksiyon yok. Store/config hatasi YUTULMAZ. Opus whole-branch review = **SHIP** (0 Critical/0 bloke; 27.6 span-sekli tuzagi YOK — extraction gercek ureticiyle eslesir; 1 Minor → TECH-DEBT §9). Full suite 2354 passed/9 skipped. **Bilincli kapsam disi:** retry token per-attempt yakalama (trace tek GENERATION span; TECH-DEBT §9), `model=`/`provider=` query filtresi, dashboard entegrasyonu, zaman-serisi zero-fill, persistence, gercek NIM faturalandirmasi, frontend UI (Phase 12). *(GitHub PR #146)* |
| #147 | **(27.9) Feedback Review → Rule Suggestion** (2026-08-03) — kullanici feedback'ini (27.3 `feedback` tablosu + 27.7 `list_feedback`) aday `{natural_query → SQL}` kural/örnek önerisine çeviren, **yan etkisiz** `GET /api/debug/rule-suggestions`. **Iki kind:** correction (`verdict=incorrect` + dolu `corrected_sql`) / confirmation (`verdict=correct` + dolu job `result_sql`); ineligible sebep önceliği `missing_job > missing_natural_query > missing_sql`. Üç katman: saf yaprak paket `backend/app/rule_suggestions/` (`contract.py` frozen record'lar — `SuggestionWindow`/`RuleSuggestion`/`IneligibleSummary`/`RuleSuggestionsReport` — + SIRALI `to_payload`; `compute.py` saf `classify_item` + `compute_rule_suggestions`: dedup anahtarı `(natural_query, suggested_sql, kind)`, support-count DESC ranking, `by_kind`/`by_category` kırılımları) — purity **yalnız stdlib** (27.8 katılığını miras alır; `app.feedback`/`app.trace.*` asla import edilmez; taze-alt-süreç purity guard'lı). Kirli adaptör `app/rule_suggestions_service.py` (`list_feedback` pencere-filtreli fetch + `get_job` job-cache korelasyonu; `scan_cap=10000`; DB hatası yutulmaz). Router `app/api/rule_suggestions_api.py` (API key + `debug_endpoints_enabled` ile gated, 200 envelope `{"status":"success","suggestions":{...}}`; trace store'a dokunmaz — feedback+jobs DB okur). **Bilinçli olarak yalnızca ÖNERİR:** insan `POST /api/rag/index/sql-history` ile ayrıca onaylar/indeksler; redaksiyon **YOK** (amaç ham SQL'i onaya sunmak, tıpkı 27.3'ün `corrected_sql`'i gibi). Full suite 2378 passed/9 skipped. **Bilinçli kapsam dışı:** onay/persistence-state (onaylanan öneri idempotent düşmez, tekrar görünür), LLM/semantik genelleme + synonym türetme (yalnız literal eşleşme), `scan_cap` üstü DB-side tam-populasyon aggregation, confirmation `result_sql`'in okuma-anındaki job durumunu yansıtması, frontend UI (Phase 12). *(GitHub PR #147)* |
| #148 | **(27.10) Per-Release Accuracy Regression Gate** (2026-08-03) — mevcut deterministik golden eval raporunu (`app.eval.run_eval --profile golden`) tüketen, **yan etkisiz** CLI/CI regresyon gate'i — debug endpoint **değil**. Saf yaprak paket `evals/regression_gate.py` (`compare_regression` + baseline helper'ları; per-case **sıfır-tolerans** birincil + aggregate ikincil karşılaştırma, `baseline ∩ current` kesişimi üzerinde; reason önceliği per-case > aggregate > bootstrap; drift bilgi amaçlıdır, regresyon sayılmaz) + kirli CLI `evals/regression_gate_cli.py` (gate çalıştırma + `--update-baseline`, exit `0`/`1`/`2`) + seeded append-only `evals/baselines/history.json` (`v1.0.0`) + `backend-ci.yml`'e yeni regresyon-gate adımı. Boş/yok baseline → **bootstrap yeşil** (exit 0; ilk sürüm için regresyon karşılaştıracak bir şey yok). **Bilinçli kapsam dışı:** gerçek-LLM accuracy (deterministik sahte pipeline üzerinde çalışır → kod-kaynaklı regresyonu yakalar, model doğruluğunu değil; subsystem C execution harness'ı ayrı kalem), debug endpoint (yalnız CLI/CI yüzeyi), sürüm etiketinin git-tag'den runtime'da otomatik okunması (`--version` operatör tarafından elle verilir), CI'da baseline'ın otomatik güncellenmesi (`--update-baseline` release'te elle çağrılmalı), frontend UI. Full suite 2400 passed/9 skipped. *(PR #148)* |
| #149 | **(27.11) Tech-Debt Cleanup** (2026-08-03) — faz ilerletmeyen bakım sprinti; davranış korunur, yalnız test-bütünlüğü/invariant/sağlamlık iyileşir (contract snapshot `git diff` boş). 7 kalem ÇÖZÜLDÜ: **§5.1** `backend/tests/errors/test_errors_package_purity.py` + `backend/tests/feedback/test_feedback_package_purity.py` taze-alt-sürece (`subprocess`) taşındı — `rule_suggestions` guard'ıyla aynı desen (27.4'ün whole-branch review bulgusu: aynı-süreç guard sızıntıyı maskeler). **§5.2** `backend/tests/test_api_surface.py`'deki `expected_routes` artık OpenAPI snapshot'ından türetilir — tek kaynak snapshot, sessiz drift riski kapandı. **§3** `backend/app/trace/end_to_end_trace_builders.py`'deki 5 builder (intent/retrieval/prompt/generation/validation) artık SKIPPED dalında da ölçülen `duration_ms`'i taşıyor (option a, SECURITY precedent'i). **§9.3** `backend/app/llm_usage/compute.py`'de null-model generation `models_missing_price`'a `"unknown"` olarak uzlaşır. **§8.2** `backend/app/dashboard_service.py`/`dashboard/contract.py`'ye `feedback_truncated` bayrağı eklendi. **§8.4(b)** `backend/app/dashboard/compose.py::bucket_timeseries` non-datetime `created_at`'i atlar. **§1** `backend/app/retrieval/nvidia_embedding_provider.py`'deki `dimension` default'u 1024→2048 hizalandı + stale FEATURE.md/RAG doküman notu düzeltildi (o dokümanlar zaten yok, README zaten doğru). Diğer TECH-DEBT kalemleri (§2, §4.3–4.9, §5.3/5.4, §6, §7, §8.1/8.3, §9.1/9.2, §10, §11) AÇIK kaldı. Full suite 2410 passed/9 skipped. *(PR #149)* |

## Phase 9 — Large Schema Production Scale · Sprint 28.x · 2026-08-04 → …
**Amaç:** 2000 tabloya yaklaşan enterprise database'lerde schema selection, join
path control, missing FK inference ve graph performance problemlerini
production-grade çözmek.

| PR | Açıklama |
|---|---|
| #150 | **(28.0) Large Schema Benchmark Suite** (2026-08-04) — Phase 9'un **ilk** sprint'i; yeni `backend/benchmarks/` paketi (dev/CI aracı, `evals/`'in kardeşi) — hiçbir `app/` dosyası değişmedi, davranış korunur. Saf çekirdek: seeded sentetik şema üreteci (`schema_generator.py`, `random.Random(seed)` ile 100/500/1000/2000 tablo ölçekleri), frozen metrik sözleşmesi (`bench_contract.py`: `BenchmarkMetric`/`BenchmarkReport`), 4 REUSED prodüksiyon hedefi için girdi/çıktı-türevli determinist metrik çıkarıcılar (`bench_metrics.py`: `from_legacy_schema` validator, `find_join_paths`, `detect_implicit_relationships`, `select_schema_context`) ve 27.10 desenini izleyen `compare_benchmark` (`bench_compare.py`). Kirli kenar: `bench_runner.py` (enjekte edilen clock ile ölçer; `wall_ms` yalnızca **bilgi amaçlı**, ASLA gate'lenmez) + `bench_cli.py` (`--gate`/`--update-baseline`, exit `0`/`1`/`2`). Seeded committed baseline `baselines/history.json` (1 kayıt, 4 hedef × 4 ölçek = 16 metrik). Determinist complexity metrikleri exact-match gate'lenebilir; wall-clock yalnızca bilgi amaçlı (determinizm korunur). Yeni source-scan purity guard testi (`backend/tests/benchmarks/test_benchmarks_purity.py`) dört saf modülün (`schema_generator`/`bench_contract`/`bench_metrics`/`bench_compare`) clock/datetime/unseeded-random içermediğini kilitler. **Bilinçli kapsam dışı (→ 28.1+, TECH-DEBT §12):** iç patlama sayaçları (`dfs_visits`/`fuzzy_comparisons` — prodüksiyon hot path'lerine enstrümantasyon gerektirir), CI perf-gate kablolaması, NetworkX/`schema_graph` pruner benchmark'ı, embedding/RAG retrieval benchmark'ı (→ 28.8), frontend UI. *(PR #150)* |
| #151 | **(28.1) Schema Graph Performance Profiling** (2026-08-04) — opsiyonel sıfır-ek-yük `ProfileProbe` (`backend/app/schema/profiling.py`) `find_join_paths`/`detect_implicit_relationships`/`select_schema_context`'e iplendi; davranış korunur (`probe=None` byte-for-byte aynı çıktı, mevcut `tests/schema` yeşil `+N/-0`). Benchmark v2 determinist iç sayaçları (`dfs_visit`, `adjacency_edge`, `path_recorded`, `pair_iteration`, `fuzzy_comparison`, `rule3_scan`, `table_scan`, `column_scan`, `related_expansion`) her hedefin determinist metriklerine birleştiriyor. Üreteç near-miss kolonlar kazandı (Rule-2 fuzzy artık egzersiz ediliyor). Yeni **5. hedef** `graph_backend`: `NetworkXGraphBackend`'i (build + pagerank + shortest_path) `to_legacy_dict` reuse'iyle egzersiz eder — çıktı-türevli tam-sayı metrikler (`graph_nodes`/`graph_edges`/`sp_*`) + `wall_ms`; pagerank float ASLA gate'lenmez. Baseline `large_schema_benchmark_v2`'ye yükseldi (gate'lenen ölçekler 100/500/1000, 15 metrik; 2000 manuel referans). Yeni CI perf-gate adımı (`backend-ci.yml`: `python -m benchmarks.bench_cli --gate --scales 100,500,1000`). Prodüksiyon davranış değişikliği YOK (probe opsiyonel/guard'lı; `tests/benchmarks/` + `tests/schema/test_profiling.py` 38 passed). **TECH-DEBT §12 ÇÖZÜLDÜ:** iç patlama sayaçları, CI perf-gate kablolaması, NetworkX/graph-backend benchmark'ı. **AÇIK kalanlar:** embedding/RAG retrieval benchmark'ı (→ 28.8), `GraphPruner` candidate/policy derin entegrasyonu + pruner iç probe'u, `scipy` eksikliği (pagerank `{}`'e düşer, gate etkilenmez), `bench_contract.py`/`bench_cli.py` docstring/help kozmetik doc-sync borcu. *(PR #151)* |
| #152 | **(28.2) Join Path Explosion Control** (2026-08-06) — `find_join_paths`'in kombinatoryal DFS patlamasını İKİ katmanla sınırlayan ve yeni `JoinPathSearchResult` sözleşmesini döndüren (tek-API kırılımı; 13 çağrı noktası `.paths`'e taşındı) sprint. **Katman 1 — çıktı-koruyan branch-and-bound:** ters-BFS `hop` mesafeleri (`_hop_distances`) admissible bir derinlik sınırı verir, artı kept-set dominance pruning (optimistic tamamlama prefiksinde kesin `>`); çıktı BYTE-FOR-BYTE korunur — pruning yalnızca ziyareti keser, brute-force denklik testiyle guard'lı. **Katman 2 — determinist güvenlik kemeri:** `node_budget` (keyword-only, `DEFAULT_JOIN_PATH_NODE_BUDGET = 200_000`); taşmada DFS determinist durur ve `budget_truncated=True` işaretler. `select_schema_context` yeni `join_search_truncated: bool = False` alanı kazandı (çift üzerinde OR'lanır) + opsiyonel `node_budget` passthrough — non-breaking. Benchmark v3 (`large_schema_benchmark_v3`, gate'lenen ölçekler 100/500/1000) join_paths hedefinde ölçülen düşüş: `dfs_visit` 205→5 / 747→6 / 2653→6; `adjacency_edge` (probe) 368→19 / 1452→186 / 5264→375; yeni `branches_pruned` 15/180/369. Çıktı-türevli metrikler DEĞİŞMEDİ (`paths_found_total`=2, `path_recorded`=2, `pairs_with_path`=2 tüm ölçeklerde) — çıktı korumasının kanıtı. Gate'lenen ölçeklerde `join_budget_truncated` yok; CI perf-gate yeşil (exit 0). **Bilinçli kapsam dışı (TECH-DEBT §12):** ağırlıklı/maliyet-tabanlı path scoring + hub-penalty (`graph_traversal.py`'deki "future work" yorumu AÇIK kalır — 28.2 yalnızca enumerasyonu sınırlar, scoring'i değil), `GraphPruner` candidate/policy derin entegrasyonu (§12.5 hâlâ AÇIK), empirik/adaptif budget tuning (sabit 200_000 kullanılır), frontend `maxNodesLimit` kalıcı çözümü (§2, Phase 12). *(PR #152)* |
| #153 | **(28.3) Table Selection Cost Model** (2026-08-06) — `select_schema_context`'in hardcoded additive relevance skoru + düz top-K'sini **config-driven benefit-vs-cost budget modeliyle** değiştiren sprint. Yeni saf `backend/app/schema/table_selection_cost.py`: frozen `TableSelectionCostModel` (named relevance ağırlıkları = eski magic number'lar `exact_table`/`singular_plural_table`/`table_token_overlap`/`exact_column`/`column_token_overlap`/`explicit_neighbor`/`implicit_neighbor` + cost ağırlıkları `w_base`/`w_col`/`w_fk` + `cost_budget`), `table_cost() = w_base + w_col*n_columns + w_fk*n_fks`, `fk_counts()` (source-side FK sayımı), toleranslı `from_config()` (27.8 `llm_pricing` deseni: geçersiz/eksik JSON → `DEFAULT_COST_MODEL`). `select_schema_context` enjekte edilen `cost_model: TableSelectionCostModel = DEFAULT_COST_MODEL` parametresi kazandı (saf yaprak korunur; `probe`/`node_budget` seam'leri değişmedi). Benefit artık model ağırlıklarıyla skorlanır — `add_score` çağrılarında kalan magic number yok. **Seçim:** exact-match focus tabloları önce garanti edilir, kalanlar `cost_budget` altında benefit-density (`benefit/cost`) **greedy** ile eklenir (skip-and-continue — bütçeyi aşan bir aday atlanır, sıradaki denenir), `max_tables` ikincil sert tavan. **Şeffaflık:** `SelectedTable.cost` + `SchemaContextSelection.total_cost`/`cost_budget`/`budget_exhausted` alanları eklendi. Kirli config yükü `sql_pipeline.py`'de: config anahtarı `table_selection_cost_model` → `from_config` → `select_schema_context`'e enjekte; anahtar yoksa `DEFAULT_COST_MODEL`. Muhafazakâr varsayılan `cost_budget=30.0` golden şema üzerinde kalibre edildi (ölçülen max `total_cost`=25.0, en geniş case `unknown_question_uses_bounded_fallback`); golden eval yeşil, **fixture kürasyonu gerekmedi**. Ölçek-güvenli: bütçe scale100'de ~2/60 sentetik case'de bağlar, scale500/1000'de hiç bağlamaz. Benchmark v4 (`large_schema_benchmark_v4`): `context_selection` hedefi cost modelini yansıtır (scale 100 `selected_tables_total` 67→61); **`join_paths` metrikleri v3'e göre DEĞİŞMEDİ** (`dfs_visit` 5/6/6, `branches_pruned` 15/180/369) — cost modelinin join-path katmanını etkilemediğinin kanıtı. Gate yeşil (exit 0). **Bilinçli kapsam dışı:** ilişki-güven-ağırlıklı komşu benefit'i (→ 28.4 Relationship Confidence Scoring; 28.3 düz `explicit_neighbor`/`implicit_neighbor` 20/10 ağırlıklarını yalnızca cost modeline taşıdı, güven-skorlamasını eklemedi), token-tabanlı gerçek maliyet (serialize footprint) — kolon-sayısı proxy'si kullanılır, gerçek 0/1-knapsack optimalliği (deterministik greedy kullanılır, optimal olmayabilir), frontend `maxNodesLimit` kalıcı çözümü (§2, Phase 12). **İki küçük borç taşındı (TECH-DEBT §12):** fallback yolu `cost_budget`'a gate edilmiyor (varsayılan bütçe altında erişilemez, test edilmiyor), pipeline wiring testi yalnızca parse sözleşmesini kilitliyor uçtan-uca enjeksiyonu değil. Full suite 2473 passed/9 skipped. `backend/app/schema/{table_selection_cost,schema_context_selector}.py`, `backend/app/sql_pipeline.py`. *(PR #153)* |
| #154 | **(28.3.1) Tech-Debt Cleanup** (2026-08-07) — 28.3'ün ardından, faz ilerletmeyen bakım sprinti (27.11 desenini izler: dar borçları SDD ile kapat, brainstorming ile curate et). `docs/TECH-DEBT.md`'de biriken 10 kalem 4 bundle altında ÇÖZÜLDÜ: **Bundle A — §5.3/§8.1/§8.4(a)/§8.4(c)**: `live_trace_assembly`'nin canlı SECURITY span `outcome`'ı artık STAGE adı yerine 27.2 error registry'sinden türetiliyor (yeni `_security_outcome(reason_code)`: kod kategorisi SECURITY ise `denied`/`error`, değilse `flagged`/`warning`; `_SECURITY_STAGES` seçimi ve pinning testi korunur) + kayıtlı olmayan sahte bir koda dayanan 2 pre-existing test düzeltildi; `database.list_feedback` pencere sınırları yeni `_normalize_feedback_boundary` ile naive-UTC'ye normalize edildi (offset-aware bir sınır artık trace tarafıyla tutarlı filtreleniyor); dashboard debug gate'i route-level `Depends(ensure_debug_enabled)`'e taşındı (debug kapalıyken geçersiz `bucket` artık 404, param validation'dan önce çözülüyor) + `TimeseriesBucket` docstring'ine `error_count`/`metrics.errors` fark notu eklendi. **Bundle B — §6.1**: `bundle_service._latest_debug_trace` artık `TraceQuery(trace_type="nl2sql", job_id=…, limit=1)` ile doğrudan sorguluyor — eski `limit=5`'in bir job'ın 5'ten fazla `end_to_end` trace'i olduğunda asıl debug trace'i pencerenin dışında bırakıp bundle `sql` bölümünü `null` döndürme riski kapandı. **Bundle C — §12.16/§12.17**: `select_schema_context`'in bounded fallback rejimi artık `budget_exhausted=False` set ediyor (fallback bir bütçe-dalı değil bounded bir güvenlik ağıdır; önceki çelişkili `budget_exhausted=True`+`fallback_used=True` kombinasyonu kalktı, fallback'in kendisi değişmedi); yeni uçtan-uca test `test_wiring_end_to_end_config_override_changes_selection` bir `table_selection_cost_model` config override'ının gerçek bir pipeline çalıştırmasında seçim sonucunu fiilen değiştirdiğini kanıtlıyor (önceki test yalnızca parse/`from_config` çağrısını doğruluyordu). **Bundle D — §12.6/§11.3/§12.7**: `NetworkXGraphBackend.personalized_pagerank` `scipy` eksikliğini `(ImportError, ModuleNotFoundError)` olarak ayrı yakalayıp DEBUG seviyesinde logluyor (beklenmeyen diğer hatalar hâlâ `logger.error`; `scipy` bağımlılığı bilinçli olarak eklenmedi); regression-gate CLI'nin `--version` bayrağı artık `^v?\d+\.\d+(\.\d+)?$` deseniyle doğrulanıyor, uyumsuz değer baseline'a hiç yazılmadan `exit 2` ile reddediliyor; §12.7 (stale docstring/help metni) **verify-close** edildi — kod değişikliği yok, `bench_contract.py`/`bench_cli.py` zaten 28.1–28.3 düzeltme dalgalarında aşamalı olarak senkronlanmıştı (5 hedefin tamamı listeli, "4-way"/`scale: 2000` metni yok, `--scales` yardımı `100,500,1000` gösteriyor). Full suite 2483 passed/9 skipped; CI perf-gate (`bench_cli --gate --scales 100,500,1000`) exit 0. **Yapısal borçlar bilinçli AÇIK bırakıldı** (sessiz düşürme yok): `scan_cap`→DB-aggregation kökü (§7.1/§8.3/§9.2/§10.3), retry-token eksik-sayımı (§9.1), onay-state persistence (§10.1), semantik dedup (§10.2), gerçek-accuracy/case-granülarite (§11.1/§11.2/§11.4/§11.5), Phase 9/12 kalemleri (§12.4/5/9/10/11/12/13/14/15). `backend/app/trace/live_trace_assembly.py`, `backend/app/database.py`, `backend/app/api/dashboard_api.py`, `backend/app/dashboard/contract.py`, `backend/app/bundle_service.py`, `backend/app/schema/schema_context_selector.py`, `backend/tests/schema/test_cost_model_pipeline_wiring.py`, `backend/app/schema_graph/networkx_backend.py`, `backend/evals/regression_gate_cli.py`. *(PR #154)* |
| #155 | **(28.4) Relationship Confidence Scoring** (2026-08-07) — `TableSelectionCostModel`'in 28.3'te yalnızca config-driven bir modele taşıdığı **düz** `explicit_neighbor`/`implicit_neighbor` (20.0/10.0) ağırlıklarını tek bir `neighbor_base: float = 20.0` alanı altında birleştiren ve komşu benefit'ini **çarpımsal** hale getiren sprint. Yeni saf `neighbor_benefit(rel_confidence, model) = neighbor_base × (conf if conf is not None else 1.0)` (`backend/app/schema/table_selection_cost.py`); `from_config()` yeni `include_fuzzy_neighbors` bool alanını kabul edecek şekilde genişledi (eski `explicit_neighbor`/`implicit_neighbor` config anahtarları artık sessizce düşürülür). `select_schema_context`'teki (`backend/app/schema/schema_context_selector.py`) EXPLICIT/CUSTOM/IMPLICIT komşu genişletmesi artık `neighbor_base × effective_confidence` ile skorlanıyor (explicit/custom ilişki `confidence=None` → 1.0, implicit ilişki 0.60–0.90 aralığında ölçülü güven); `IMPLICIT_FUZZY` komşular yeni `include_fuzzy_neighbors: bool = False` ile **yalnızca opt-in** genişliyor (varsayılan kapalı). Güven artık **tek** trust sinyali — reason etiketleri tip-tabanlı (stabil prefix) kalıyor, tip-ağırlık çifte-sayımı yok. `probe`/`node_budget`/`cost_budget`/seçim/join-path/fallback davranışı **DEĞİŞMEDİ** — güven yalnızca komşu benefit büyüklüğünü etkiliyor. Muhafazakâr `neighbor_base=20.0` seçimi mevcut fixture'larda **davranış-koruyucu**: golden şemanın 6 kenarı hepsi explicit `confidence=None` → `20×1.0=20` = eski `explicit_neighbor` ağırlığı; golden eval yeşil, **fixture kürasyonu gerekmedi**. **Benchmark: versiyon bump gerekmedi** — `context_selection` metrikleri committed `large_schema_benchmark_v4` baseline'ıyla **byte-identical** (sentetik şema yalnız explicit-FK içeriyor → komşu benefit'i etkilenmedi); gate yeşil (exit 0), `join_paths`/`implicit_fk` metrikleri dokunulmadı. **TECH-DEBT §12.13 ÇÖZÜLDÜ** (diğer §12 kalemleri — §12.14 token-tabanlı gerçek maliyet, §12.15 gerçek 0/1-knapsack optimalliği, §12.5/9/10/11/12 vb. — bilinçli AÇIK kaldı). Full suite 2490 passed/9 skipped; CI perf-gate exit 0. `backend/app/schema/table_selection_cost.py`, `backend/app/schema/schema_context_selector.py`. *(PR #155)* |
| #156 | **(28.5) Missing Foreign Key Inference v2** (2026-08-07) — `detect_implicit_relationships`'i (`backend/app/schema/implicit_relationships.py`) **PK-aware** hale getiren sprint. Yeni saf `resolve_target_key(table) -> str \| None`: tek-kolonlu deklare PK (`ColumnSchema.primary_key`/`TableSchema.primary_key_columns`) → o kolon; composite PK (2+ deklare kolon) → `None`; deklare PK yoksa `"id"` sonra `"<singular_table>_id"` konvansiyon fallback'i; hiçbiri yoksa `None`. **Rule 1** (`singular_table_id_pattern`, conf 0.90) ve **Rule 2** (`fuzzy_prefix_to_table_match`, IMPLICIT_FUZZY conf 0.75/0.65) artık hardcoded `"id"` yerine `resolve_target_key(tgt)`'i hedefliyor → **recall**: `id` olmayan PK'li tablolarda ilişki artık yakalanıyor. **Rule 3** (`exact_non_generic_column_match`, conf 0.60) artık YALNIZ kaynak kolon adı hedefin çözülmüş anahtarına eşitse tetikleniyor → **precision**: PK olmayan bir kolona rastgele isim-eşleşmesiyle kurulan spurious FK kenarları düştü. Confidence değerleri ve `reason` string'leri **korundu** (benchmark `derive_implicit_fk_metrics` bucketing etkilenmedi); `raw["target_key"]` yeni alan eklendi. Production (`schema_manager.py`) çıkarımı iyileşir; schema-only ve deterministik kalır. **Benchmark v5** (`large_schema_benchmark_v5`): `implicit_fk` hedefinde `rule3_exact` 461/13024/56449 → **0** (scale 100/500/1000) — v1 on binlerce spurious non-key kenar çıkarıyordu; `implicit_rels_found` artık yalnız rule1+rule2 (92/730/1472); diğer implicit_fk sayaçları (`fuzzy_comparison`/`pair_iteration`/`rule1_singular_id`/`rule2_fuzzy`/`rule3_scan`/`other`) tüm ölçeklerde DEĞİŞMEDİ. **`join_paths`/`context_selection`/`graph_backend` v4'e göre BYTE-IDENTICAL** (`select_schema_context`/`find_join_paths` explicit FK üzerinde çalışır, implicit-detection çıktısını tüketmez — coupling yok, unintended coupling kanıtı). Gate yeşil (exit 0). **Bilinçli kapsam dışı (TECH-DEBT §13):** type-uyumluluk sinyali/gate, config-driven eşikler (confidence/fuzzy-ratio/generic-liste hardcoded kalır), unique-ama-PK-olmayan hedefler (PK-only tasarım kararı, Q2), composite-PK FK çıkarımı (tek-kolon PK'ye odaklanılır), veri örneklemesi/value-overlap/cardinality (schema-only kalır). Full suite 2501 passed/9 skipped. `backend/app/schema/implicit_relationships.py`, `backend/benchmarks/baselines/history.json`, `backend/benchmarks/{bench_cli,bench_contract}.py`. *(PR #156)* |
| #157 | **(28.6) Schema Cache Invalidation** (2026-08-07) — `SchemaManager.load_schema`'nın (`backend/app/schema_manager.py`) eski `db_type`-only okuma predikatını içeriğe duyarlı bir fingerprint'le değiştiren sprint. Yeni saf `backend/app/schema_cache_fingerprint.py`: `SCHEMA_CACHE_VERSION = "v1"` + `compute_cache_fingerprint(*, db_type, hidden_tables_raw, hidden_columns_raw, embedding_model, cache_version=SCHEMA_CACHE_VERSION) -> str` — girdileri `\x1f` ayracıyla birleştirip (`None` → `""`) sha256 alır; stdlib-only, deterministik. `load_schema` artık `_schema_lock` içinde en başta güncel fingerprint'i hesaplıyor (embedding model `SchemaEmbeddingIndex().embedding_client.model` ile **ucuz** okunuyor, index build tetiklenmiyor), cache'teki `cache_fingerprint`'le karşılaştırıyor; uyuşmazlık/eksiklikte yeniden çıkarım yapılıyor. Eski `cache_data.get("db_type") == self.params["type"]` kontrolü yeni fingerprint'e **subsumed** (`db_type` fingerprint girdilerinden biri). Yazma payload'ı yeni `cache_fingerprint` alanını kazandı. Lock/cache-stampede/deepcopy/RAG-indexleme/ilişki-dressing **DEĞİŞMEDİ**. **Etki:** `hidden_tables_<db_type>`/`hidden_columns_<db_type>` config drift'i ve embedding-model değişikliği artık cache'i otomatik invalidate ediyor; fingerprint'siz eski (legacy) bir `schema_cache.json` bir kez yeniden çıkarılıp fingerprint'li olarak yeniden yazılıyor; `SCHEMA_CACHE_VERSION` bump'ı (extraction-kod değişikliklerinde) tüm cache'leri invalidate ediyor. Self-contained — yeni bir DB sorgusu **YOK**. **Benchmark etkilenmedi** (`from_legacy_schema` sentetik şemalar üzerinde çalışır, `SchemaManager.load_schema`'yı egzersiz etmez) — gate yeşil (exit 0), versiyon bump gerekmedi. **Bilinçli kapsam dışı (TECH-DEBT §14):** ham DB şema drift'i (tablo/kolon değişikliği — self-contained fingerprint bunu yakalamaz, hafif per-dialect DB fingerprint sorgusu gerekir; 28.7 Incremental Schema Sync ile örtüşebilir), TTL/zaman-tabanlı invalidation, explicit invalidation endpoint/event, granüler embedding-only invalidation (şu an tüm cache yeniden yazılıyor). Full suite 2509 passed/9 skipped. `backend/app/schema_cache_fingerprint.py`, `backend/app/schema_manager.py`, `backend/tests/schema/test_schema_cache_fingerprint.py`, `backend/tests/schema/test_schema_manager_cache_invalidation.py`. *(PR #157)* |
| #158 | **(28.7) Incremental Schema Sync** (2026-08-10) — `SchemaManager.load_schema`'nın 28.6'da bilinçli kapsam dışı bırakılan ham DB yapısal drift'ini (tablo/kolon/FK ekleme-kaldırma-değişim) kapatan sprint. Yeni saf `backend/app/schema/schema_signature.py`: `SCHEMA_SIGNATURE_VERSION = "v1"` + `normalize_structure(schema)` (dialect-agnostic, sıra-bağımsız kanonik yapı — kolon/FK'ler sıralı tuple'lara indirgenir) + `compute_schema_signature(normalized) -> str` (sha256, stdlib-only, deterministik) + `diff_structures(old, new) -> StructuralDrift` (frozen dataclass: `added_tables`/`removed_tables`/`added_columns`/`removed_columns`/`changed_columns`/`added_fks`/`removed_fks` + `has_drift` + `as_dict()`). `SchemaManager`'a `_current_normalized_structure()`/`_current_schema_signature()` helper'ları eklendi (ikincisi `extract_schema_metadata()`'yı yeniden çalıştırıp taze signature üretir — ultra-ucuz ayrı bir DB-side sinyal değil, extract-reuse); cache payload'ı yeni `schema_signature` alanını kazandı. `load_schema`'da yeni **opt-in** yapısal drift kontrolü: config `auto_schema_drift_check` **varsayılan KAPALI** (`_is_truthy_config` toleranslı okuma; `None`/kapalı → 28.6 davranışı BYTE-FOR-BYTE korunur). Açıldığında, cache-fingerprint eşleştiğinde bile lock içinde güncel signature hesaplanıp cache'teki `schema_signature`'la karşılaştırılır; uyuşmazlıkta/eksiklikte `base_schema=None` set edilip TAM yeniden çıkarım + yeniden embedding tetiklenir (aynı `if base_schema is None` dalı — kod tekrarı yok). Yeni debug-gated router `backend/app/api/schema_sync_api.py` (metrics_api/rule_suggestions_api deseni: kendi `APIRouter`, `verify_api_key` + route-level `ensure_debug_enabled()` → debug kapalıyken 404): **yan-etkisiz** `GET /api/debug/schema/drift` (cache dosyasını okur + `SchemaManager()._current_schema_signature()` ile taze signature hesaplar, ASLA `load_schema` çağırmaz — rebuild yok) → `{drifted, signature_version, cached_signature, current_signature, drift}`; **drift-aware** `POST /api/debug/schema/sync?force=false` — yalnız `drifted` veya `force=true` ise `SchemaManager().load_schema(force_refresh=True)` çağırır (`action: "rebuilt"` \| `"up_to_date"`). **Benchmark etkilenmedi** (28.7 dört benchmark hedefine de `SchemaManager.load_schema`'nın egzersiz edilen yoluna da dokunmuyor — `bench_cli --gate --scales 100,500,1000` exit 0) — versiyon bump gerekmedi. **Bilinçli kapsam dışı (TECH-DEBT §15, sessiz düşürme yok):** true per-table incremental re-extract/merge (drift'te hâlâ TAM şema yeniden çıkarımı, kısmi/tablo-bazlı merge yok), granüler embedding-only re-index (→ 28.8 Embedding/RAG Re-Index Pipeline, TECH-DEBT §14.4 hâlâ açık), TTL/zaman-tabanlı invalidation (TECH-DEBT §14.2 hâlâ açık), ultra-ucuz tek-sorgulu DB-taraflı drift sinyali kullanılmadı (extract-reuse tercih edildi), yalnızca yapısal drift (satır/veri-seviyesi drift analiz edilmez), `POST /sync` rebuild'i tüm-cache'dir (şema+embedding birlikte), kısmi/seçici rebuild değil. TECH-DEBT §14.1 (ham DB şema drift'i) ve §14.3 (explicit sync endpoint) ÇÖZÜLDÜ. Full suite 2537 passed/9 skipped; benchmark gate yeşil (exit 0). `backend/app/schema/schema_signature.py`, `backend/app/schema_manager.py`, `backend/app/api/schema_sync_api.py`, `backend/app/api/schemas.py`, `backend/app/main.py`. *(PR #158)* |
| #159 | **(28.8) Embedding / RAG Re-Index Pipeline** (2026-08-10) — 28.7'nin bilinçli kapsam dışı bıraktığı "drift'te tüm cache (`schema`+`embeddings`) yeniden yazılıyor" sınırını embedding tarafında kapatan sprint. Yeni saf `backend/app/schema/reindex_planner.py`: `build_table_embedding_text(table_name, table_meta)` (embed edilen metnin tek doğruluk kaynağı), `compute_embedding_fingerprint(text, model, *, version=REINDEX_FINGERPRINT_VERSION) -> str` (sha256, model-aware), `stable_point_id(name) -> int` (deterministik, restart-bağımsız Qdrant id — `sha256` digest'inin ilk 8 byte'ından türetilir) ve `plan_reindex(old_fingerprints, new_schema, model) -> ReindexPlan` (frozen dataclass: `to_embed`/`to_keep`/`to_delete`, sıralı/deterministik). `SchemaEmbeddingIndex._generate_table_fingerprint` (`backend/app/schema_embedding.py`) artık `build_table_embedding_text`'i delege ediyor (çıktı byte-identical — embed edilen metinle fingerprint'lenen metin artık tek kaynaktan besleniyor, ayrışamaz). **Kök düzeltme** — `backend/app/rag_manager.py`'deki `index_ddl`/`index_schema_batch` eskiden `int(hash(table_name) % 10**8)` ile point id üretiyordu; Python `hash()` `PYTHONHASHSEED`'e bağlı ve **süreç-başına farklı**dır — her restart aynı tabloya farklı bir point id verip eskisini Qdrant'ta orphan bırakabiliyordu. Artık `stable_point_id` kullanılıyor (`business_rules`/`sql_history` koleksiyonları bilinçli olarak `hash()`'te bırakıldı — TECH-DEBT §16.1). Yeni `delete_schema_points(names)`/`audit_schema_ddl_points(valid_table_names)`/`prune_schema_ddl_points(valid_table_names)` (`schema_ddl` koleksiyonuna özel, best-effort — Qdrant hatası `logger.warning` + boş/sıfır sonuçla yutulur, asla raise etmez). Yeni `backend/app/schema_reindex.py`: `reindex_embeddings(*, old_embeddings, new_schema, model, embedder, rag=None, force=False) -> (dict, ReindexReport)` — `plan_reindex`'i kullanıp yalnız `to_embed` tablolarını `embedder.build_index` ile embed eder (bir alt-şema üzerinde, tek çağrı), `to_keep` vektörlerini cache'ten reuse eder (cache'te vektörü eksik bir `to_keep` savunmacı olarak yeniden embed listesine eklenir), `to_delete` tablolarını `rag.delete_schema_points` ile Qdrant'tan tahliye eder; çıktı aynı model+şema için tam `build_index`'e **çıktı-eşdeğerdir**. `SchemaManager.load_schema`'nın (`backend/app/schema_manager.py`) rebuild dalı artık bu executor'ı çağırıyor; cache `embeddings` yeni `fingerprints` alanını kazandı; `force_refresh=True` iken cache-okuma bloğu tamamen atlandığı için `old_embeddings_for_reindex` hep `None` kalır → tam yeniden-embed (28.6/28.7'nin force-refresh davranışı korunur; cache-fingerprint/schema-signature akışına dokunulmadı). Yeni debug-gated endpoint'ler (`backend/app/api/schema_sync_api.py`, mevcut `/drift`/`/sync` deseniyle aynı router): yan-etkisiz `GET /api/debug/schema/reindex-status` (cache'i okur + `plan_reindex` + `RAGManager.audit_schema_ddl_points` ile `{status, model, fresh, stale, new, to_delete, orphaned_qdrant}` raporlar, **rebuild YOK**) ve `POST /api/debug/schema/reindex?force=false` (yalnız cache'li şema üzerinde embedding-only re-index çalıştırır — `SchemaManager()`/DB re-extract'e **hiç dokunmaz**; `reindex_embeddings` + `prune_schema_ddl_points` + `index_schema_batch` upsert + cache'e tazelenmiş `embeddings`+`cache_fingerprint` yazımı → `{status, embedded, kept, deleted, pruned, model}`). Yeni response modelleri `ReindexStatusEnvelopeResponse`/`ReindexEnvelopeResponse` (`backend/app/api/schemas.py`). **Benchmark etkilenmedi** (28.8 dört benchmark hedefine de `SchemaManager.load_schema`'nın egzersiz edilen yoluna da dokunmuyor — `bench_cli --gate --scales 100,500,1000` exit 0) — versiyon bump gerekmedi. TECH-DEBT §14.4 ve §15.2 (granüler embedding-only re-index) ÇÖZÜLDÜ. Full suite 2569 passed/9 skipped. **Bilinçli kapsam dışı (TECH-DEBT §16, sessiz düşürme yok):** `business_rules`/`sql_history` koleksiyonları hâlâ non-deterministic `hash()` id / granüler re-index yok, embedding-model değişikliğinde otomatik embedding-only kısa-devre yok (yalnız elle çağrılan `POST /reindex` DB re-extract'ten kaçınır), Qdrant collection/vector_size migration, embed-text zenginleştirme (örnek değerler), batch-size tuning değişmedi. Ayrıca dar bir edge-case NARROWED: `force=True` + eşzamanlı tablo kaldırma → `to_delete` force-sıfırlanmış (`old_fp={}`) fingerprint kümesinden hesaplandığı için o zorlanmış rebuild'in kendi `reindex_embeddings` çağrısı kaldırılan tablonun Qdrant point'ini tahliye EDİLMEZ — final-review fix wave'de (0807a0b) eklenen prune-on-rebuild (aynı rebuild bloğunda `reindex_embeddings`'ten hemen sonra aynı `_rag` örneğiyle `RAGManager.prune_schema_ddl_points` çağrısı) artık aynı rebuild içinde temizliyor; ayrıca `POST /api/debug/schema/reindex` talep üzerine prune eder. (Önceki "bir sonraki ... force-olmayan bir drift ile kendiliğinden düzelir" iddiası YANLIŞTI — kaldırılan tablo `fingerprints`'te hiç yer almadığından hiçbir sonraki `plan_reindex` onu `to_delete`'e koymaz; düzeltildi.) `backend/app/schema/reindex_planner.py`, `backend/app/schema_reindex.py`, `backend/app/schema_embedding.py`, `backend/app/rag_manager.py`, `backend/app/schema_manager.py`, `backend/app/api/schema_sync_api.py`, `backend/app/api/schemas.py`. *(PR #159)* |
| #160 | **(28.9) Semantic / Result Cache** (2026-08-10) — Phase 9'un **SON** sprint'i. Üretilen SQL'i tamamen aynı (dialect + 28.7'nin `schema_signature`'ı + normalize edilmiş natural-language sorgu) istekler için writer-critic LLM döngüsünü atlayarak döndüren **deterministik exact result cache** (semantik/embedding-benzerlik cache DEĞİL, bilinçli kapsam dışı — TECH-DEBT §17.1). Yeni saf `backend/app/cache/result_cache_key.py`: `RESULT_CACHE_KEY_VERSION = "v1"` + `normalize_query(q)` (casefold + whitespace-collapse) + `compute_result_cache_key(natural_query, dialect, schema_signature, *, version=RESULT_CACHE_KEY_VERSION) -> str` (sha256, stdlib-only, deterministik; `\x1f`-ayraçlı payload) — `schema_signature`'a bağlanma yapısal şema drift'inde anahtarı otomatik değiştirir. Yeni `sql_cache` SQLite tablosu (`database.init_db` migration: `cache_key` PK, `sql`, `dialect`, `schema_signature`, `created_at`, `hit_count`; **RAW NL query kolonu YOK — secret-free**) + `backend/app/result_cache.py` adaptor: `is_result_cache_enabled(value)` (varsayılan **AÇIK** — `None`/boş → `True`, yalnız `"0"/"false"/"no"/"off"` kapatır), `get_cached_sql`/`put_cached_sql` (`INSERT OR REPLACE`, mevcut `hit_count` korunur)/`bump_hit`/`clear_cache() -> int`/`cache_stats()`. Yeni `SchemaManager.get_cached_schema_signature()` (`backend/app/schema_manager.py`) — cache dosyasını doğrudan okur (`load_schema` tetiklenmez, DB extract YOK; eksik/legacy/okunamaz cache → `None`). `backend/app/sql_pipeline.py`'ye yeni `_cache_lookup(*, aqr, dialect, pruned_schema, log_callback)`: `cache_active = is_result_cache_enabled(...) and isinstance(schema_signature, str)`; aktifse anahtar hesaplanıp `get_cached_sql` ile bakılır — **hit'te cache'lenmiş SQL `self.validate_sql(...)` ile YENİDEN doğrulanır, güvenlik/guardrail/AST/semantic zinciri ASLA bypass edilmez** (`test_hit_but_invalid_falls_through_to_generation`); geçersiz hit sessizce tam üretime düşer. `run_pipeline`: retrieval sonrası/writer-critic öncesi bu lookup çağrılır; geçerli hit'te **LLM döngüsü tamamen ATLANIR** (`test_cache_hit_skips_writer_critic`) + `bump_hit`; miss+başarıda yeni SQL `put_cached_sql` ile yazılır (`test_cache_miss_generates_and_stores`); `schema_signature` yoksa (`test_no_signature_disables_cache`) veya config kapalıysa (`test_disabled_config_always_generates`) cache tamamen devre dışı, davranış 28.8-öncesiyle birebir. Sonuçta yeni `cache_hit: bool` alanı + legacy trace metadata'sında aynı bayrak. Yeni debug-gated router `backend/app/api/cache_api.py` (metrics_api/schema_sync_api deseni): yan-etkisiz `GET /api/debug/cache/stats` (`{status, entries, total_hits}`) + `POST /api/debug/cache/clear` (`{status, deleted}`); yeni response modelleri `CacheStatsEnvelopeResponse`/`CacheClearEnvelopeResponse` (`backend/app/api/schemas.py`). Config `sql_result_cache_enabled` varsayılan **AÇIK**. **Benchmark etkilenmedi** (28.9 dört benchmark hedefine de dokunmuyor, `load_schema`/`run_pipeline` egzersiz edilmez — `bench_cli --gate --scales 100,500,1000` exit 0) — versiyon bump gerekmedi. **Bilinçli kapsam dışı (TECH-DEBT §17, sessiz düşürme yok):** semantik/embedding-benzerlik cache (precision riski), execution/result-set (satır-seviyesi) cache (→ Phase 10), TTL/boyut-tabanlı otomatik eviction (yalnız elle çağrılan `POST /clear`), prompt-template/model değişikliğinde otomatik invalidation (anahtar yalnız `schema_signature`'a bağlı — freshness-only tasarım), cache-hit-rate agregasyonu/dedike dashboard paneli (yalnız ham `cache_hit` bayrağı), frontend `maxNodesLimit=5` kalıcı çözümü (farklı — frontend — katman, AÇIK kalır, §2). Full suite 2610 passed/9 skipped. **Bununla Phase 9 (Large Schema Production Scale) kapanır** — sıradaki Phase 10 (29.0 PostgreSQL Docker Integration Adapter). `backend/app/cache/result_cache_key.py`, `backend/app/result_cache.py`, `backend/app/database.py`, `backend/app/schema_manager.py`, `backend/app/sql_pipeline.py`, `backend/app/api/cache_api.py`, `backend/app/api/schemas.py`, `backend/app/main.py`. *(PR #160)* |
| #161 | **(29.0) PostgreSQL Docker Integration Adapter** (2026-08-11) — Phase 10'un **ilk** sprint'i, **Phase 10 başlar**. 25.8'in local-Docker-only stub'ının üzerine gerçek `information_schema` introspection'ı ekleyen sprint — hâlâ **yalnızca local Docker**, hâlâ production pipeline'a wire edilmemiş (inert/standalone). Repo kökünde local Docker parity `docker-compose.yml` (`postgres:16-alpine`, CI ile parite) + deterministik seed fixture `backend/tests/fixtures/postgres/seed.sql`: `customers`/`orders`/`order_items` (tek-kolon PK/FK) + `product_variants`/`variant_stock` (composite PK/FK). Yeni saf `backend/app/evaluation/postgres_connection_resolver.py`: `resolve_local_docker_connection(env) -> SQLPostgresLocalDockerConnection \| None` (ASLA raise etmez; uzak/geçersiz host → `None`; DB driver/socket importu yok) — bağlantı kurulan TEK yeniden-kullanılabilir yer. Yeni contract-first `backend/app/evaluation/postgres_schema_adapter.py`: `information_schema`'yı **yeni, güvenli** bir bağlantı üzerinden (local-docker-only, read-only, lazy `psycopg2` import, credential-free hata sanitizasyonu, `connection=None` → inert `NOT_IMPLEMENTED`) okuyup EXPLICIT ilişkili bir `DatabaseSchema` üreten `build_database_schema_from_introspection(...)` + `SQLPostgresSchemaAdapterContract.introspect()`; **composite (çok-kolonlu) FK'ler `referential_constraints` + `position_in_unique_constraint` ile ordinal-eşleştirmeli doğru şekilde eşleniyor**. Docker'sız safe-skip gated entegrasyon testleri (CI'da koşar): seeded tabloların introspection'ı (composite-FK doğru eşleşme dahil) + 25.8 execution adapter'ı üzerinden gerçek çok-tablolu `JOIN`. CI'a (`backend-ci.yml`) "Seed PostgreSQL integration schema" adımı eklendi. **Benchmark etkilenmedi** (29.0 dört benchmark hedefine de dokunmuyor) — versiyon bump gerekmedi, gate yeşil (exit 0). Full suite 2629 passed/16 skipped. Spec: `docs/superpowers/specs/2026-08-11-sprint-29.0-postgresql-docker-integration-adapter-design.md`; Plan: `docs/superpowers/plans/2026-08-11-sprint-29.0-postgresql-docker-integration-adapter.md`. `docker-compose.yml`, `backend/tests/fixtures/postgres/seed.sql`, `backend/app/evaluation/postgres_connection_resolver.py`, `backend/app/evaluation/postgres_schema_adapter.py`, `backend/app/evaluation/__init__.py`, `backend/tests/evaluation/test_postgres_connection_resolver.py`, `backend/tests/evaluation/test_postgres_schema_adapter.py`, `backend/tests/evaluation/test_postgres_schema_adapter_integration.py`, `backend/tests/evaluation/test_postgres_fixtures_contract.py`, `backend/tests/evaluation/test_postgres_adapter_integration.py`, `.github/workflows/backend-ci.yml`. *(PR #161)* |
| #162 | **(29.1) PostgreSQL Read-Only Execution** (2026-08-12) — 25.8 stub'ının gerçeği. 29.0'ın yalnızca *şema okuma* katmanında bıraktığı yerden devam edip, dialect-agnostic paylaşılan execution sözleşmesine (`backend/app/evaluation/multi_database_execution.py`, `SQLDatabaseExecutionAdapter`) bağlı yeni `PostgresDatabaseExecutionAdapter`'ı ekleyen sprint — hâlâ **yalnızca local Docker**, hâlâ production pipeline'a wire edilmemiş (standalone/inert). Yeni `backend/app/evaluation/postgres_execution_adapter.py`: 29.0'ın `resolve_local_docker_connection`'ı ile local-Docker connection'ı çözer, gerçek `SELECT`'i 25.8'in `SQLPostgresAdapterContract`'ına delege eder, ham sonucu saf `normalize_postgres_execution_result` ile paylaşılan `SQLDatabaseExecutionResult`'a normalize eder (dict satırlar kolon adlarından kurulur; arity uyuşmazlığında pozisyonel `col_<i>` fallback'i + uyarı). 25.8'e geriye-uyumlu ekleme: `SQLPostgresAdapterExecutionResult.columns` (`cur.description`'dan yakalanır) — dict satırların gerçek kolon adı taşımasını sağlar. Bağlantı çözümlenemediğinde (resolver `None`) adapter **asla raise etmez** — boş `SQLDatabaseExecutionResult` + `execution_error` döner. Import-time driver-izolasyonu subprocess testiyle kilitlendi (`psycopg2`/`asyncpg`/`sqlalchemy` import edilmez). Docker'sız safe-skip gated entegrasyon testleri (CI'da koşar): gerçek `SELECT` → adlandırılmış dict satırlar, çok-tablolu `JOIN`, `max_rows` truncation, `statement_timeout`. **Production request pipeline'ına HİÇ WIRE EDİLMEDİ** — adapter standalone kalır. **Bilinçli kapsam dışı (TECH-DEBT §19, sessiz düşürme yok):** canlı HTTP pipeline wiring (→ Phase 11 · 30.2 Connection Registry API / 30.4 Query Run API), Connection Registry/çoklu-bağlantı (→ 30.2), 26.3/26.4 risk/sensitive-tablo gate wiring, EXPLAIN-only mode (→ 29.2), Oracle/MySQL/SQL Server execution adapter'ları (→ 29.3–29.6), execution trace/audit persistence (→ 30.4), merkezî/canlı execution router registry, import-time network-client isolation testi (yalnız DB-driver isolation edildi). `backend/app/evaluation/postgres_execution_adapter.py`, `backend/app/evaluation/postgres_adapter.py`, `backend/tests/evaluation/test_postgres_execution_adapter.py`, `backend/tests/evaluation/test_postgres_execution_adapter_integration.py`. *(PR #162)* |
| #163 | **(29.2) PostgreSQL EXPLAIN-Only Mode** (2026-08-26) — 29.1'in yalnız `READ_ONLY` kabul edip `EXPLAIN_ONLY`'yi reddettiği yerden devam edip, PostgreSQL için **gerçek EXPLAIN(-only) execution path'ini** ekleyen sprint — hâlâ **yalnızca local Docker**, hâlâ production pipeline'a wire edilmemiş (standalone/inert). İki katman. **Katman 1** (`backend/app/evaluation/postgres_adapter.py`): `execution_mode="explain_only"` iken doğrulanmış iç SELECT'in önüne `EXPLAIN <sql>` (opt-in `EXPLAIN (ANALYZE) <sql>`) ekleyip koşan gerçek EXPLAIN path'i + saf `_build_explain_sql` helper'ı; `SQLPostgresAdapterConfig.explain_analyze: bool = False` (default güvenli düz EXPLAIN, sessiz side-effect yok). 25.8'in read-only gate'i (26.2 string-level SELECT enforcement) **iç SELECT üzerinde değişmedi** — EXPLAIN yalnız doğrulanmış SELECT'in önüne eklenir, trailing tek `;` doğru arındırılır; non-SELECT/multi-statement → `REJECTED` (EXPLAIN prefix'i eklenmez). `sql_sha256` **her zaman orijinal** SQL'den hesaplanır (EXPLAIN'li metinden değil — unit assert ile kilitlendi). **Katman 2** (`backend/app/evaluation/postgres_execution_adapter.py`): `EXPLAIN_ONLY` modunu **kabul eder** (artık raise etmez), `capabilities()` → `(CONNECTION_REF, READ_ONLY, EXPLAIN_ONLY)`, `_to_pg_config` shared→25.8 config'i (`explain_analyze` dahil) eşler. Plan satırları tek-kolonlu `"QUERY PLAN"` dict-row'ları (`{"QUERY PLAN": ...}`) olarak döner — execution kontratı DEĞİŞMEDİ. Shared `SQLDatabaseExecutionConfig.explain_analyze: bool = False` (`backend/app/evaluation/multi_database_execution.py`) geriye-uyumlu eklendi (mevcut çağıranlar kırılmaz). Düz EXPLAIN hiç çalıştırmaz; ANALYZE opt-in ve read-only tx (`default_transaction_read_only=on`) + `statement_timeout` writes'ı/runaway'i bloklar. Driver-izolasyon korunur (import-time'da `psycopg2`/socket yüklenmez). Docker'sız safe-skip gated entegrasyon testleri (CI'da seeded Postgres ile koşar): `EXPLAIN <JOIN>` → `"QUERY PLAN"` satırları (**altındaki sorgunun data satırlarının sızmadığı** doğrulanır), `EXPLAIN (ANALYZE)` → plan + gerçek-execution satırları (non-deterministik olduğu için gerçek süreye assert edilmez, yapı/anahtar-kelime varlığına bakılır), `max_rows` truncation → `truncated=True`, kısa `timeout` → `execution_error` (raise yok). **Production request pipeline'ına HİÇ WIRE EDİLMEDİ** — adapter standalone kalır; 29.1'in postürü korunur. **Bilinçli kapsam dışı (TECH-DEBT §20, sessiz düşürme yok):** canlı HTTP pipeline wiring (→ 30.2 Connection Registry API / 30.4 Query Run API), `EXPLAIN (FORMAT JSON)` yapılandırılmış/makine-okunur plan + plan-tabanlı maliyet gate/analiz (→ ileri sprint), connection registry/çoklu-bağlantı/uzak & production connection (→ 30.2), Oracle/MySQL/SQL Server EXPLAIN path'leri (→ 29.3–29.6), merkezî/canlı execution router registry (→ 30.x), execution trace/audit persistence (→ 30.4). Bilinen minor (bloklamaz, 30.x wiring'de uzlaştırılacak — TECH-DEBT §20): `backend/app/evaluation/connection_aware_execution.py` non-READ_ONLY'yi hard-reject ederken adapter EXPLAIN_ONLY advertise ediyor — 29.2'de bug yok (inert). Spec: `docs/superpowers/specs/2026-08-14-sprint-29.2-postgres-explain-only-mode-design.md`; Plan: `docs/superpowers/plans/2026-08-14-sprint-29.2-postgres-explain-only-mode.md`. `backend/app/evaluation/postgres_adapter.py`, `backend/app/evaluation/postgres_execution_adapter.py`, `backend/app/evaluation/multi_database_execution.py`, `backend/tests/evaluation/test_postgres_adapter.py`, `backend/tests/evaluation/test_postgres_execution_adapter.py`, `backend/tests/evaluation/test_postgres_execution_adapter_integration.py`, `backend/tests/evaluation/test_multi_database_execution.py`. *(PR #163)* |
| #164 | **(29.3) Oracle Contract Adapter** *(25.9 stub'ının gerçeği)* (2026-08-26) — 25.9'un Oracle adapter **stub**'ını (tüm execution flag'leri `False`, `execute()` her zaman `NOT_IMPLEMENTED`/`REJECTED`), PostgreSQL 29.0→29.1 desenini birebir izleyerek gerçek bir **local-Docker-only, read-only Oracle execution path**'ine çeviren sprint — hâlâ **standalone/inert**, canlı production pipeline'a wire EDİLMEDİ. Üç katman. **Katman 1** (`backend/app/evaluation/oracle_adapter.py`): 25.9 stub'ının contract yüzeyi korunarak (geriye-uyum: all-`False` `default_oracle_stub_capability` + `connection=None` inert `NOT_IMPLEMENTED` path) üzerine gerçek `oracledb` **thin mode** read-only SELECT path'i eklenir. Üç savunma katmanı: (1) 26.2 string-level read-only SELECT gate herhangi bir driver dokunuşundan ÖNCE; (2) lazy `import oracledb` yalnız `_execute_select` içinde (import-time driver-izolasyon, subprocess testiyle kilitli); (3) DB-seviyesi `SET TRANSACTION READ ONLY` iç SELECT'ten önce + `conn.autocommit = False` + `conn.call_timeout = int(timeout_seconds*1000)` (thin-mode round-trip timeout) + `finally` bloğunda guarded `rollback()`/`close()` (her çıkış yolunda). Yeni: `SQLOracleAdapterStatus.{EXECUTED,EXECUTION_ERROR}`; `default_local_docker_capability()` (invariant Postgres-tarzı bölümlemeye gevşetildi — yalnız `supports_live/remote/production_execution` `True` olamaz, kalan flag'ler serbest boolean; böylece hem all-`False` stub hem True-karışımı local-docker capability geçerli); `SQLOracleLocalDockerConnection(host, port, service_name, user, password, environment)` (local-docker host allowlist `localhost`/`127.0.0.1`/`::1` + port 1..65535 + non-empty `service_name`/`user`); result `columns: Tuple[str,...]` alanı (geriye-uyumlu, dict satırlar gerçek kolon adı taşısın); secret-free `_sanitize_oracle_error` (DSN/host/user/password/service ASLA; oracledb timeout kodları → ayrı deterministik timeout mesajı). `execute()` sırası **validate-first** (read-only gate → `connection is None` kontrolü) — 25.9 stub'ının "no-connection + unsafe SQL → `REJECTED`" kontratını korur ve strictly-safer (unsafe SQL her koşulda REJECTED). **Katman 2** (yeni `backend/app/evaluation/oracle_connection_resolver.py`): `resolve_local_docker_connection(env)` `ORACLE_TEST_HOST/PORT/SERVICE/USER/PASSWORD` (default `localhost`/`1521`/`FREEPDB1`/`sqlgen`/`sqlgen`) → `SQLOracleLocalDockerConnection | None`; asla connect etmez, asla raise etmez (non-int port / uzak host → `None`). **Katman 3** (yeni `backend/app/evaluation/oracle_execution_adapter.py`): dialect-agnostic `SQLDatabaseExecutionAdapter` köprüsü — `dialect=ORACLE`, `capabilities() = (CONNECTION_REF, READ_ONLY)` (**EXPLAIN_ONLY YOK**, reddedilir), `connection_ref` zorunlu + `fixture_ref` reddedilir, resolver `None` → graceful boş `SQLDatabaseExecutionResult` + `execution_error="No local Docker Oracle connection available"` (raise yok); `normalize_oracle_execution_result` + `_rows_to_dicts` (arity uyuşmazlığı/boş columns → `col_<i>` fallback + uyarı, veri düşürülmez); `_to_oracle_config` shared→low-level eşler. `backend/app/evaluation/multi_database_execution.py` **DEĞİŞMEDİ** (`SQLDatabaseDialect.ORACLE` + `READ_ONLY`/`CONNECTION_REF` zaten mevcut). `oracledb==2.5.1` zaten `requirements.txt`'de pinliydi. Testler: fake-`oracledb` unit testleri (çalıştırılan SQL sırası `["SET TRANSACTION READ ONLY", sql]`, `call_timeout`/`autocommit`/`rollback`/`close`, truncation, timeout→`EXECUTION_ERROR`, driver-yok→`EXECUTION_ERROR`), bridge normalization/graceful/EXPLAIN-reject/router-pluggability testleri, iki driver-isolation subprocess testi (`oracledb`/`cx_Oracle`/`sqlalchemy` import-time'da yüklenmez; network-client listesi postgres'teki aynı pydantic re-export false-positive nedeniyle portlanmadı). **Bilinçli kapsam dışı (TECH-DEBT §21, sessiz düşürme yok):** gerçek Oracle Docker imajı + seed şeması + CI job (**→ 29.4** Oracle Docker/Test Harness Strategy — bu yüzden gated `integration`+`oracle`-mark'lı testler bugün safe-skip eder, bare-Oracle `DUAL`/`CONNECT BY LEVEL` sorguları kullanır, seed'e ihtiyaç duymaz), Oracle schema introspection (`ALL_TAB_COLUMNS`/`ALL_CONSTRAINTS`/`ALL_CONS_COLUMNS`; Postgres 29.0 dengi → ileriye), Oracle EXPLAIN-only mode (`EXPLAIN PLAN FOR` + `DBMS_XPLAN`; 29.2 dengi → ileriye), canlı HTTP pipeline wiring (→ 30.2/30.4), connection registry/çoklu-bağlantı/uzak & production connection/wallet/TNS (→ 30.2), `oracledb` thick mode/Instant Client, tam bridge→low-level real-execution unit testi fake-driver ile (→ 29.4). Full suite 2682 passed/28 skipped; driver-isolation 7/7; CI (Backend tests) yeşil. Spec: `docs/superpowers/specs/2026-08-26-sprint-29.3-oracle-contract-adapter-design.md`; Plan: `docs/superpowers/plans/2026-08-26-sprint-29.3-oracle-contract-adapter.md`. `backend/app/evaluation/oracle_adapter.py`, `backend/app/evaluation/oracle_connection_resolver.py`, `backend/app/evaluation/oracle_execution_adapter.py`, `backend/app/evaluation/__init__.py`, `backend/pytest.ini`, `backend/tests/evaluation/test_oracle_adapter.py`, `backend/tests/evaluation/test_oracle_connection_resolver.py`, `backend/tests/evaluation/test_oracle_execution_adapter.py`, `backend/tests/evaluation/test_oracle_execution_adapter_integration.py`. *(PR #164)* |
| #165 | **(29.4) Oracle Docker/Test Harness Strategy** (2026-08-27) — 29.3'ün Docker'sız her zaman safe-skip eden `oracle`-mark'lı gated entegrasyon testlerine gerçek bir local-Docker imajı + seed şeması + ayrı CI job'ı ekleyen sprint — 29.3'ün üç execution modülüne (`oracle_adapter.py`/`oracle_connection_resolver.py`/`oracle_execution_adapter.py`) **HİÇ DOKUNULMADI** (PostgreSQL 29.0 desenini izler). İmaj seçimi `gvenzl/oracle-free:23-slim-faststart` (29.3 resolver varsayılanlarıyla — `FREEPDB1`/1521/`sqlgen` — birebir örtüşür). Yeni `backend/tests/fixtures/oracle/seed.sql`: Oracle dialect (`NUMBER`/`VARCHAR2`/`TO_TIMESTAMP`), 29.0 Postgres seed'inin şeklini birebir yansıtan 5 tablo — `customers`/`orders`/`order_items` (tek-kolon PK/FK) + `product_variants`/`variant_stock` (composite PK/FK, `(product_id, sku)`), 18 statement (satır başı tek `INSERT`). Yeni saf `backend/app/evaluation/oracle_seed.py`: `split_statements()` + `apply_seed(conn, sql)` (driver-izole çekirdek) + lazy-`oracledb` `main()` (yalnız `main()` içinde import edilir; `python -m app.evaluation.oracle_seed` ile CI'dan çağrılır) — bilinçli olarak `app/evaluation/__init__.py`'den re-export edilmedi (yalnız harness/CI-amaçlı). Yeni `backend/tests/evaluation/test_oracle_seed.py`: 7 unit test (gerçek seed'in 18-statement parse'ı + driver-izolasyon subprocess testi dahil). `backend/tests/evaluation/test_oracle_execution_adapter.py`'ye TECH-DEBT §21.4'ü kapatan fake-`oracledb` bridge→low-level unit testi eklendi (resolver-non-`None` delegasyon yolu artık Docker'sız da kilitli). Yeni `backend/tests/evaluation/test_oracle_execution_adapter_seeded_integration.py`: seeded canlı entegrasyon testleri (3-tablo `JOIN`, composite-FK `JOIN`, `max_rows` truncation) — Docker'sız safe-skip. `docker-compose.yml`'e yeni `oracle` servisi (`gvenzl/oracle-free:23-slim-faststart`, `FREEPDB1`, `APP_USER=sqlgen`, seed `/container-entrypoint-initdb.d/`'e mount, `healthcheck.sh`). `.github/workflows/backend-ci.yml`'e mevcut (değişmeyen) `backend-tests` job'ından **ayrı, additive** yeni `oracle-integration` job'ı: Oracle service container + `python -m app.evaluation.oracle_seed` ile seed + `pytest -m oracle` (artık canlı çalışır). TECH-DEBT §21.1 ve §21.4 ÇÖZÜLDÜ. **Bilinçli kapsam dışı (TECH-DEBT §21'in kalan kalemleri, değişmeden, sessiz düşürme yok):** Oracle schema introspection (§21.2 → ileriye), Oracle EXPLAIN-only (§21.3 → ileriye), connection registry/uzak & production connection/wallet/TNS (§21.5 → 30.2), canlı HTTP pipeline wiring/merkezî execution router registry (§21.6 → 30.x), `oracledb` thick mode/Instant Client (§21.7), deferred minor'lar (§21.8). 29.3'ün 3 execution modülünde diff BOŞ (doğrulandı). CI golden-gate kontrat testi (`tests/test_golden_eval_ci_gate_contract.py::test_backend_ci_golden_gate_does_not_use_secrets_or_network_env`) "Run golden eval profile" satırından dosya sonuna kadar tarayıp `env:`/`secrets` arıyordu (tek job'a scope'lu değildi); yeni `oracle-integration` job'ının kendi `env:` bloklarına (Oracle bağlantı ayarları — gerçek secret değil) bulaşıp yanlış-pozitif FAIL veriyordu — testin tarama sınırı "Run golden eval profile" ile bir sonraki üst-seviye job header'ı arasına daraltılarak (`backend-tests` job'una scope'lu) düzeltildi, bkz. TECH-DEBT §22 ÇÖZÜLDÜ. Full suite 2690 passed/31 skipped (skip +3 vs 29.3'ün 28'i — yeni seeded-integration testleri). Spec: `docs/superpowers/specs/2026-08-27-sprint-29.4-oracle-docker-test-harness-strategy-design.md`; Plan: `docs/superpowers/plans/2026-08-27-sprint-29.4-oracle-docker-test-harness.md`. `backend/tests/fixtures/oracle/seed.sql`, `backend/app/evaluation/oracle_seed.py`, `backend/tests/evaluation/test_oracle_seed.py`, `backend/tests/evaluation/test_oracle_execution_adapter.py`, `backend/tests/evaluation/test_oracle_execution_adapter_seeded_integration.py`, `backend/tests/test_golden_eval_ci_gate_contract.py`, `docker-compose.yml`, `.github/workflows/backend-ci.yml`. *(PR #165)* |
| #166 | **(29.5) MySQL Adapter Contract** (2026-08-28) — Oracle 29.3'ün (Contract Adapter) ve 29.4'ün (Docker/Test Harness) ikisini TEK sprint'te birleştiren sprint (MySQL, Oracle'a göre daha düşük riskli — saf-Python driver, küçük imaj, lisans sorunu yok); 25.9'un aksine ön-var bir MySQL stub'ı YOKTU, sıfırdan başladı. `backend/app/evaluation/multi_database_execution.py`'ye yeni `SQLDatabaseDialect.MYSQL = "mysql"` (TEK satır, başka davranış değişikliği yok — `git diff 24f9679..HEAD -- backend/app/evaluation/multi_database_execution.py` ile doğrulandı) + `backend/requirements.txt`'e `PyMySQL==1.1.1` pin + `backend/pytest.ini`'ye `mysql` marker. Üç katman (Postgres 29.0→29.1 / Oracle 29.3 desenini izler). **Katman 1** (yeni `backend/app/evaluation/mysql_adapter.py`): `SQLMySQLAdapterContract` — connection yoksa tamamen inert (driver import yok, deterministik `NOT_IMPLEMENTED`; unsafe SQL her koşulda deterministik `REJECTED`), `SQLMySQLLocalDockerConnection(host, port, database, user, password, environment)` (Oracle'ın `service_name`'i yerine MySQL'in düz `database` parametresi; host allowlist `localhost`/`127.0.0.1`/`::1`) verilince gerçek `pymysql` read-only SELECT path'i: `SET SESSION max_execution_time = <ms>` (Oracle'ın `call_timeout`'una denk MySQL-özgü zaman aşımı mekanizması) → `START TRANSACTION READ ONLY` → execute → `max_rows+1` fetch (truncation tespiti) → `finally`'de guarded `rollback()`+`close()`; secret-free `_sanitize_mysql_error` (timeout error code'ları 3024/1969/2013 ayrı deterministik mesaj). **Katman 2** (yeni `backend/app/evaluation/mysql_connection_resolver.py`): `resolve_local_docker_connection(env)` — `MYSQL_TEST_HOST/PORT/DB/USER/PASSWORD` (default `localhost`/`3306`/`sqlgen_test`/`sqlgen`/`sqlgen`) → `SQLMySQLLocalDockerConnection | None`; asla connect etmez, asla raise etmez. **Katman 3** (yeni `backend/app/evaluation/mysql_execution_adapter.py`): dialect-agnostic `SQLDatabaseExecutionAdapter` köprüsü `MySQLDatabaseExecutionAdapter` — `capabilities() = (CONNECTION_REF, READ_ONLY)` (**EXPLAIN_ONLY YOK**, bu kontrat sürümünde reddedilir), `connection_ref` zorunlu + `fixture_ref` reddedilir, resolver `None` → graceful boş `SQLDatabaseExecutionResult` + `execution_error` (raise yok); `normalize_mysql_execution_result` + `_rows_to_dicts` (arity uyuşmazlığı/boş columns → `col_<i>` fallback + uyarı). Oracle'ın §21.4'ünü kapatan testin dengi **baştan** eklendi (Docker gerektirmeden): `test_execute_with_resolved_connection_delegates_to_low_level` (fake-`pymysql` modülü + enjekte edilmiş resolver ile bridge→low-level delegasyon yolu kilitlendi). Yeni deterministik `backend/tests/fixtures/mysql/seed.sql`: MySQL/InnoDB dialect (`ENGINE=InnoDB`, **küçük harf** tablo/kolon adları), Postgres 29.0 / Oracle 29.4 seed'lerinin şeklini birebir yansıtan 5 tablo — `customers`/`orders`/`order_items` (tek-kolon PK/FK) + `product_variants`/`variant_stock` (composite PK/FK, `(product_id, sku)`), 18 statement (5 `CREATE TABLE` + 13 `INSERT`). Yeni saf `backend/app/evaluation/mysql_seed.py`: `split_statements()` + `apply_seed(conn, sql)` (driver-izole çekirdek, `oracle_seed.py`'yi birebir yansıtır) + lazy-`pymysql` `main()` (yalnız `main()` içinde import edilir; `python -m app.evaluation.mysql_seed` ile CI'dan çağrılır) — bilinçli olarak `app/evaluation/__init__.py`'den re-export edilmedi (yalnız harness/CI-amaçlı, production yüzeyine sızmaz). Yeni seeded entegrasyon testleri (Docker'sız safe-skip, `integration`+`mysql`-mark'lı): 3-tablo `JOIN`, composite-FK `JOIN`, `max_rows` truncation — MySQL büyük/küçük harf duyarsız kolon adlarını korur, dict-row anahtarları **küçük harf** (seed şemasıyla birebir). `docker-compose.yml`'e yeni `mysql` servisi (`mysql:8.4`, `sqlgen`/`sqlgen`/`sqlgen_test`, seed init-script mount, healthcheck) + `.github/workflows/backend-ci.yml`'e mevcut (değişmeyen) `backend-tests`/`oracle-integration` job'larından **ayrı, additive** yeni `mysql-integration` job'ı: MySQL service container + `python -m app.evaluation.mysql_seed` ile seed + `pytest -m mysql` (canlı çalışır). `git diff 24f9679..HEAD` doğrulandı: `multi_database_execution.py` diff'i yalnız `MYSQL = "mysql"` satırı. Full suite: 2724 passed/34 skipped (skip sayısı 29.4'ün 31'inden +3 — yeni seeded-integration testleri). **Bilinçli kapsam dışı (TECH-DEBT §23, sessiz düşürme yok):** MySQL schema introspection (`information_schema`; Postgres 29.0 dengi → ileriye), MySQL EXPLAIN-only mode (29.2 dengi → ileriye), canlı HTTP pipeline wiring/merkezî execution router registry (→ 30.x), connection registry/uzak & production connection/TLS (→ Phase 11 · 30.2), C-tabanlı driver'lar (`mysqlclient`/`mysql-connector-python`)/thick mode (yalnız saf-Python `PyMySQL`). Spec: `docs/superpowers/specs/2026-08-27-sprint-29.5-mysql-adapter-contract-design.md`; Plan: `docs/superpowers/plans/2026-08-27-sprint-29.5-mysql-adapter-contract.md`. `backend/app/evaluation/mysql_adapter.py`, `backend/app/evaluation/mysql_connection_resolver.py`, `backend/app/evaluation/mysql_execution_adapter.py`, `backend/app/evaluation/mysql_seed.py`, `backend/app/evaluation/__init__.py`, `backend/app/evaluation/multi_database_execution.py`, `backend/pytest.ini`, `backend/requirements.txt`, `backend/tests/fixtures/mysql/seed.sql`, `backend/tests/evaluation/test_mysql_adapter.py`, `backend/tests/evaluation/test_mysql_connection_resolver.py`, `backend/tests/evaluation/test_mysql_dialect_registered.py`, `backend/tests/evaluation/test_mysql_execution_adapter.py`, `backend/tests/evaluation/test_mysql_execution_adapter_seeded_integration.py`, `backend/tests/evaluation/test_mysql_seed.py`, `docker-compose.yml`, `.github/workflows/backend-ci.yml`. *(PR #166)* |
| #167 | **(29.6) SQL Server Adapter Contract** (2026-08-28) — MySQL 29.5'in üç-katmanlı desenini SQL Server'a taşıyan sprint (25.9'un aksine ön-var bir MSSQL stub'ı YOKTU, sıfırdan başladı). `backend/app/evaluation/multi_database_execution.py`'ye yeni `SQLDatabaseDialect.SQLSERVER = "sqlserver"` (TEK satır, başka davranış değişikliği yok — `git diff cf7f3bb..HEAD -- backend/app/evaluation/multi_database_execution.py` ile doğrulandı) + `backend/requirements.txt`'e `pymssql==2.3.2` pin + `backend/pytest.ini`'ye `sqlserver` marker. **SQL Server'ın 29.0-29.5'ten temel farkı — read-only bir transaction pre-statement'ı YOK:** diğer üç dialect `SET TRANSACTION READ ONLY`/`START TRANSACTION READ ONLY` gibi bir ifadeyle read-only'yi DB-seviyesinde zorlarken, SQL Server'da read-only **`db_datareader`-only bir login** ile enforce edilir; adapter connection'ı açıp doğrulanmış SQL'i doğrudan çalıştırır, önüne hiçbir pre-statement eklemez. Üç katman (Postgres 29.0→29.1 / Oracle 29.3 / MySQL 29.5 desenini izler). **Katman 1** (yeni `backend/app/evaluation/mssql_adapter.py`): `SQLMSSQLAdapterContract` — connection yoksa tamamen inert (driver import yok, deterministik `NOT_IMPLEMENTED`; unsafe SQL her koşulda deterministik `REJECTED`), `SQLMSSQLLocalDockerConnection(host, port, database, user, password)` (host allowlist `localhost`/`127.0.0.1`/`::1`) verilince gerçek `pymssql` read-only SELECT path'i: connection **connect anında** `login_timeout`/`timeout` kwarg'larıyla query timeout uygulanır (Oracle'ın `call_timeout`/MySQL'in `max_execution_time` session-ifadesine denk, ama SQL Server'da connect-kwarg mekanizması) → execute (pre-statement YOK) → `max_rows+1` fetch (truncation tespiti) → her zaman `rollback()` (backstop) + `close()`; secret-free `_sanitize_mssql_error` (pymssql/DB-Library timeout hata kodları 20003/20004 ayrı deterministik mesaj). **Katman 2** (yeni `backend/app/evaluation/mssql_connection_resolver.py`): `resolve_local_docker_connection(env)` — `MSSQL_TEST_HOST/PORT/DB/USER/PASSWORD` (default `localhost`/`1433`/`sqlgen_test`/`sqlgen_ro`/`Sqlgen_Ro0!Pass`) → `SQLMSSQLLocalDockerConnection | None`; asla connect etmez, asla raise etmez. **Katman 3** (yeni `backend/app/evaluation/mssql_execution_adapter.py`): dialect-agnostic `SQLDatabaseExecutionAdapter` köprüsü `MSSQLDatabaseExecutionAdapter` — `capabilities() = (CONNECTION_REF, READ_ONLY)` (**EXPLAIN_ONLY YOK**, bu kontrat sürümünde reddedilir), resolver `None` → graceful boş `SQLDatabaseExecutionResult` + `execution_error` (raise yok); `_rows_to_dicts` (arity uyuşmazlığı → `col_<i>` fallback + uyarı). Oracle §21.4/MySQL denginin aynısı **baştan** eklendi (Docker gerektirmeden): `test_execute_with_resolved_connection_delegates_to_low_level` (fake-`pymssql` modülü + enjekte edilmiş resolver ile bridge→low-level delegasyon yolu kilitlendi). Yeni deterministik `backend/tests/fixtures/mssql/seed.sql`: SQL Server dialect (`NVARCHAR`/`DATETIME2`, **küçük harf** tablo/kolon adları), Postgres/Oracle/MySQL seed'lerinin şeklini birebir yansıtan 5 tablo — `customers`/`orders`/`order_items` (tek-kolon PK/FK) + `product_variants`/`variant_stock` (composite PK/FK), 18 statement. Yeni saf `backend/app/evaluation/mssql_seed.py`: `split_statements()` + `apply_seed(conn, sql)` (driver-izole çekirdek, `mysql_seed.py`'yi birebir yansıtır) + saf, test edilebilir `readonly_login_ddl(login, password, database)` DDL builder'ı + lazy-`pymssql` `main()` — SQL Server diğer üç dialect'ten farklı olarak **ekstra orkestrasyon** gerektirir: seed database'i baştan YOK ve seed'den SONRA ayrı bir `db_datareader`-only login/user çifti provision edilmelidir; `main()` önce `master`'a admin bağlantıyla DB+login'i oluşturur, sonra `sqlgen_test`'e bağlanıp seed'i uygular + user'ı oluşturup `db_datareader` grant eder; container başlangıç gecikmesi için connect-retry döngüsü içerir — bilinçli olarak `app/evaluation/__init__.py`'den re-export edilmedi. Yeni seeded entegrasyon testleri (Docker'sız safe-skip, `sqlserver`-mark'lı): 3-tablo `JOIN`, composite-FK `JOIN`, `max_rows` truncation — dict-row anahtarları **küçük harf**. `docker-compose.yml`'e yeni `mssql` servisi (`mcr.microsoft.com/mssql/server:2022-latest`) + `.github/workflows/backend-ci.yml`'e mevcut (değişmeyen) `backend-tests`/`oracle-integration`/`mysql-integration` job'larından **ayrı, additive** yeni `sqlserver-integration` job'ı: MSSQL service container + `python -m app.evaluation.mssql_seed` ile seed+login provisioning + `pytest -m sqlserver` (canlı çalışır). `git diff cf7f3bb..HEAD` doğrulandı: `multi_database_execution.py` diff'i yalnız `SQLSERVER = "sqlserver"` satırı. Full suite: 2760 passed/37 skipped (skip sayısı 29.5'in 34'ünden +3 — yeni seeded-integration testleri). **Bilinçli kapsam dışı (TECH-DEBT §24, sessiz düşürme yok):** SQL Server schema introspection (`INFORMATION_SCHEMA`/`sys.*`; Postgres 29.0 dengi → ileriye), SQL Server EXPLAIN-only/SHOWPLAN (29.2 dengi → ileriye), canlı HTTP pipeline wiring/merkezî execution router registry (→ 30.x), connection registry/uzak & production connection/TLS/Azure AD (→ Phase 11 · 30.2), `pyodbc`/ODBC driver path (yalnız saf-Python `pymssql`). **Bununla Phase 10 (Real Database Adapter Layer) dört dialect'in (Postgres/Oracle/MySQL/SQL Server) tümünde contract-first read-only execution adapter'ına sahip.** Spec: `docs/superpowers/specs/2026-08-28-sprint-29.6-sqlserver-adapter-contract-design.md`; Plan: `docs/superpowers/plans/2026-08-28-sprint-29.6-sqlserver-adapter-contract.md`. `backend/app/evaluation/mssql_adapter.py`, `backend/app/evaluation/mssql_connection_resolver.py`, `backend/app/evaluation/mssql_execution_adapter.py`, `backend/app/evaluation/mssql_seed.py`, `backend/app/evaluation/__init__.py`, `backend/app/evaluation/multi_database_execution.py`, `backend/pytest.ini`, `backend/requirements.txt`, `backend/tests/fixtures/mssql/seed.sql`, `backend/tests/evaluation/test_mssql_adapter.py`, `backend/tests/evaluation/test_mssql_connection_resolver.py`, `backend/tests/evaluation/test_mssql_dialect_registered.py`, `backend/tests/evaluation/test_mssql_execution_adapter.py`, `backend/tests/evaluation/test_mssql_execution_adapter_seeded_integration.py`, `backend/tests/evaluation/test_mssql_seed.py`, `docker-compose.yml`, `.github/workflows/backend-ci.yml`. *(PR #167 — backfilled after PR opens)* |
| #168 | **(29.7) Adapter Conformance Eval Suite** (2026-09-02) — 29.0–29.6'da dört dialect'e (Postgres/Oracle/MySQL/SQL Server) ayrı ayrı, birbirini yansıtan (ama kopya) şekilde inşa edilmiş execution adapter'larını **tek bir profil kaydı** altında birleştiren sprint — hâlâ standalone/inert, canlı production pipeline'a wire edilmedi; `backend/app/evaluation/multi_database_execution.py` **DEĞİŞMEDİ** (`git diff --stat main..HEAD -- backend/app/evaluation/multi_database_execution.py` boş, doğrulandı). Yeni `backend/app/evaluation/adapter_conformance.py`: `AdapterConformanceProfile` dataclass (`dialect`/`adapter_factory`/`inert_adapter_factory`/`resolver`/`expected_capabilities`/`case_fold`/`is_connection_based`/`pytest_marker`/`driver_module_name`/`adapter_module_name`/`accepts_explain_only`/`seed_fixture_path`) + `CONFORMANCE_PROFILES` (5 dialect — sqlite dahil, tek gerçek kaynak) + `profiles_for(connection_based=...)` (4 connection-based / 1 fixture-based sqlite bölümlemesi) + `CaseFold`/`fold()` (küçük-harf seed şemalarını normalize eden saf helper). Docker-free **deterministik conformance çekirdeği** (marker'sız, `backend-tests` job'ında koşar): her profil için dialect eşleşmesi, `capabilities()` beklenen kümeyle birebir, tüm 5 adapter tek `SQLDatabaseExecutionRouter`'a kayıt olabiliyor, connection-based 4 dialect'in inert adapter'ı `execute()`'ta raise ETMEDEN graceful boş sonuç + `execution_error` döner, `EXPLAIN_ONLY` tutarlılığı (yalnız Postgres kabul eder — 29.2; diğer üçü `SQLMultiDatabaseExecutionContractError` fırlatır — 29.3/29.5/29.6), `contract_conformance_report(profile)` şekli, `assert_contract_conformance(profile) -> list` violation listesi. Import-time driver-izolasyonu profil-güdümlü hale geldi (`test_adapter_module_import_is_driver_isolated`, `profiles_for(connection_based=True)` üzerinden parametrized) — 29.1/29.3/29.5/29.6'nın birbirini yansıtan dört ayrı subprocess testinin yerini alıyor. Modülün kendisinin de sürücü yüklemediğini kilitleyen ayrı subprocess testi `test_importing_module_loads_no_db_driver`: ilk sürüm bunu in-process `sys.modules` kontrolüyle yapıyordu; `sys.modules` process-global olduğundan legacy `app/schema_manager.py`'nin koşulsuz `oracledb`/`psycopg2` import'uyla kirlenen full-suite sıralamasında yanlış-FAIL veriyordu (Task 6'da yakalandı ve düzeltildi) — mevcut `test_adapter_module_import_is_driver_isolated` deseniyle birebir aynı taze-subprocess yaklaşımına çevrildi (`app/schema_manager.py`'ye DOKUNULMADI — legacy koşulsuz driver import'u bilinçli kapsam dışı tech-debt). Yeni **konsolide canlı execution matrix** `backend/tests/evaluation/test_adapter_conformance_matrix.py`: dört connection-based dialect'in 29.1/29.3/29.5/29.6'daki üç kopya seeded-integration test dosyasını (`test_oracle_execution_adapter_seeded_integration.py`, `test_mysql_execution_adapter_seeded_integration.py`, `test_mssql_execution_adapter_seeded_integration.py` — **silindi**) emekli edip tek parametrized dosyaya indiren, `profile.pytest_marker` ile CI job-bazlı slicing'i koruyan (`pytest -m postgres|oracle|mysql|sqlserver` hâlâ çalışır) matrix — 3-tablo `JOIN`, composite-FK `JOIN`, `max_rows` truncation, dict-row anahtarları `profile.case_fold`'a göre normalize; Postgres'in kendi test dosyası (`test_postgres_execution_adapter_integration.py`) yalnız matrix'in KAPSAMADIĞI EXPLAIN-only + `statement_timeout` testlerine trim edildi (silinmedi — 29.2'nin EXPLAIN path'i matrix'in genel şeklinin dışında). Yeni **rapor emitter'ı** `python -m app.evaluation.adapter_conformance report [--json]`: `build_conformance_report()` → 5 dialect'in `contract_conformance_report()` çıktısını tek JSON/insan-okunur tabloya toplayan, Docker/DB gerektirmeyen manuel gözlemlenebilirlik CLI'ı (canlı pass/skip verisi bilinçli olarak dışarıda — yalnız kontrat-conformance); `report --json` smoke: `len(d['dialects'])==5` doğrulandı. Full suite: 2804 passed/37 skipped/0 failed (29.6'nın 2760'ından net +44 — 3 kopya seeded-integration dosyasının silinmesiyle kaybolan testlerin yerini deterministik çekirdek + matrix + rapor testleri fazlasıyla dolduruyor). **Bilinçli kapsam dışı (TECH-DEBT §25, sessiz düşürme yok):** SQLite canlı seeded matrix (yalnız deterministik çekirdek — fixture-based olduğundan Docker-seeded battery yok), rapor emitter'ının CI artifact'ı olması / canlı+kontrat birleşik rapor (→ ileri sprint), conformance'ın sert bir gate olması (ayrı gate CLI/CI step — mevcut dialect-başına job'lar dolaylı gate), NL2SQL doğruluk conformance'ı (kapsam dışı — bu suite yalnız execution-contract conformance'ı). **Bununla Phase 10 (Real Database Adapter Layer) dört dialect'in tümünde hem contract-first execution adapter'ına HEM de tek-kaynaklı conformance doğrulamasına sahip — Phase 10 TAMAMLANDI.** Spec: `docs/superpowers/specs/2026-09-02-sprint-29.7-adapter-conformance-eval-design.md`; Plan: `docs/superpowers/plans/2026-09-02-sprint-29.7-adapter-conformance-eval.md`. `backend/app/evaluation/adapter_conformance.py`, `backend/tests/evaluation/test_adapter_conformance.py`, `backend/tests/evaluation/test_adapter_conformance_matrix.py`, `backend/tests/evaluation/test_adapter_conformance_report.py`, `backend/tests/evaluation/test_postgres_execution_adapter_integration.py`, `docs/TECH-DEBT.md`, `docs/PLANNED-SPRINTS.md`, `docs/SPRINT-PR-LOG.md`, `CHANGELOG.md`, `ROADMAP.md`. *(PR #168 — backfilled after PR opens)* |

## Phase 11 — API / Backend Productization · Sprint 30.x · 2026-09-06 → …
**Amaç:** Internal backend'i product-grade public/backend API yüzeyine dönüştürmek.

| Adım | Açıklama |
|---|---|
| #169 | **(30.0) Public Query API Contract** (2026-09-06) — Phase 11'in (API/Backend Productization) İLK sprint'i ve "Contract" sprint'i: sonraki tüm 30.x yüzeylerinin (Workspace/Connection Registry/Schema Sync/Query Run/History/Feedback/Admin/Auth) uyacağı **kesişen API konvansiyonunu** kuran, yalnız konvansiyon katmanı — iş endpoint'i EKLEMEZ, mevcut davranışı DEĞİŞTİRMEZ (29.x inert stili: tek-kaynak sözleşme + conformance guard; mevcut 20+ `/api/*` endpoint grandfathered). Yeni `backend/app/api/contract.py`: kanonik generic `ApiResponse[T]` = `{status: Literal["success"], data: T, meta: ResponseMeta | None}` (tek `data` anahtarı — kaynak-adlı `job`/`feedback` değil), `PageMeta` (`limit`/`offset`/`count`)/`ResponseMeta` (offset pagination), `API_VERSION="v1"`/`API_V1_PREFIX="/api/v1"`/`REQUEST_ID_HEADER="X-Request-ID"` sabitleri, frozen & **hashable** `ApiContract`/`API_CONTRACT` descriptor (tüm alanlar immutable tuple — review sonrası mutable dict alan tuple'a çevrildi) + saf `contract_descriptor() -> dict`; mevcut `ErrorResponse`/`ErrorBody` (`{"error":{code,message,details}}`, 11.2/27.2) **re-export** edilir, yeniden tanımlanmaz. Yeni `backend/app/api/v1_meta.py` public `GET /api/v1` discovery endpoint'i: `ApiResponse[ContractDescriptor]` içinde makine-okunur sözleşmeyi kanonik zarfla döner (29.7'nin rapor-emitter-demonstratör deseninin endpoint hâli); auth dependency YOK, yan etki YOK, deterministik — conformance guard'ın uyguladığı TEK route. `backend/app/main.py`'ye tek `include_router(v1_meta_router)` satırı (additive). Yeni deterministik, Docker-free, **marker'sız** conformance guard `backend/tests/api/test_public_api_contract_conformance.py` (`backend-tests` job'ında her CI run'da koşar): `/api/v1` route set'i = bilinen-conforming `EXPECTED_V1_ROUTES` allowlist'i (allowlist dışı yeni v1 route → FAIL — negatif alt-süreç kontrolüyle kanıtlandı; `startswith` yerine segment-sınırı eşleşmesi → `/api/v1beta`/`/api/v10` yakalanmaz), her v1 route'un başarı gövdesi OpenAPI şemasında kanonik `{status,data,meta}` zarfı (robust 2xx-envelope resolution — malformed/undeclared response'ta KeyError yerine temiz assertion), kanonik hata handler'ları kayıtlı + gerçek `HTTPException` yolunda (`GET /api/jobs` API-key'siz → 403) `{error:{code,message,details}}` + `X-Request-ID`, descriptor ↔ modeller senkron (`required_error_codes` ↔ `errors.py::STATUS_TO_CODE` drift testi + `ContractDescriptor` ↔ `contract_descriptor()` anahtar drift testi). Legacy `/api/*` grandfathered — mevcut `tests/test_api_surface.py`+`openapi_snapshot.json` route-ekleme tripwire'ı olduğundan guard ~40 legacy route'u enumerate etmez (planlamada keşfedilen daha az kırılgan refinement). **`GET /api/v1` bilinçli public** — yalnız statik sözleşme descriptor'ı (envelope alan adları, hata kodları) döner, zaten public `/openapi.json` & `/docs`'tan daha az sızdırır; iki security-contract allowlist'inde (`test_public_endpoint_allowlist` exact-match + `test_protected_endpoints_require_api_key`) bless edildi. `backend/app/api/errors.py`'de `STATUS_TO_CODE` sabiti expose edildi (additive extraction, `map_status_to_code` davranışı birebir aynı). Contract snapshot'ları (`openapi_snapshot`/`schema_contract_snapshot`/`status_contract_snapshot`) `python scripts/generate_contract_snapshots.py` ile additive-only regenerate (yeni `/api/v1` path + `ApiResponse_ContractDescriptor_`/`ContractDescriptor`/`PageMeta`/`ResponseMeta` component şemaları; 0 silme, hiç hand-edit yok — "blind blessing" değil). Kod diff'i tamamen additive (2 yeni modül + 1 `include_router` satırı + additive `STATUS_TO_CODE`; **0 silme**). Whole-branch review (opus): 0 runtime correctness bug; 5 drift/robustness/cleanup bulgusu — **hepsi giderildi** (frozen-dataclass immutability/hashability, error-code drift sync testi, segment-boundary v1-path eşleşmesi, robust envelope resolution, descriptor-model anahtar drift testi). Full suite: 2825 passed/37 skipped (tek local `test_pagerank_missing_scipy_does_not_log_error` fail'i pre-existing, environment-only — testin "scipy yok" varsayımı bu venv'de yanlış; 30.0'a dokunulmadı, `main`'e göre boş diff, CI'da scipy yok → geçer). **Bilinçli kapsam dışı (TECH-DEBT §26, sessiz düşürme yok):** legacy `/api/*` migrasyonu (kanonik `data` envelope'a), legacy path deprecation/sunset, route-miss 404 kanonikleştirme (bilinmeyen path'e 404 hâlâ Starlette default `{"detail":...}`), cursor pagination + sayfa-üstü `total`, `app/errors/` domain taksonomisinin HTTP hata `code`'una eşlenmesi, AuthN (30.8)/rate-limiting/quota/request-signing, OpenAPI doc publishing/SDK üretimi, somut `POST /api/v1/query` iş endpoint'i (→ ileri sprint'ler, bu sözleşmenin üstüne). **Bununla Phase 11 (API/Backend Productization) başladı.** Spec: `docs/superpowers/specs/2026-09-06-sprint-30.0-public-query-api-contract-design.md`; Plan: `docs/superpowers/plans/2026-09-06-sprint-30.0-public-query-api-contract.md`. `backend/app/api/contract.py`, `backend/app/api/v1_meta.py`, `backend/app/api/errors.py`, `backend/app/main.py`, `backend/tests/api/test_api_contract_models.py`, `backend/tests/api/test_v1_meta_endpoint.py`, `backend/tests/api/test_public_api_contract_conformance.py`, `backend/tests/test_security_contract.py`, `backend/tests/contracts/openapi_snapshot.json`, `backend/tests/contracts/schema_contract_snapshot.json`, `docs/TECH-DEBT.md`, `docs/PLANNED-SPRINTS.md`, `docs/SPRINT-PR-LOG.md`, `CHANGELOG.md`, `ROADMAP.md`. *(PR #169)* |
| #170 | **(30.1) Workspace API** (2026-09-06) — Phase 11'in ikinci sprint'i: standalone bir `workspaces` kaynağı (`/api/v1/workspaces` CRUD) ekler — 30.0 kanonik sözleşmesine (`ApiResponse` envelope + offset `PageMeta`) uyan **İLK gerçek route ailesi**, dolayısıyla 30.0 conformance guard'ını da dogfood eder. Backend şu an tek-tenant/global; workspace, sonraki Phase 11 kaynaklarının (connections/schema/query-run/history) ait olacağı üst-düzey container. 30.1 onu yeni/standalone ekler, mevcut global tabloları (`jobs`/`feedback`/`query_traces`/`sql_cache`) **retro-scope ETMEZ**. Yeni `workspaces` tablosu `backend/app/database.py::init_db`'ye additive `CREATE TABLE IF NOT EXISTS` ile eklendi (`id TEXT PK`, `name TEXT NOT NULL`, `slug TEXT NOT NULL UNIQUE`, `description TEXT`, `created_at`/`updated_at TEXT NOT NULL`). Yeni tek-sorumluluklu `backend/app/workspace_repository.py`: saf `slugify(name)` (lowercase, non-alfanumerik run'lar → tek `-`, trim; boş → uuid fallback, slug hep non-empty & unique), `create_workspace`/`get_workspace`/`get_workspace_by_slug`/`list_workspaces(limit,offset, ORDER BY created_at DESC,id)`/`update_workspace`/`delete_workspace` + `WorkspaceSlugConflict`; UPDATE `SET`'i `_UPDATABLE_WORKSPACE_COLUMNS=("name","description")` whitelist'inden kurulur (kullanıcı girdisi kolon adına dönüşmez — `database.py::_UPDATABLE_JOB_COLUMNS` desenini yansıtır); slug 30.1'de değiştirilemez; duplicate slug tek race-free guard olan UNIQUE(slug) + `sqlite3.IntegrityError`→`WorkspaceSlugConflict` ile yakalanır. Yeni `backend/app/api/workspaces.py`: `verify_api_key` korumalı `APIRouter(prefix="/api/v1/workspaces")`, pydantic `WorkspaceCreate`(name 1..200, opsiyonel `slug` pattern `^[a-z0-9]+(-[a-z0-9]+)*$`, `description` ≤2000)/`WorkspaceUpdate`(en az bir alan — `model_validator`)/`WorkspaceResponse`. 5 endpoint, hepsi kanonik `ApiResponse` zarfı: `POST ""`(201, slug verilmezse `slugify(name)`, `WorkspaceSlugConflict`→`HTTPException(409)`), `GET ""`(`limit` default 50/1..200, `offset`≥0 → `ApiResponse[list[WorkspaceResponse]]` + `meta.pagination=PageMeta{limit,offset,count}`), `GET/PATCH/DELETE /{workspace_id}`(yok→404); DELETE silinen kaynağı döndürür ki her 2xx gövde kanonik `{status,data,meta}` kalsın (204 değil) ve `delete_workspace()` bool dönüşüyle gate'lenir (concurrent delete → stale-200 yerine 404). `backend/app/main.py`'ye 1 import + 1 `include_router(workspaces_router)` (additive). **Additive `409 CONFLICT`**: `backend/app/api/errors.py::STATUS_TO_CODE`'a `409:"CONFLICT"` + `backend/app/api/contract.py::_REQUIRED_ERROR_CODES`'a `"CONFLICT"` eklendi (409 önceden generic `HTTP_ERROR`'a düşüyordu; `map_status_to_code`'un diğer davranışı birebir aynı); 30.0 guard'ının `test_required_error_codes_match_http_handler_mapping` drift-sync testi ikisini hizalı tutar, `GET /api/v1` descriptor'ı artık `CONFLICT` reklamlar — 30.0 sözleşmesinin additive genişletilebilirliğini gösterir. `backend/tests/api/test_public_api_contract_conformance.py::EXPECTED_V1_ROUTES` 5 workspace route'uyla genişletildi (dogfood — mevcut `test_every_v1_route_success_uses_canonical_envelope` her v1 route'un başarı gövdesini kanonik zarfa zorlar; allowlist dışı yeni v1 route hâlâ FAIL). Contract snapshot'ları (`openapi_snapshot`/`schema_contract_snapshot`/`status_contract_snapshot`) `python scripts/generate_contract_snapshots.py` ile additive-only regenerate (yeni `/api/v1/workspaces` + `/api/v1/workspaces/{workspace_id}` path'leri + `WorkspaceCreate`/`WorkspaceUpdate`/`WorkspaceResponse`/`ApiResponse_WorkspaceResponse_`/`ApiResponse_list_WorkspaceResponse__` component şemaları; 0 silme, hand-edit yok). Mevcut hiçbir tablo/endpoint/handler davranışı DEĞİŞMEDİ (yalnız additive); workspace route'ları korumalı → `test_protected_endpoints_require_api_key` security-allowlist değişikliği gerektirmedi (403 without key). Full suite 2844 passed/37 skipped (tek local `test_pagerank_missing_scipy_does_not_log_error` fail'i pre-existing environment-only — testin "scipy yok" varsayımı bu venv'de yanlış; 30.1'e dokunulmadı, `main`'e göre boş diff, CI'da scipy yok → geçer). Whole-branch review (opus): 0 crash bug, 6 bulgu — 3 giderildi (atomik DELETE gating; gereksiz slug pre-check kaldırıldı; kullanılmayan `count_workspaces` düşürüldü — `total`/cursor pagination §27'de ertelendi, YAGNI), 3 gerekçeyle atlandı (explicit `conn.commit()` + `conn.close()` yokluğu `database.py` codebase idiom'uyla tutarlı; PATCH `None`=omit `update_job_status` semantiğiyle aynı). **Bilinçli kapsam dışı (TECH-DEBT §27, sessiz düşürme yok):** global tabloların `workspace_id`'ye scope edilmesi (jobs/feedback/traces/cache hâlâ global), connection/schema/history'nin workspace'e bağlanması (30.2+), workspace membership/rol/per-workspace izin (AuthN 30.8), slug mutasyonu/soft-delete/arşivleme/workspace-ayar/kota/name-uniqueness, cursor pagination + sayfa-üstü `total`, delete cascade semantiği. Spec: `docs/superpowers/specs/2026-09-06-sprint-30.1-workspace-api-design.md`; Plan: `docs/superpowers/plans/2026-09-06-sprint-30.1-workspace-api.md`. `backend/app/database.py`, `backend/app/workspace_repository.py`, `backend/app/api/workspaces.py`, `backend/app/api/errors.py`, `backend/app/api/contract.py`, `backend/app/main.py`, `backend/tests/test_workspace_repository.py`, `backend/tests/api/test_workspaces_api.py`, `backend/tests/api/test_public_api_contract_conformance.py`, `backend/tests/contracts/openapi_snapshot.json`, `backend/tests/contracts/schema_contract_snapshot.json`, `docs/TECH-DEBT.md`, `docs/PLANNED-SPRINTS.md`, `docs/SPRINT-PR-LOG.md`, `CHANGELOG.md`, `ROADMAP.md`. *(PR #170)* |
| #171 | **(30.2) Connection Registry API** (2026-09-07) — Phase 11'in üçüncü sprint'i: persistent, API-fronted bir connection registry (`/api/v1/connections` CRUD) — 30.0 kanonik sözleşmesindeki (`ApiResponse` envelope + offset `PageMeta`) ikinci kaynak ve **workspace'e bağlanan İLK** kaynak. Phase 7 connection domain modelini (`backend/app/evaluation/connection_abstraction.py`: `SQLConnectionProfile`/`SQLConnectionEndpoint`/`SQLConnectionSecretRef`/`SQLConnectionAuthMode`/`SQLConnectionAccessMode`/`SQLConnectionEnvironment`) validasyon için **yeniden kullanır** (yeniden inşa etmez); `connection_abstraction.py` ve `app/security/connection_credential_vault.py` import edilir, DÜZENLENMEZ. **Secret-by-reference (mimarinin dayattığı, güvenlik):** registry yalnız `secret_ref` `{provider,key}` pointer'ı saklar/döner, ham parola/connection-string ASLA — `SQLConnectionSecretRef.__post_init__` `scan_secret_patterns` ile URL/connection-string/ham-secret'i reddeder; DB'de ham-secret sütunu yok. Yeni `connections` tablosu `backend/app/database.py::init_db`'ye additive `CREATE TABLE IF NOT EXISTS` (`id TEXT PK` uuid, `connection_ref TEXT NOT NULL UNIQUE` business key, `workspace_id TEXT` nullable, `name`/`dialect`/`environment`/`host`/`port`/`database`/`access_mode`/`auth_mode` NOT NULL, `secret_provider`/`secret_key` nullable pointer, `max_rows`/`timeout_seconds`, timestamps) + `idx_connections_workspace_id` index. Yeni `backend/app/connection_repository.py`: `build_profile(...)` her yazmada `SQLConnectionProfile` kurup domain validasyonunu tetikler (READ_ONLY zorlama, endpoint/port 1..65535, auth-mode↔secret_ref eşleşmesi, ham-secret taraması); dialect/environment/auth_mode domain constructor'a **string** geçilir ki bad değer bare `ValueError`/500 yerine `SQLConnectionAbstractionContractError`/422'ye wrap olsun. `create_connection` (uuid id + timestamps, `access_mode="read_only"` sabit; `sqlite3.IntegrityError`→`ConnectionRefConflict` **yalnız** `connection_ref` UNIQUE ihlalinde, diğer integrity hataları yüzeye çıkar), `get_connection`/`get_by_ref`/`list_connections(limit,offset,workspace_id?)`/`update_connection`(column-whitelist `name/host/port/database/environment/auth_mode/secret_provider/secret_key/max_rows/timeout_seconds`, re-validate; **auth_mode→"none" geçişinde secret_provider/secret_key'i NULL'a çeker** — domain invariant'ı 'none' durumunu ulaşılabilir kılar, aksi halde kalıcı 422 çıkmazı olurdu)/`delete_connection`/`row_to_response_dict`(nested `endpoint`+`secret_ref` pointer, flat secret sütunları sızmaz). Yeni `verify_api_key` korumalı `backend/app/api/connections.py` router: `SecretRefModel`/`EndpointModel`/`ConnectionCreate`(connection_ref pattern `^[a-z0-9]+(-[a-z0-9]+)*$`, port 1..65535, dialect/environment/auth_mode enum-typed → geçersiz değer Pydantic'te 422)/`ConnectionUpdate`(≥1 alan, `model_validator`)/`ConnectionResponse`(ham secret alanı YOK). 5 endpoint, hepsi kanonik `ApiResponse`: `POST ""`(201; `workspace_id` verilmişse `workspace_repository.get_workspace` ile doğrulanır → yok ise **400 BAD_REQUEST**; `ConnectionRefConflict`→**409 CONFLICT**; `SQLConnectionAbstractionContractError`→**422**), `GET ""`(opsiyonel `?workspace_id=` filtresi + `limit`≤200/`offset` → `ApiResponse[list[ConnectionResponse]]` + `PageMeta`), `GET/PATCH/DELETE /{connection_id}`(yok→404; DELETE kaynağı döner + `delete_connection()` bool-gate concurrent'ta 404). `backend/app/main.py`'ye 1 import + 1 `include_router(connections_router)` (additive). `backend/tests/api/test_public_api_contract_conformance.py::EXPECTED_V1_ROUTES` 5 connection route'uyla genişletildi (dogfood — `test_every_v1_route_success_uses_canonical_envelope` her birini kanonik envelope'a zorlar). Contract snapshot'ları `python scripts/generate_contract_snapshots.py` ile **tamamen additive** regenerate — python ile doğrulandı: removed schemas/paths = boş, modified existing schemas = boş; yalnız yeni `/api/v1/connections`(+`/{connection_id}`) path'leri + `ConnectionCreate`/`ConnectionUpdate`/`ConnectionResponse`/`SecretRefModel`/`EndpointModel`/`ApiResponse_ConnectionResponse_`/`ApiResponse_list_ConnectionResponse__` + referanslanan enum şemaları (`SQLConnectionAuthMode`/`SQLConnectionEnvironment`/`SQLDatabaseDialect`) eklendi (git diff'teki tek `-` satırı `SchemaSyncEnvelopeResponse` title'ının re-anchor artifact'iydi, obje birebir aynı doğrulandı). Mevcut hiçbir tablo/endpoint/handler davranışı değişmedi; `connection_abstraction.py`/vault düzenlenmedi; connection route'ları korumalı → `test_protected_endpoints_require_api_key` security-allowlist değişikliği gerektirmedi. Standalone/inert: canlı DB bağlantısı açmaz, `secret_ref` çözmez. Full suite 2867 passed/37 skipped (tek local `test_pagerank_missing_scipy_does_not_log_error` fail'i pre-existing environment-only — scipy yerelde kurulu, 30.2'ye dokunulmadı, `main`'e göre boş diff, CI'da scipy yok → geçer). Whole-branch review (opus): 7 bulgu — 4 giderildi (secret_ref→none geçişi ulaşılabilir kılındı; enum-validasyon 422-wrap; IntegrityError narrowing; `workspace_id` index), 3 gerekçeyle atlandı (`conn.close()` yokluğu / per-page `count` / `utcnow` — üçü de `workspace_repository`/`database.py` codebase idiom'uyla paylaşımlı, tek modülde sapmak tutarsızlık olurdu). **Bilinçli kapsam dışı (TECH-DEBT §28, sessiz düşürme yok):** canlı connection testi/health-check/secret çözümü/rotation (→ 30.4/ileri), execution pipeline wiring (→ 30.4 Query Run), non-READ_ONLY access mode, per-workspace RBAC/sahiplik/paylaşım (→ AuthN 30.8), `connection_ref` mutasyonu/soft-delete/**workspace-delete cascade** (workspace silinince `workspace_id` dangling kalabilir), IAM secret detayları/TLS/pooling, cursor pagination + sayfa-üstü `total`. Spec: `docs/superpowers/specs/2026-09-07-sprint-30.2-connection-registry-api-design.md`; Plan: `docs/superpowers/plans/2026-09-07-sprint-30.2-connection-registry-api.md`. `backend/app/database.py`, `backend/app/connection_repository.py`, `backend/app/api/connections.py`, `backend/app/main.py`, `backend/tests/test_connection_repository.py`, `backend/tests/api/test_connections_api.py`, `backend/tests/api/test_public_api_contract_conformance.py`, `backend/tests/contracts/openapi_snapshot.json`, `backend/tests/contracts/schema_contract_snapshot.json`, `docs/TECH-DEBT.md`, `docs/PLANNED-SPRINTS.md`, `docs/SPRINT-PR-LOG.md`, `CHANGELOG.md`, `ROADMAP.md`. *(PR #171)* |
| #172 | **(30.3) Schema Sync API** (2026-09-07) — Phase 11'in dördüncü sprint'i: per-connection `/api/v1/schema-syncs` kaynağı (30.0 sözleşmesindeki üçüncü kaynak, connection'a bağlı) — bir connection'ın şema yapısını zaman içinde kaydeder + ardışık sync'ler arası **yapısal drift** tespit eder. **Phase 9 drift engine'ini** (`backend/app/schema/schema_signature.py`: `normalize_structure`/`compute_schema_signature`/`diff_structures`→`StructuralDrift.as_dict()`) birebir **reuse eder** (import, DÜZENLEMEZ). **Inert (30.2 desenini izler):** 30.2 connection'ları canlı bağlantı açmadığından sync de canlı DB introspection YAPMAZ — şema yapısı request'te `structure` alanıyla sağlanır (canlı introspection → 30.4/resolver wiring'e ertelendi); `normalize_structure`→`compute_schema_signature`→connection'ın önceki snapshot'ıyla `diff_structures`→append-only kayıt. Legacy 28.7 `/api/debug/schema/*` global-şema drift yüzeyi DOKUNULMADI (iki yüzey bir arada). Yeni `schema_syncs` tablosu `backend/app/database.py::init_db`'ye additive (`id TEXT PK` uuid, `connection_id TEXT NOT NULL`, `signature`, `previous_signature` nullable, `drifted INTEGER`, `structure_json`/`drift_json` TEXT [JSON], `created_at`) + `idx_schema_syncs_connection_id` index. Yeni `backend/app/schema_sync_repository.py`: `create_schema_sync(connection_id, schema)` (bozuk structure [dict değil ya da `tables` mapping yok] → `ValueError`; **baseline sync `diff_structures({}, new)` = "hepsi added"** → `drifted=True` ile payload tutarlı [review finding 6]; `json.dumps` ile structure/drift saklanır), `get_schema_sync`/`get_latest_for_connection`/`list_schema_syncs(limit,offset,connection_id?)`/`delete_schema_sync`/`row_to_response_dict` (JSON alanları parse eder). **Ordering `ORDER BY rowid DESC` (sqlite monotonik insertion key)** — coarse `created_at`+random-uuid tiebreak yerine (review finding 1/2: aynı-timestamp'te yanlış "previous" snapshot seçilip yanlış drift/previous_signature üretilmesi + kararsız pagination çözüldü; rapid-sequential-sync testiyle kilitlendi). Yeni `verify_api_key` korumalı `backend/app/api/schema_syncs.py`: `SchemaSyncCreate{connection_id, structure}` (request/JSON alanı **`structure`** — `schema` alan adı Pydantic `BaseModel.schema`'yı gölgeler VE `Field(alias="schema")` FastAPI body-context'te `UnsupportedFieldAttributeWarning` verir; `structure` ikisini de düşürür ve `SchemaSyncResponse.structure` ile simetrik), `SchemaSyncResponse{id,connection_id,signature,previous_signature?,drifted,drift,structure,created_at}`; 4 endpoint hepsi kanonik `ApiResponse`: `POST ""`(201; `connection_repository.get_connection` ile connection doğrulanır → yok **400 BAD_REQUEST**; repo `ValueError`→**422**), `GET ""`(`?connection_id=` filtre + `limit`≤200/`offset` + `PageMeta`), `GET/DELETE /{sync_id}`(yok→404; DELETE silinen kaydı döner + `delete_schema_sync()` bool-gate); **PATCH YOK** (append-only immutable history). `backend/app/main.py`'ye 1 import + 1 `include_router(schema_syncs_router)` (additive). `backend/tests/api/test_public_api_contract_conformance.py::EXPECTED_V1_ROUTES` 4 schema-sync route'uyla genişletildi (dogfood). Contract snapshot'ları `python scripts/generate_contract_snapshots.py` ile **additive** regenerate — python ile **main'e göre** doğrulandı: removed = boş, modified existing = boş; yalnız `/api/v1/schema-syncs`(+`/{sync_id}`) path'leri + `SchemaSyncCreate`/`SchemaSyncResponse`/`ApiResponse_SchemaSyncResponse_`/`ApiResponse_list_SchemaSyncResponse__` şemaları eklendi. Mevcut hiçbir tablo/endpoint/handler davranışı değişmedi; `schema_signature.py`/28.7 reuse/düzenlenmedi; schema-sync route'ları korumalı → `test_protected_endpoints_require_api_key` security-allowlist değişikliği gerektirmedi. Full suite 2883 passed/37 skipped (tek local `test_pagerank_missing_scipy_does_not_log_error` fail'i pre-existing environment-only — scipy yerelde kurulu, 30.3'e dokunulmadı, `main`'e göre boş diff, CI'da scipy yok → geçer). Whole-branch review (opus): 6 bulgu — 3 giderildi (rowid ordering [finding 1/2], baseline drift tutarlılığı [finding 6] + rapid-sync testi, request alanı `structure` [alias warning + reserved-shadow]), 3 gerekçeyle atlandı (`conn.close()` yokluğu / DELETE bool-gate = 30.1/30.2 codebase idiom'u; `utcnow` artık ordering'i beslemiyor). **Bilinçli kapsam dışı (TECH-DEBT §29, sessiz düşürme yok):** canlı schema introspection (şema request'te sağlanır; canlı DB'ye bağlanıp secret_ref çözme → 30.4/resolver wiring), registry'den tetikleme/zamanlama/auto-sync, workspace-scoping, retention/pruning, sync PATCH mutasyonu (append-only), connection-delete cascade (connection silinince sync'ler dangling `connection_id` ile kalır — 30.2 §28.5 ile tutarlı), legacy 28.7 global-şema drift ile uzlaştırma (iki yüzey bir arada), cursor pagination + sayfa-üstü `total`. Spec: `docs/superpowers/specs/2026-09-07-sprint-30.3-schema-sync-api-design.md`; Plan: `docs/superpowers/plans/2026-09-07-sprint-30.3-schema-sync-api.md`. `backend/app/database.py`, `backend/app/schema_sync_repository.py`, `backend/app/api/schema_syncs.py`, `backend/app/main.py`, `backend/tests/test_schema_sync_repository.py`, `backend/tests/api/test_schema_syncs_api.py`, `backend/tests/api/test_public_api_contract_conformance.py`, `backend/tests/contracts/openapi_snapshot.json`, `backend/tests/contracts/schema_contract_snapshot.json`, `docs/TECH-DEBT.md`, `docs/PLANNED-SPRINTS.md`, `docs/SPRINT-PR-LOG.md`, `CHANGELOG.md`, `ROADMAP.md`. *(PR #172)* |
| #173 | **(30.4) Query Run API** (2026-09-08) — Phase 11'in beşinci sprint'i: **inert** per-connection `/api/v1/query-runs` ledger'ı (30.0 sözleşmesindeki dördüncü kaynak, connection'a bağlı) — bir connection'a karşı SQL run'larını kaydeder: SQL + opsiyonel **sağlanan sonuç** (result satırları) ya da **execution_error**. **Kullanıcı-onaylı kapsam: inert/record-only** — 30.4 hiçbir şey çalıştırmaz, `secret_ref` çözmez (canlı execution + secret resolution güvenlik-hassas/geri-alınması zor ilk adım; kullanıcıya soruldu, "inert/kayıt-only" seçildi; sonraki sprint'e ertelendi). 30.3'ün "schema request'te sağlanır" deseninin executor karşılığı: 30.3 sağlanan şemayı, 30.4 sağlanan sonucu saklar. Yeni `query_runs` tablosu `backend/app/database.py::init_db`'ye additive (`id TEXT PK` uuid, `connection_id TEXT NOT NULL`, `sql TEXT NOT NULL`, `status TEXT NOT NULL` [recorded/succeeded/failed], `row_count INTEGER`?, `columns_json`/`rows_json` TEXT [JSON]?, `truncated INTEGER`?, `execution_error TEXT`?, `duration_ms REAL`?, `created_at`) + `idx_query_runs_connection_id` index. Yeni `backend/app/query_run_repository.py`: `create_query_run(connection_id, sql, result=None)` — **boş/whitespace sql** persistence sınırında `ValueError` (invariant yalnız Pydantic'te kalmasın); status türetme: `result is None`→`recorded`; `result.get("execution_error") is not None` (**key-presence, boş-string dahil** — truthiness DEĞİL, yoksa boş-string hata sessizce success olurdu)→`failed`; aksi→`succeeded` (`row_count=len(rows)`, columns/rows JSON); `get_query_run`/`list_query_runs(limit,offset,connection_id?,status?)` (**ORDER BY rowid DESC** monotonik insertion) / `delete_query_run` (bool) / `row_to_response_dict` (JSON parse; `result` yalnız succeeded'te, `execution_error` top-level). Yeni `verify_api_key` korumalı `backend/app/api/query_runs.py`: `QueryRunResult{columns:list[str], rows:list[list], truncated:bool, duration_ms?}`/`QueryRunCreate{connection_id, sql:Field(min_length=1), result?, execution_error?}` (`model_validator`: result+execution_error **birlikte verilirse 422** — sessiz result-drop yerine)/`QueryRunResponse{id,connection_id,sql,status,row_count?,result?,execution_error?,created_at}`; 4 endpoint hepsi kanonik `ApiResponse`: `POST ""`(201; `connection_repository.get_connection`→yok **400 BAD_REQUEST**; repo `ValueError`→**422** [try/except; whitespace-only sql `min_length=1`'i geçse de yakalanır]; outcome: execution_error→failed, result→succeeded, neither→recorded), `GET ""`(`?connection_id=`+`?status=` filtre + `limit`≤200/`offset` + `PageMeta`), `GET/DELETE /{run_id}`(yok→404; DELETE kaydı döner+bool-gate); **PATCH YOK** (append-only immutable history). Result şekli 29.x `SQLDatabaseExecutionResult` (columns/rows/truncated/duration_ms) ile hizalı — yalnız field template, import/construct EDİLMEZ. `backend/app/main.py`'ye 1 import + 1 `include_router(query_runs_router)` (additive). `EXPECTED_V1_ROUTES` 4 query-run route'uyla genişletildi (dogfood). Contract snapshot'ları additive regenerate — python ile **main'e göre** doğrulandı: removed=boş, modified existing=boş; yalnız `/api/v1/query-runs`(+`/{run_id}`) + `QueryRunCreate`/`QueryRunResult`/`QueryRunResponse`/`ApiResponse_QueryRunResponse_`/`ApiResponse_list_QueryRunResponse__`. Mevcut hiçbir tablo/endpoint/handler davranışı değişmedi; query-run route'ları korumalı → `test_protected_endpoints_require_api_key` security-allowlist değişikliği gerektirmedi. Full suite 2900 passed/37 skipped (tek local `test_pagerank_missing_scipy_does_not_log_error` fail'i pre-existing environment-only — scipy yerelde kurulu, 30.4'e dokunulmadı, CI'da yok→geçer). Whole-branch review (opus): 5 bulgu — 4 giderildi (boş-string execution_error→failed [finding 1], result+error birlikte→422 [finding 3], boş-sql repo sınırı+422 [finding 4], ölü failed-branch duration_ms kaldırıldı [finding 2]), 1 gerekçeyle atlandı (`utcnow` = `database.py`/`*-repository` codebase idiom'u [finding 5]). **Bilinçli kapsam dışı (TECH-DEBT §30, sessiz düşürme yok):** canlı execution (`secret_ref` çözme + read-only bağlantı + gerçek SELECT via 29.x adapter — güvenlik-hassas/kullanıcı-onaylı → 30.x), NL→SQL üretimi (30.4 SQL run kaydeder, NL'den üretmez — pipeline/`jobs`), result pagination/streaming/boyut-limiti/iptal/retry, failed-run latency, workspace-scoping, connection-delete cascade (connection silinince run'lar dangling `connection_id` ile kalır), cursor pagination + sayfa-üstü `total`, run PATCH mutasyonu. Spec: `docs/superpowers/specs/2026-09-08-sprint-30.4-query-run-api-design.md`; Plan: `docs/superpowers/plans/2026-09-08-sprint-30.4-query-run-api.md`. `backend/app/database.py`, `backend/app/query_run_repository.py`, `backend/app/api/query_runs.py`, `backend/app/main.py`, `backend/tests/test_query_run_repository.py`, `backend/tests/api/test_query_runs_api.py`, `backend/tests/api/test_public_api_contract_conformance.py`, `backend/tests/contracts/openapi_snapshot.json`, `backend/tests/contracts/schema_contract_snapshot.json`, `docs/TECH-DEBT.md`, `docs/PLANNED-SPRINTS.md`, `docs/SPRINT-PR-LOG.md`, `CHANGELOG.md`, `ROADMAP.md`. *(PR #173)* |
| #174 | **(30.5) Query History API** (2026-09-08) — Phase 11'in altıncı sprint'i: **read-only** history/analytics yüzeyi (`/api/v1/query-history` + `/api/v1/query-history/summary`) — 30.4 `query_runs` ledger'ı üzerinde okur (`connections` LEFT JOIN ile workspace scope). **Yeni tablo/yazma YOK** — per-resource `GET /api/v1/query-runs` list'inin (yalnız connection_id/status filtreli) ötesinde değer katan read katmanı: workspace scope (connection→workspace join), `since`/`until` `created_at` aralığı, `q` SQL substring araması, status-sayım özeti. Yeni `backend/app/query_history_repository.py` (saf okuma, tablosuz; tek paylaşımlı parameterized `_where(connection_id,workspace_id,status,since,until,q)` builder — her değer bound parametre, yalnız sabit kolon adları interpolate → injection-safe): `list_history(limit,offset,...filters)` (`SELECT qr.id,qr.connection_id,c.workspace_id,qr.sql,qr.status,qr.row_count,qr.created_at FROM query_runs qr LEFT JOIN connections c ON qr.connection_id=c.id{where} ORDER BY qr.rowid DESC LIMIT ? OFFSET ?` — **hafif satırlar, result rows YOK**) + `summary(...filters)` (`GROUP BY qr.status` → `{total, by_status:{recorded:n,succeeded:n,failed:n}}`, eksik status 0). `q` filtresi LIKE metachar escape'li: `_escape_like` (`\`→`\\`, `%`→`\%`, `_`→`\_`) + clause `qr.sql LIKE ? ESCAPE '\'` → free-text literal eşleşir, `%`/`_` wildcard'a dönmez. Yeni `verify_api_key` korumalı `backend/app/api/query_history.py`: `QueryHistoryEntry{id,connection_id,workspace_id?,sql,status,row_count?,created_at}`/`QueryHistorySummary{total,by_status:dict[str,int]}`; `GET ""` (`connection_id?`/`workspace_id?`/`status?`/`since?`/`until?`/`q?` + `limit`≤200/`offset` → `ApiResponse[list[QueryHistoryEntry]]` + `PageMeta`), `GET "/summary"` (aynı filtreler → `ApiResponse[QueryHistorySummary]`). **POST/PATCH/DELETE YOK** (read-only; history 30.4 `query_runs`'tan türetilir). Bilinmeyen filtre değeri (connection/workspace/status) → boş sayfa / sıfır sayım (**400 yok, bilinçli** — filtre, referans değil; 30.4 status-filtresiyle tutarlı). `backend/app/database.py`'ye `idx_query_runs_created_at` index (yeni tarih-aralığı/summary scan deseni; additive `CREATE INDEX IF NOT EXISTS`). `backend/app/main.py`'ye 1 import + 1 `include_router(query_history_router)` (additive). `EXPECTED_V1_ROUTES` 2 history route'uyla genişletildi (dogfood). Contract snapshot'ları additive regenerate — python ile main'e göre doğrulandı: removed=boş, modified existing=boş; yalnız `/api/v1/query-history`(+`/summary`) path'leri + `QueryHistoryEntry`/`QueryHistorySummary`/`ApiResponse_list_QueryHistoryEntry__`/`ApiResponse_QueryHistorySummary_` şemaları. Mevcut hiçbir tablo/endpoint/handler davranışı değişmedi; query-history route'ları korumalı → `test_protected_endpoints_require_api_key` security-allowlist değişikliği gerektirmedi. Full suite 2917 passed/37 skipped (tek local `test_pagerank_missing_scipy_does_not_log_error` fail'i pre-existing environment-only — scipy yerelde kurulu, 30.5'e dokunulmadı, CI'da yok→geçer). Whole-branch review (opus): 5 bulgu — 3 giderildi (`q` LIKE-metachar escape [finding 1], kullanılmayan `count_history` kaldırıldı — cross-page total `/summary`'de [finding 3], `idx_query_runs_created_at` [finding 4]), 2 kararla ele alındı (`since`/`until` lexicographic-ISO string karşılaştırması — mikrosaniye `created_at`'e karşı kısa-hassasiyetli `until` under-select eder; §31.7'de bilinen-sınır olarak belgelendi [finding 2]; `conn.close()` yokluğu `query_run_repository`/`database.py` codebase idiom'uyla tutarlı bırakıldı [finding 5]). **Bilinçli kapsam dışı (TECH-DEBT §31, sessiz düşürme yok):** yazma (history türetilmiş, run'lar 30.4'ten), cursor pagination + list'te sayfa-üstü `total` (`/summary` total verir), full result satırları (→ `GET /query-runs/{id}`), zaman-kovalı/connection-gruplu analytics + latency percentile, filtre-referans varlık doğrulaması (bilinmeyen→boş, bilinçli), `since`/`until` sub-precision normalize, legacy `jobs`/`query_traces` NL2SQL geçmişiyle uzlaştırma. Spec: `docs/superpowers/specs/2026-09-08-sprint-30.5-query-history-api-design.md`; Plan: `docs/superpowers/plans/2026-09-08-sprint-30.5-query-history-api.md`. `backend/app/query_history_repository.py`, `backend/app/api/query_history.py`, `backend/app/database.py`, `backend/app/main.py`, `backend/tests/test_query_history_repository.py`, `backend/tests/api/test_query_history_api.py`, `backend/tests/api/test_public_api_contract_conformance.py`, `backend/tests/contracts/openapi_snapshot.json`, `backend/tests/contracts/schema_contract_snapshot.json`, `docs/TECH-DEBT.md`, `docs/PLANNED-SPRINTS.md`, `docs/SPRINT-PR-LOG.md`, `CHANGELOG.md`, `ROADMAP.md`. *(PR #174)* |
